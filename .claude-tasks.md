# Task Log

## 2026-07-23

- **[STYLE]** "Enquire this Property" CTA: desktop-friendly size, spaced below the details rows, animated to invite clicks (client: "Make this button more desktop friendly brother you can move it down a little bit maybe and make it like an animated so user will click on it")
  - Files changed: `resources/js/Website/Sections/PropertyDetail/PropertyGallery.jsx`, `resources/css/app.css`
  - Details: The button hugged the Exposure row and shrank to h-10 on desktop. Now: h-12 at every breakpoint (dropped md:h-10), mt-6 md:mt-8 pushes it down off the last detail row, and a new .enquire-cta class (app.css) animates it - a soft radiating pulse ring every 2.4s (box-shadow keyframes, deliberately NOT transform so it can't fight the hover state), a periodic white shine sweep via a skewed ::after gradient every 3.2s (pointer-events none), hover scale(1.03) lift + active scale(0.98) press replacing the old hover:opacity-90, all disabled under prefers-reduced-motion. Site CSS not widget CSS, so app.css only (bunpilot.css untouched). Brand buttonTertiaryBg/Text inline colors untouched. VERIFIED: compiled app-*.css carries 12 enquire-cta rule references, SSR HTML renders the new class string with mt-6 md:mt-8 and zero md:h-10 leftovers, client+SSR build clean. Server needs: git pull, npm run build, restart SSR daemon.

- **[FIX]** Detail labels font-medium -> font-semibold; medium rendered identical on the live site (client: "still it not bold why" with wellcondostoronto.ca screenshot)
  - Files changed: `resources/js/Website/Sections/PropertyDetail/PropertyGallery.jsx`
  - Details: The first pass used font-medium (500), but `.font-work-sans` is a LEGACY ALIAS - app.css:99 maps it (plus font-space-grotesk/font-red-hat) to plain 'Helvetica','Arial',sans-serif, NOT Work Sans - and Arial ships only regular(400) + bold(700) faces, so per CSS font-matching a 500 target falls back DOWN to 400 and renders pixel-identical to the values; the bunny.net Work Sans 400/500/600/700 load is a red herring since the family is never referenced by these classes. Labels bumped to font-semibold (600), which resolves UP to the bold face and is visibly thicker; values stay font-normal. VERIFIED via SSR HTML fetch of the same listing: 8 font-semibold labels, 0 font-medium leftovers, centered Details h2 intact; client+SSR build clean. Server needs: git pull, npm run build, restart SSR daemon.

- **[FIX]** Listing detail card polish + price history sign-in gate (client screenshots: "These needs to be slightly thicker font then the descriptions of it on the right", "Make this centered so it'll look more proper", "price history shouldnt be live without user sign in")
  - Files changed: `resources/js/Website/Sections/PropertyDetail/PropertyGallery.jsx`, `resources/js/Website/Components/PropertyDetail/PriceHistory.jsx`, `resources/js/Pages/PropertyPriceHistory.jsx`, `resources/js/Pages/BuildingPriceHistory.jsx`
  - Details: (1) The 8 detail labels in the gallery details card (Type/Beds/Bathrooms/Area/Parking/Maintenance Fees/Property Taxes/Exposure) go font-normal -> font-medium while the values stay font-normal, so labels read slightly heavier than the descriptions on the right (label class signature is unique in the file - 8/8 verified before replace_all; the identical-looking PropertySidebar.jsx is imported nowhere, left untouched). (2) The "%h1% Details" h2 above the rows gains text-center. (3) Price history re-gated behind sign-in, reversing d32cc50 which had made it public: PriceHistory.jsx reads Inertia shared auth (auth prop OR usePage().props.auth) and for guests caps the list to ONE row with every data field blurred (blur-sm select-none), status swapped for a rose "Sign in required", the whole row click/keyboard-activatable to onLoginClick (fallback router.visit('/login')), and the footer swaps "View full price history" for a "Sign in to view price history" CTA; the guest cap deliberately overrides showAll so the dedicated pages can't bypass the gate. PropertyPriceHistory.jsx (/price-history/{listingKey}) now hosts its own LoginModal and passes auth + onLoginClick; BuildingPriceHistory.jsx (/{city}/{slug}/price-history) renders its own rows so it gets an early-return guest gate (title/subtitle kept, lock icon + "Sign in to view the full listing history" card matching the MarketData gate, LoginModal) placed after all hooks. VERIFIED locally over HTTP with SSR running via the ?website=nobu-residences preview override on /mls/ottawa/homes-for-rent/179-george-street-X13596260: guest HTML has 8 font-medium labels / 0 font-normal leftovers, text-center on the Details h2, 1 blurred "Sign in required" row + "Sign in to view price history" CTA and ZERO "View full price history"; /price-history/X13596260 guest shows the same gate; /toronto/the-skyline-tower/price-history guest shows the gate card with no rows and no filter UI; then a real curl login (ct CSRF cookie + sid session, temp user, 302) and re-fetch of all three pages shows 0 blur, 0 gate CTAs, the full-history link back, filter UI back, and the 8 font-medium labels intact; client+SSR build clean; temp user and scratch files deleted. Server needs: git pull, npm run build, restart SSR daemon.

## 2026-07-22

- **[FEATURE]** New registrants auto-subscribe to the building's daily listings alert, with the BUILDING logo and a personalized subject/body (client: "Exactly bro let's do this one as well... make sure that email will have building logo... subject and body content will be customized for the receiver it'll contain their name")
  - Files changed: `app/Support/AutoBuildingAlert.php` (new), `app/Http/Controllers/Auth/RegisteredUserController.php`, `app/Http/Controllers/Auth/GoogleAuthController.php`, `app/Notifications/SavedSearchAlertNotification.php`, `app/Support/EmailBranding.php`, `app/Models/EmailTemplate.php`
  - Details: New AutoBuildingAlert::subscribe(User, ?Website) creates the EXACT SavedSearch row the "Get alerts" button creates (building_id idempotence key, name "Alerts: {building}", search_params building_id + comma-joined street addresses + status Both, email_alerts true, frequency 1 daily, website_id) so the existing alerts:send-saved-search scheduler, the site's Get-alerts toggle state, and unsubscribe all work unchanged; with last_alert_sent null the first daily run covers the past day's listings — "starts from the next day", no flood. Guarded try/catch (never breaks signup), no-op when the site has no linked building. Wired after new-user creation in all three registration paths: email form (RegisteredUserController::store — now resolves the full Website instead of just ?->name), Google relayed callback (handleGoogleCallback — website already resolved from the relay origin), and the stateless Google callback() (same full-Website resolution added); existing-user Google sign-ins deliberately do NOT subscribe. Building logo: new EmailBranding::logoOverride(logoValue, branding) absolutizes the building's logo and maps it to a local file path, and SavedSearchAlertNotification feeds the override into the template's battle-tested inline-embed (cid:) chain when savedSearch->building has a logo — site logo fallback for plain saved searches / logo-less buildings. Personalization: saved_search_alert built-in defaults now lead with the recipient's name — subject '%first_name%, New Listings Match "%search_name%" - %listing_count% Properties Found' and intro "Hi %first_name%, thank you for registering for the %frequency% Listings Alert at %site_link%..." (admins can still override per site; %first_name% was already in mergeValues). VERIFIED via tinker: subscribe() twice → exactly one correctly-shaped row (frequency 1, alerts true, street_addresses populated, last_alert_sent null) and a clean null-website guard; full RegisteredUserController::store() invocation with a tenant-resolved request → user created + "Alerts: The Skyline Tower" auto-created (freq 1, alerts on); array-mailer render of SavedSearchAlertNotification with a building logo set → subject "Test, New Listings Match \"Alerts: The Skyline Tower\" - 1 Properties Found", "Hi Test," intro, 2 inline cid attachments (building logo + agent photo), card renders; php -l clean on all six files; test users/temp scripts cleaned up. No migration needed. Server: git pull only.

- **[FIX]** Client feedback batch: FUB "PCD IDX" label + tour appointments, always-related amenity icons, "About %building_name%" homepage heading, search map sync/draw/filter fixes (screenshots 18-23)
  - Files changed: `config/services.php`, `app/Http/Controllers/Api/TourRequestController.php`, `resources/js/utils/amenityIcon.js` (new), `resources/js/Website/Components/PropertyDetail/Amenities.jsx`, `BuildingAmenities.jsx`, `resources/js/Website/Sections/Home/AmenitiesSection.jsx`, `BuildingInfo.jsx`, `resources/js/Website/Pages/Search.jsx`, `resources/js/Website/Components/ClusteredPropertyMap.jsx`
  - Details: (18) FUB events said "Added ... by Nobu Residences" because services.followupboss.system fell back to APP_NAME — config default is now 'PCD IDX' (env FUB_SYSTEM still overrides); per-tenant `source`/tags untouched. (19) Tour requests never became FUB appointments: ViewingRequestModal sends selected_time as Morning/Afternoon/Evening and parseTourSlot only understood "9AM to 12PM" ranges, so Carbon::parse('date Morning') threw → silent skip; word slots now map to the widget's advertised ranges (9AM-12PM / 12PM-4PM / 4PM-8PM) and empty time defaults to 9AM instead of midnight — VERIFIED via tinker reflection: all word slots, range format, null time and null date behave correctly, and config('services.followupboss.system')='PCD IDX'. (20) Amenity chips all showed the generic star because tenant amenity rows have null icon values and the exact-name maps missed real names ("Fitness Centre", "24-Hour Concierge"...) — new shared resolveAmenityIconPath() (DB URL → legacy exact map → ordered keyword regex matcher over the existing /assets/svgs library → amenity-default) wired into all three renderers (property/building Amenities, BuildingAmenities, homepage AmenitiesSection incl. replacing its inline diamond fallback) — VERIFIED: all 14 amenity names from the client screenshot resolve to themed icons (node test), homepage SSR emits themed svg paths, no diamond path in HTML. (21) BuildingInfo heading "A landmark address on {address}" → "About {building name}" (already h2) — VERIFIED in homepage SSR: "About The Skyline Tower". (22/23) Search map: content ternary reordered so map/mixed views no longer UNMOUNT the map on every fetch (isLoading now only gates grid view; mixed uses its inner left-pane loader); new refs (drawnPolygonRef/viewportBoundsRef/searchFiltersRef/performSearchRef) + performSearch options.viewportBounds param; handlePolygonDraw now actually refetches the list scoped to the drawn polygon (page 1; Reset clears) — backend already mapped viewport_bounds.polygon to the Repliers map param; new ClusteredPropertyMap onViewportChange prop fires debounced (600ms) ONLY for user pans/zooms (suppressNextIdleNotifyRef starts true and re-arms before every programmatic fitBounds/setCenter — double loop guard: the viewport handler also never touches searchFilters so mapSearchFilters identity stays stable), wired on the mixed-view instance so panning refetches the left list as an exact rectangle polygon ring (3-decimal rounding for cache stability); page+sort stripped from the mapSearchFilters memo so pagination/sorting no longer wipes/refetches markers; new-location searches (search bar, handleSearch, tab change) clear the viewport scope; filter bar wrapper becomes sticky top-0 in full map view + scrollIntoView on entering map view (bar was in the DOM but scrolled out of sight). VERIFIED: php -l clean, client+SSR build clean, /search still server-renders with single title, SSR log clean. Map pan/draw/sticky interactions need a quick manual browser smoke test after deploy (headless env here can't drive Google Maps). Server needs: git pull, npm run build, restart SSR daemon, php artisan config:cache.

- **[FEATURE]** Listing-page heading hierarchy per client markup + listing brokerage removed everywhere (client screenshot batch: images 5-17)
  - Files changed: `resources/js/utils/listingTitle.js` (new), `resources/js/Website/Global/Components/PropertyHeader.jsx`, `resources/js/Website/Components/PropertyDetail/PropertyDetailsSections.jsx`, `Amenities.jsx`, `PriceHistory.jsx`, `MoreBuildings.jsx`, `ComparableSales.jsx`, `RealEstateLinksSection.jsx`, `resources/js/Website/Sections/PropertyDetail/PropertySections.jsx`, `PropertyGallery.jsx`, `resources/js/Website/Global/Components/PropertyCards/PropertyCardV1|V2|V5|V6.jsx`, `resources/js/Website/Sections/Home/ListingCarousel.jsx`
  - Details: New shared getListingH1() util (the "{unit} - {street}, {city}" H1 text) now feeds PropertyHeader's H1 AND the two new H2s so they never drift. Heading changes per the client images: Overview card → h2 titled "Overview of %h1%" (shared Card component gained a headingTag prop, default h3 so Property Details/rooms cards are untouched); tab labels (Overview/Property rooms/Amenities/Mortgage Calculator/Nearby schools) span→h3; Amenities sub-heads "Building Features & Services"/"Included in Maintenance Fees" h3→h4 (also applies on BuildingDetail which reuses the component); "Price History" h2→h3; MoreBuildings + ComparableSales titles got a headingTag prop — property page (PropertySections) passes h3 for "Nearby Listings"/"Comparable Sales" while BuildingDetail grids keep h2; RealEstateLinksSection (PropertyDetail copy) three section heads h2→h4 with the "Downtown Core"-style category heads demoted h3→h5 to keep order sane; PropertyGallery price row ("FOR SALE $709,999") div→h2, "Properties detail" h3→h2 retitled "%h1% Details", the 8 detail-row labels (Type/Beds/Bathrooms/Area/Parking/Maintenance Fees/Property Taxes/Exposure) span→h3 and their values span→p (16 spans replaced via the two unique class signatures + unique close-tag pattern, counts verified 8/8/8 before replace_all). Brokerage removal — MLS listing office only, never the site's own agent_info brokerage: Overview MLS line now "MLS®: {number}" (office pick dropped), brokerage blocks + now-unused getBrokerageName imports removed from PropertyCardV1/V2/V5/V6 (V5 also from its contentSections height calc), homepage ListingCarousel listing.broker line removed; getBrokerageName util itself kept (debug tooling references), EnhancedPropertyCard left as-is (zero importers, dead). VERIFIED locally over HTTP with SSR running on /mls/toronto/homes-for-sale/318-king-street-e-C13591536: exactly one h1; h2 set = breadcrumb (pre-existing) + "FOR SALE $419,900" + "317 - 318 King Street E, Toronto Details" + "Overview of 317 - 318 King Street E, Toronto"; h3 set includes all 8 detail labels + all tabs + Price History + Nearby Listings + Comparable Sales; h4 set includes the three GTA-links heads; MLS line renders without "ONE PERCENT REALTY"; building page still 200 with "Properties For Sale/Rent" h2; client+SSR build clean, SSR log shows only the pre-existing fetchPriority warning. Server needs: git pull, npm run build, restart SSR daemon.

- **[FEATURE]** Client SEO spec implemented: clean /for-sale + /for-rent tenant pages, homepage count-based meta templates, and the /mls/ listing URL migration with 301s (client doc "SYSTEM URL Structures and SEO Settings")
  - Files changed: `routes/web.php`, `app/Http/Controllers/WebsiteController.php`, `app/Helpers/ListingUrlBuilder.php` (new), `app/Helpers/PropertyUrlHelper.php` (deprecated-in-place), `app/Http/Controllers/SitemapController.php`, `resources/views/app.blade.php`, `resources/js/Website/Pages/Search.jsx`, `resources/js/Website/Pages/BuildingDetail.jsx`, `resources/js/Website/Home.jsx`, `resources/js/Website/Global/Components/PropertyHeader.jsx`, `resources/js/Website/Global/Navbar.jsx`, `resources/js/Website/Sections/Home/HeroSection.jsx`, `resources/js/Website/Components/PropertyDetail/MerchandiseLofts.jsx`, `resources/js/utils/propertyUrl.js`
  - Details: (1) New domain-root /for-sale + /for-rent pages per tenant building: renderTenantBuildingSearch() pulls live counts (Building::getLiveListingCounts), renders Search.jsx with the client's H1/meta templates ("%name% Condos %city% Unit for Rent | N Available" etc.), zero-count no-number fallback so a SERP never shows "0 condos", self-canonical with ?page=N the only surviving param (new seo.canonical override in app.blade.php), Schema building+FAQ JSON-LD; legacy /search?query=..&building_id=UUID links 301 there ONLY when the UUID is the tenant's own building and no extra filters are present (bedrooms/price/sold links keep the search page); navbar/hero/MerchandiseLofts (tenant building only) now emit the clean paths; sitemap gains both URLs. Search.jsx consumes seo.title in its <Head> (the blade promotes the SSR title over the seo prop, so a hardcoded Head title would shadow it — same fix applied to BuildingDetail.jsx and Home.jsx), renders the server pageH1, and performSearch() no longer writes query/status/building_id back into the /for-sale|/for-rent address bar. (2) Homepage meta title/description per client templates with both live counts + zero-count fallback, on BOTH homepage modes (Welcome and use_building_as_homepage via renderBuildingDetailPage seoOverrides). (3) Listing pages migrated to /mls/: canonical URLs built by new ListingUrlBuilder (backend) mirrored byte-for-byte by rewritten propertyUrl.js generatePropertyUrl (all 9 card/consumer call-sites) — condo form /mls/{city}/{building-slug}/{street-slug}/unit-{unit}-{MLS} when a Building matches, /mls/{city}/homes-for-{sale|rent}/{street}-{MLS} otherwise; three new routes (homes form, 4-segment condo form, collapsed 3-segment doc variant that 301s to 4-segment; Laravel binds route params POSITIONALLY so the collapsed variant needs its own propertyDetailMlsShort action) with "mls" added to every {city} negative-lookahead; the old canonical block in propertyDetail() replaced by one ListingUrlBuilder check that 301s every legacy shape (/{city}/{address}/{mls}, old building-rich-slug form, /property/{mls} now a single-hop 301 via light matchBuildingSlugForListing; DB_/numeric backend properties exempt); both building-match queries get orderBy('id') so the canonical never ping-pongs; meta title/description replaced with the client templates incl. formatPriceShort ($419,900→$420K), Bd/Ba + /mo rent variants, condo-vs-home noun via Repliers class (case-insensitive — API returns 'CondoProperty'), streetDirection added to formatRepliersPropertyData/street label/slugs; PropertyHeader H1 now "unit - street w/ suffix+direction, City" (dropped the append-"Street" heuristic), matched-building breadcrumb order neighbourhood→sub→city per client screenshot (Repliers fallback keeps neighborhood→area→city since area is the TRREB region), dead '#' breadcrumb links render as plain text; listing Schema.org left as TODO (client doc says PENDING), location + school pages deferred (spec incomplete). VERIFIED locally over HTTP with SSR running: /for-rent & /for-sale single title/desc/H1 exactly per template with primed counts (5/3) and with zero counts; canonical bare vs ?page=3 self-canonical vs utm stripped; /search?building_id 301s (rent→/for-rent, default→/for-sale, page=2 preserved, other-building/bedrooms/plain all 200); homepage title "The Skyline Tower Condos in Toronto | For Sale 5 | For Rent 3"; /property/C13058782 and the collapsed /mls form both 301 once to /mls/toronto/the-skyline-tower/100-king-street-w/C13058782 which renders 200 stable ×3 (loop check); unmatched condo 318 King St E → homes-for-sale form with "317 - 318 King Street E, Toronto" H1 + "Moss Park, Toronto" breadcrumb; rental C13591542 title "1304 - 95 Lombard Street Toronto | 2 Bd 1 Ba for Rent | $2,850/mo"; /toronto/for-sale|for-rent/blogs still 200, junk /mls 404s, sitemap carries /for-sale + /for-rent; php -l clean, client+SSR build passes, SSR log clean. Server needs: git pull, npm run build, restart SSR daemon.

- **[FIX]** SSR now renders every public page with its own title - search/for-sale/for-rent pages fell back to client-only rendering and every page's title was shadowed by the site default (client: "ssr enabled now check again universalcitycondos.com")
  - Files changed: `resources/views/app.blade.php`, `resources/js/Website/Pages/Search.jsx`, `resources/js/Website/Components/ClusteredPropertyMap.jsx`, `resources/js/Website/Components/ViewportAwarePropertyMap.jsx`, `resources/js/services/frontendGeocoding.js`
  - Details: With the Ploi SSR daemon finally running (bootstrap/ssr/ssr.js), live checks showed the homepage/blogs/contact/building pages server-render but (a) every page carried TWO title tags - the blade's site-default first, the SSR per-page one second - and crawlers take the FIRST, so per-page titles never counted; (b) /search and /{city}/for-sale|for-rent didn't SSR at all. Three stacked SSR crashes: Search.jsx read window.location.search during render; ClusteredPropertyMap/ViewportAwarePropertyMap did `import { debounce } from 'lodash'` which Node's ESM loader rejects for the externalized CommonJS lodash index (switched to lodash/debounce); and frontendGeocoding.js news up a module-scope singleton whose constructor touched window at import time (SSR guard added). app.blade.php now dispatches the SSR gateway itself using the same memo variables the @inertia/@inertiaHead directives reuse, skips its fallback <title> whenever the SSR head provides one, and promotes the SSR title text into og:title/twitter:title so social previews match. VERIFIED locally with a running inertia:start-ssr: /search and /toronto/for-sale render server-side with a single "Property Search - Nobu Residences" title, /blogs shows single "Real Estate Blog", SSR log clean; client+SSR build passes. Server needs: git pull, npm run build, restart the Ploi NodeJS (SSR) server.

- **[FIX]** Gemini generations no longer die at MAX_TOKENS on thinking models - homepage AI content failed with "Gemini response was cut off (MAX_TOKENS at 3000 tokens)" right after the thinkingConfig fix (client error log)
  - Files changed: `app/Services/GeminiAIService.php`
  - Details: Follow-up to the thinkingConfig-drop fix: the model behind gemini-flash-latest cannot have thinking disabled at all (probe: thinkingLevel=low still burned 188 thought tokens on a 200-token budget, identical to no config; thinkingBudget=0 is a 400), and its reasoning tokens spend from the SAME maxOutputTokens budget, so a budget sized for the visible answer truncates mid-reply. The retry loop now detects a successful response whose finishReason is MAX_TOKENS and doubles maxOutputTokens (capped at MAX_OUTPUT_TOKENS_CAP = 16384) before retrying, and the final MAX_TOKENS error message reports the actual last-used budget instead of the caller's original. VERIFIED via tinker both live paths in laravel.log: generateWebsiteHomeContent() with a real building context returns full copy (welcome_text/main_heading/subheading/overview/faqs) after the thinkingConfig drop, and a reflection call with a deliberately tiny 400-token budget logs "Gemini hit MAX_TOKENS - retrying with budget 800" then returns the complete 727-char answer; php -l clean.

- **[FIX]** Gemini AI content generation works again after the key/project rotation - HTTP 400 "Request contains an invalid argument" on every generation (client: "Gemini website home content error ... HTTP 400")
  - Files changed: `app/Services/GeminiAIService.php`, `app/Services/GeminiDescriptionService.php`
  - Details: History: the old key was revoked by Google as leaked (it sits in plaintext in ai-integration-test.md/test-ai-integration.md in the public repo), the first replacement key was minted in the same flagged project (403 "project denied access"), and the working key from a fresh project then hit 400s. Payload bisection against the live API showed the minimal request returns 200 while the service payload 400s, and the culprit is generationConfig.thinkingConfig.thinkingBudget=0 - the gemini-flash-latest alias now resolves (for new projects) to a model generation that rejects thinkingBudget while 2.5 Flash accepted it; topK is fine. GeminiAIService's retry loop now catches a 400 while thinkingConfig is present, strips it, logs a warning and retries, so one config works on either side of a silent alias bump. Also switched GeminiDescriptionService off its hardcoded gemini-1.5-flash URL (model retired - 404s for new Google projects) onto the same services.gemini.model config. Server .env still needs the new GEMINI_API_KEY + config:clear on production. VERIFIED via tinker: generateBuildingDescription() with throwOnFailure returns real generated copy through the new retry path; php -l clean on both services. Follow-up worth doing: redact the leaked keys from the four md files (Gemini x2 + Google Maps in GOOGLE_MAPS_SETUP.md/MAP_STATUS.md).

- **[FIX]** Tenant alert emails show the site logo and agent photo again - broken image icons in every landing-page email (client screenshot: The Well saved-search alert with broken logo + broken Jatin Gill photo)
  - Files changed: `app/Support/EmailBranding.php`
  - Details: The listing-alert template embeds images inline (cid:) exactly because mail-client image proxies get blocked by Cloudflare on tenant domains, but the embed chain broke itself twice: (a) localPublicPath() refused any value starting with http, and tenant logos/agent photos are stored as absolute URLs to their own domain, so the local-file branch never ran even though every tenant domain serves this same public/ dir; (b) the URL-download fallback used Laravel Http with Guzzle's default User-Agent, which is on BlockScrapers' blocked list - the server 403ed its OWN download, embedImage() returned null, and the email fell back to a remote URL the recipient's mail proxy could not fetch either. localPublicPath() now maps absolute URLs by their path into public_path() (with a .. traversal guard) so scheduled console sends embed straight from disk with zero HTTP, and the download fallback sends a BunPilotMailer User-Agent that BlockScrapers does not match. VERIFIED via tinker through the array mailer with the prod failure shape (logoPath=null + absolute URL logo, local-path agent photo): 2 inline attachments, 2 cid: img refs, 0 remote img refs in the rendered HTML; reflection check confirms URL->local mapping and that ../ paths return null; php -l clean.

- **[FEATURE]** Per-tenant sitemap.xml + dynamic robots.txt with Sitemap line + city-correct default nav links (client: "make sure all new landing pages have proper URL structures and SEO" re universalcitycondos.com)
  - Files changed: `app/Http/Controllers/SitemapController.php` (new), `app/Http/Controllers/RobotsTxtController.php` (new), `routes/web.php`, `public/robots.txt` (deleted), `app/Http/Middleware/HandleInertiaRequests.php`, `resources/js/Website/Global/Navbar.jsx`
  - Details: Live SEO audit of universalcitycondos.com found (a) /sitemap.xml 404 - no sitemap existed anywhere in the app, (b) robots.txt was a static public/ file shared by all tenants so it could never advertise a per-domain sitemap, (c) default navbar Rent/Sale links hardcode /toronto/for-rent|for-sale - wrong city for a Pickering building site. New invokable SitemapController resolves the tenant, 404s on the admin host, and emits home, the linked building page (/{citySlug}/{buildingSlug} with lastmod), the building city's /for-sale + /for-rent, the static public pages and up to 500 published blog posts; cached 1h per website+host. RobotsTxtController replaces the deleted static file with identical rules plus a Sitemap: {host}/sitemap.xml line on resolved tenants (admin host gets no Sitemap line). HandleInertiaRequests now shares building_city/building_slug on globalWebsite (cached building select widened) and Navbar builds default Rent/Sale from the site's building city with Toronto fallback. Audit also confirmed: homepage head SEO is healthy (title/description/canonical/OG/twitter/ApartmentComplex JSON-LD all present per-tenant), 404 pages are noindex, and per-page <Head> titles exist in code but do not reach crawlers because the Inertia SSR node process (port 13714) is NOT running on production - #app renders empty server-side; flagged as ops item (start inertia SSR daemon on the server), plus prod data cleanup: duplicate building record universal-city-condos-1480-bayly-st serves a second copy of the building page and unit canonicals point at it. VERIFIED locally over HTTP: /sitemap.xml renders valid urlset with building/city/blog entries for the resolved tenant, /robots.txt shows Sitemap line only for tenants, php -l clean on all three PHP files, client+SSR build passes.

- **[FIX]** Building-scoped search outside Toronto returned 0 listings — For Sale/For Rent hero links on universalcitycondos.com showed nothing (client: "my sale and rent page show me 0 listings" with /search?building_id=...&status=For Sale / For Rent)
  - Files changed: `app/Http/Controllers/PropertySearchController.php`
  - Details: buildRepliersListingsParams() applies a city=Toronto default whenever no city is set, and the building_id scope block that runs AFTER it never overrode the city — for Universal City Condos (address "1480 Bayly St, Pickering") Repliers got streetNumber=1480 + streetName=Bayly + city=Toronto and returned zero. The homepage carousels worked all along because HomepagePropertiesController already filters by the building's own city. The building_id branch now sets city = building->city when present (same source as the carousels). Reproduced the exact 0 against production by replaying the frontend's real nested search_params JSON payload (earlier flat/PowerShell-quoted probes were mangled and misleadingly returned the unscoped 6798 Toronto dump). VERIFIED over real HTTP on nobu.test with the local copy of the same building (ff62cc57, address-only "1480 Bayly St, Pickering"): For Sale total=5, For Rent total=7 — exactly matching the site's homepage carousel counts; php -l clean.

- **[FIX]** Tenant-domain links no longer carry ?website={slug} — logo click on universalcitycondos.com stays at "/" (client: "when i click on logo it show me /?website=universal-city-condos, can we hide it")
  - Files changed: `resources/js/Website/Global/Navbar.jsx`
  - Details: Navbar's buildUrl() appended ?website={slug} to every link (logo, nav links, user dropdown) whenever the site wasn't website id 1 — but TenantResolver only honors that param on the admin/main host (preview override); on a custom domain the site is resolved by domain and the param is dead weight in every visible URL. buildUrl now reads the shared isMainDomain Inertia prop: on the admin host it still appends the slug (previews keep working, now including the default site), on tenant domains it returns the clean path and strips any website param already stored inside header_links URLs saved during preview. HomeHeader (ICE homepage) wraps this same Navbar so the reported logo link is covered. Investigated alongside the "For Sale/For Rent empty" report on the same site: no code change needed there — /api/homepage-properties on universalcitycondos.com live-returns 5 for-sale + 7 for-rent correctly tagged to Universal City Condos (verified with a browser-UA curl), carousels are live Repliers queries with no ingestion delay; the earlier emptiness was pre-building-link/cached state. VERIFIED: npm run build (client+SSR) passes.

- **[FIX]** Signed-in visitors get their name/email/phone pre-filled in the Request a Viewing form and the enquiry/contact modals (client: "I've signed in xo2 and then clicked on enquiry but my infos didn't fill up automatically")
  - Files changed: `resources/js/Website/Global/Components/ViewingRequestModal.jsx`, `resources/js/Website/Components/PropertyEnquiryModal.jsx`, `resources/js/Website/Components/ContactAgentModal.jsx`, `resources/js/Website/Components/EnhancedContactAgentModal.jsx`
  - Details: The screenshotted form is ViewingRequestModal (date strip + Morning/Afternoon/Evening + consent) — it initialized formData to empty strings and never read the shared Inertia auth prop, so a logged-in user always saw blank Full Name/Email/Phone. It now destructures `auth` from usePage().props and seeds formData from auth.user (name, email, phone — User has a fillable, non-hidden phone column and HandleInertiaRequests shares the full user), and the post-submit reset restores those seeded values instead of wiping to empty. PropertyEnquiryModal, ContactAgentModal and EnhancedContactAgentModal already pre-filled name/email but left phone empty — they now seed phone from auth.user.phone too (PhoneInput formats the raw stored value on render). Note: users created via Google OAuth have no phone on record, so that one field stays empty for them until they set it. VERIFIED: npm run build (client+SSR) passes.

## 2026-07-21

- **[FIX]** CSV import's 29 errors: globally-unique neighbourhood/sub-neighbourhood slugs collided when the same name exists under different parents — slugs now auto-suffix like Developer's (client: "again 29 error")
  - Files changed: `app/Models/SubNeighbourhood.php`, `app/Models/Neighbourhood.php`
  - Details: 26 of the 29 errors were "Duplicate entry 'north-york'/'scarborough'/'etobicoke' for sub_neighbourhoods_slug_unique": the importer firstOrCreate()s a sub-neighbourhood per (name, neighbourhood_id) — legitimate data — but the model's creating hook set slug = Str::slug(name) unconditionally while the slug column is globally unique, so the second "North York" under a different parent died on the index. Both SubNeighbourhood and Neighbourhood now use a uniqueSlugFor() helper (same pattern Developer already had): base slug, then -2/-3... suffixes, updating hook passes ignoreId. The remaining 3 errors are source-data problems: CSV rows 136/144/145 (9699 Jane St Maple, 9441 Jane St Maple, 11 Via Romano Blvd, all Vaughan) have "-" in Street_Address_1 — need addresses in the sheet or manual creation. After the fix landed mid-run, a server-side heal pass over a saved copy of the same CSV with the same mapping in update mode re-processed all 324 rows: 26 created (the slug casualties), 295 updated, 0 skipped, only the 3 address-less rows still erroring — 326 buildings total. VERIFIED: php -l clean, tinker slug matrix (same name under two parents + null parent -> base/-2/-3, rolled back), and the live heal-pass numbers above.

- **[FIX]** CSV import "200 Errors / 0 Created" (live + local): condos.ca export's Street_Address_1 header was never auto-mapped so Address stayed unmapped and every new-building row failed with "Address is empty" (client: "on live we have 200 error, same on local")
  - Files changed: `resources/js/Pages/Admin/Buildings/Import.jsx`
  - Details: Diagnosed from the new building_imports history + parked CSV: the stored mapping had no column on "address" — the sheet's header is Street_Address_1, which HEADER_ALIASES didn't know (only street_1/street_address/full_address/...), so the wizard auto-selected "Don't import" for the street column and importRow threw "Address is empty." for all 200+ rows on both environments. Added aliases: street_address_1 -> address, number_of_floors -> floors (col was silently dropped too), all_image_urls/all_images -> images. Upgraded the mapping-step notice from an easily-missed amber "Tip" to a red warning spelling out that every non-existing building will fail when no Address column is mapped. VERIFIED against the actual parked CSV via tinker with the corrected mapping: first 5 rows -> processed=5 created=5 errors=0, sample row composed "1480 Bayly St, Pickering" with floors=17, units=275, developer auto-created, 14 amenities synced (transaction rolled back); client+SSR build passes.

- **[FIX]** CSV building import stuck at "0 of 324 rows" on the live server — chunks are now time-boxed so every request saves progress before any server timeout (client: "10 minutes done but it stuck on this [importing 0%]")
  - Files changed: `app/Services/BuildingCsvImportService.php`, `app/Http/Controllers/Admin/BuildingImportController.php`, `resources/js/Pages/Admin/Buildings/Import.jsx`
  - Details: Each process() request imported a fixed 200-row chunk and only saved progress at the end. On the production server the chunk ran past the PHP/gateway timeout, the request died with progress still at 0, the browser retried into the still-held 120s cache lock whose "no progress" 200 responses reset the retry counter — an infinite loop showing 0 of 324 forever. importChunk() now accepts a microtime deadline and stops after CHUNK_TIME_BUDGET_SECONDS (8s) of work, always processing at least one row per request so the import is guaranteed to advance; the lock TTL dropped 120s -> 60s (CHUNK_LOCK_SECONDS) so a dead request blocks at most a minute; the wizard polls gently (1.5s instead of 250ms) while responses show no advance and surfaces a clear "reload to resume" error if nothing advances for 3 minutes instead of spinning forever. VERIFIED via tinker matrix (past deadline -> exactly 1 row processed + done=false, generous deadline -> finishes, null deadline -> unchanged behavior; transaction rolled back), php -l clean, client+SSR build passes.

- **[FIX]** Google sign-in on a tenant custom domain no longer strands the visitor on pcdadmin.com/dashboard (client: "When I sign in to xo2-condos.ca it took me to pcdadmin.com/dashboard")
  - Files changed: `routes/auth.php`, `app/Http/Controllers/Auth/GoogleAuthController.php`, `tests/Feature/Auth/GoogleAuthRedirectTest.php` (new), `tests/Feature/Auth/AuthenticationTest.php`
  - Details: The Google OAuth callback is registered for ONE host, so tenant domains relay sign-in through it (xo2-condos.ca/auth/google -> pcdadmin.com/auth/google?origin=xo2-condos.ca -> Google -> back to the tenant with a one-time token). But the auth/google route sat inside the `guest` middleware — when the visitor already had a session on the callback host (the site owner logged into the pcdadmin admin), RedirectIfAuthenticated intercepted the relay hop and bounced it to the CALLBACK host's own /dashboard; Google was never reached and the user landed stranded on pcdadmin.com/dashboard. The route now sits outside `guest` (like the callback) and redirectToGoogle() guards explicitly: a relay arrival (callback host + origin param) proceeds to Google regardless of auth state (that flow never touches the callback host's session — it mints the one-time token for the tenant), while an already-signed-in visitor starting a login is simply sent back to redirect_to or the site root on their own domain. Also fixed the stale AuthenticationTest expectation (store() redirects non-admins to user.dashboard, not dashboard). VERIFIED: new GoogleAuthRedirectTest covers the exact bug (signed-in on callback host + relay hop -> accounts.google.com, not dashboard) plus guest relay, signed-in tenant visitor stays on own domain, and tenant->callback-host hop with origin — 8/8 auth+relay tests pass; the 6 PasswordReset/Registration failures are pre-existing (those routes are deliberately 404 on the main domain via not.main-domain and the tests hit the main domain).

- **[FEATURE]** Import History section on the CSV building import wizard — every import permanently recorded (file, date, status, counts, row errors) with a Resume button for interrupted imports (client: "Import History wala section banao")
  - Files changed: `database/migrations/2026_07_21_190000_create_building_imports_table.php` (new), `app/Models/BuildingImport.php` (new), `app/Http/Controllers/Admin/BuildingImportController.php`, `resources/js/Components/Admin/BuildingImportHistory.jsx` (new), `resources/js/Pages/Admin/Buildings/Import.jsx`
  - Details: The only import state used to be the 12h progress cache, so once it expired there was zero trace of what ran. New central `building_imports` table (token unique, filename, status queued|processing|finished|failed, duplicate_action, total_rows/processed/created_count/updated_count/skipped_count/error_count, errors json, message, started_at/finished_at); run() upserts the row (frontend now sends the CSV's original filename), process() mirrors cache progress onto it once per 200-row chunk via syncHistory() including both failure paths, show() passes the latest 20 rows as a `history` prop with a computed `resumable` flag (running status AND progress cache alive AND parked CSV still on disk). New BuildingImportHistory component renders the table below the wizard: date, file, status badge (Completed/Failed/In progress/Interrupted — interrupted = running row whose cache expired), rows processed/total, per-count columns, failure message, and a Resume button that jumps straight to the Importing step where the existing processing effect continues from the exact server-side row offset; history partial-reloads (Inertia only:['history']) when an import reaches Results. VERIFIED: migration runs, php -l clean, model round-trip via tinker (create → cast checks → update → delete), npm run build (client+SSR) passes.

- **[FIX]** CSV building import survives page reload — wizard restores live progress / results instead of silently resetting to step 1 (client: "page reload pe show nahi ho raha — old import complete hua ya pending, kidhar dekhoon?")
  - Files changed: `resources/js/Pages/Admin/Buildings/Import.jsx`
  - Details: The import is browser-driven (process endpoint imports 200 rows per call) and the server caches progress under the token for 12h, but the token only lived in React state — any reload lost it, the wizard reset to Upload, an in-flight import stalled with no way to see done/failed/rows-left. The wizard now persists the token in localStorage (RESUME_STORAGE_KEY) when the import is queued; on mount it re-reads the token, fetches /import/progress/{token} and restores: finished → Results step with full counts, failed/expired → error message + token cleared, queued/processing → jumps to the Importing step where the existing processing effect resumes driving chunks from the exact row offset (server-side Cache::lock already guards a second tab). Failure paths in the driving effect fall back to 'upload' instead of 'map' when the session was restored (no headers/mapping to render) and clear the stored token; resetWizard clears it too. Importing-step copy now says a reload picks up where it left off. VERIFIED: npm run build (client+SSR) passes.

- **[FIX]** Building-scoped search count exact: For Sale click showed 27 (one stray listing) — Repliers cross-product filtered out via exact address pairs
  - Files changed: `app/Http/Controllers/PropertySearchController.php`
  - Details: Follow-up to the streetDirection fix. Repliers ORs the streetNumber and streetName arrays INDEPENDENTLY, so The Well's expanded range (455-480 Front + 455 Wellington) cross-matched "468 Wellington St" (a separate loft building: 468 in the Front number range, Wellington in the name set) — 27 instead of 26. buildRepliersListingsParams now stashes the exact number|name pairs under an internal _streetAddressPairs key (stripped before the API call); fetchPropertiesFromRepliersAPI fetches the full set (resultsPerPage 100) for address-scoped queries, post-filters listings to the real pairs, sets total to the filtered count and paginates locally (>100 fallback: per-page stray-drop only); getMapCoordinates strips the internal key. VERIFIED against the live Repliers API with the exact production input ("455-480 Front St W & 455 Wellington St W"): For Sale total=26 (all 26 hero MLS ids, 468 Wellington excluded), For Rent total=12 — both exactly matching the hero counts.

- **[FIX]** Building-scoped search no longer drops direction-less MLS listings — hero "For Sale 26 / For Rent 12" clicks now show all 26/12 instead of 17/7
  - Files changed: `app/Http/Controllers/PropertySearchController.php`
  - Details: Diffed the live wellcondostoronto.ca hero counts (26/12 from /api/homepage-properties street-name+number matching) against the building-scoped /api/property-search the hero buttons open (17/7). Per-address probes proved the cause: buildRepliersListingsParams passed streetDirection=W to Repliers, which ANDs it — and many listings (ALL of "455 Front St W"'s 6 units, plus scattered 470/480 Front and 455 Wellington units) are stored WITHOUT a direction in their MLS record, so they were silently filtered out ("455 Front St W" scope returned 0). The direction is still stripped from streetName (Repliers needs that) but is no longer sent as a filter; streetNumber+streetName+city is precise enough. Verified live: the same queries without direction return 3+6+10+7 = 26 for sale and the rent equivalents = 12, exactly matching the hero counts.

- **[FIX]** Home (ICE) carousel cards now link with the canonical building-slug property URL (client: URL was "/toronto/470-front-street-toronto/unit-2015-C13580920" instead of the xo2-style building-slug form)
  - Files changed: `resources/js/Website/Sections/Home/iceData.js`, `resources/js/Website/Home.jsx`
  - Details: normalizeListing hardcoded url = "/property/{MLS}" so every home-page card went through the redirect, which cannot know the building; it now takes the site's building and calls generatePropertyUrl(p, building) -> "/{city}/{building-slug}/unit-{unit}-{MLS}" exactly like xo2's cards (address-slug fallback when the site has no linked building). Verified via client+SSR build.

- **[FIX/FEATURE]** Client batch round 4: canonical property URLs everywhere, footer email/address clickable + dashboard Address Link field, Contact page phone heal, hero/navbar Rent-Sale go to the building's listings, search map follows results, nearby schools geocode compound addresses, amenities pipeline verified
  - Files changed: `resources/js/Website/Global/Footer.jsx`, `resources/js/Website/Global/contactDefaults.js` (new), `resources/js/Website/Sections/Contact/ContactInfo.jsx`, `resources/js/Pages/Admin/Websites/Edit.jsx`, `app/Models/AgentInfo.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `resources/js/Website/Global/Navbar.jsx`, `resources/js/Website/Sections/Home/HeroSection.jsx`, `resources/js/Website/Components/Property/PropertyCard.jsx`, `resources/js/Website/Components/Property/PropertiesForSale.jsx`, `resources/js/Website/Components/Property/PropertiesForRent.jsx`, `app/Http/Controllers/WebsiteController.php`, `resources/js/Website/Components/ClusteredPropertyMap.jsx`, `app/Http/Controllers/PropertySearchController.php`, `app/Http/Controllers/Api/SchoolController.php`, `app/Models/Building.php`
  - Details: (1) URL CONSISTENCY (client: "url structure work on wellcondos didn't apply to xo2"): the old PropertyCard still linked /property/{MLS} whose server redirect (WebsiteController::propertyDetailRedirect) emitted the short "/toronto/470-front-street/C13496692" form — the card now uses generatePropertyUrl() like every V-card, and the redirect itself now builds the canonical "{street#}-{street}-{suffix}-{city}/unit-{unit}-{MLS}" slug, so ALL remaining old links land on canonical URLs on every site. (2) FOOTER: email is a mailto link; address links to a NEW dashboard field (Admin > Websites > Edit > Contact Information > "Address Link (URL)", stored in contact_info.address_link) falling back to a Google Maps search; Contact page address honors the same field. (3) CONTACT PAGE PHONE 647-490-1532: wellcondos' AgentInfo row inherited Nobu's default agent phone at site creation — AgentInfo::agentPhone accessor now blanks that exact legacy value on non-default sites (same heal pattern as Website::getContactInfo) and ContactInfo falls through contact_info.phone to the fixed global 416 669 4755 (new shared contactDefaults.js used by Footer + Contact page). (4) BUILDING-SCOPED RENT/SALE (client: "take user to available for rent and for sale in that specific building"): globalWebsite now shares building_id; Navbar rewrites '/{city}/for-rent|for-sale' links to '/search?status=...&building_id=...&query=...' (same shape as the Unit Types badges fix) on building-linked sites; hero For Sale/For Rent CTAs navigate there too (scroll fallback when no building); PropertiesForSale/Rent "View all" no longer hits the invalid '/{street-slug}/for-sale' route — goes to street_addresses-scoped search. (5) MAP FOLLOWS RESULTS (client screenshots: Mississauga cards vs Toronto map; "Map does not working"): map fetches sent the stale viewport with every search so results got ANDed against the previous area — ClusteredPropertyMap now flags search_context_changed on filter-change refetches and getMapCoordinates re-queries WITHOUT the spatial filter whenever a fresh search carries area intent (query/city/street_addresses/building_id), returning fit_bounds so the map pans to the results; pans/zooms keep viewport behaviour. (6) NEARBY SCHOOLS: SchoolController::normalizeCompoundAddress splits "&"-joined multi-tower addresses and takes the first number of ranges ("455-480 Front St W & 455 Wellington St W" -> "455 Front St W"), and the simpler-retry no longer requires a comma — The Well's address now geocodes (verified: exact production strings normalize correctly). (7) AMENITIES: display pipeline verified end-to-end locally (pivot amenity -> getDisplayData -> tab shape correct); root cause for The Well is DATA — the legacy buildings.amenities JSON column was dropped (2025_09_16 migration) without migrating data to the amenity_building pivot, so pre-migration buildings show none until amenities are re-selected on the admin Building edit form (documented in Building::getDisplayData). VERIFIED: php -l clean on all changed PHP, address-normalization matrix via tinker, amenities pipeline via tinker (transaction rolled back), npm run build (client+SSR) passes, Cloudflare tests still green.

- **[FEATURE]** Zone-managed custom domains: app now creates the customer's domain as a DNS zone in our own Cloudflare account and adds the apex CNAME via API automatically — customer's only step is pointing nameservers (zero steps for domains bought inside the account) (client: xo2-condos.ca GoDaddy "@ CNAME" rejection — GoDaddy forbids apex CNAMEs; wellcondos only worked because its DNS already sits on Cloudflare nameservers)
  - Files changed: `app/Services/CloudflareService.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Jobs/SyncCustomHostnameStatusJob.php`, `app/Console/Commands/CloudflareSyncHostnames.php`, `app/Models/Website.php`, `config/services.php`, `.env.example`, `docs/CLOUDFLARE-DOMAINS.md`, `resources/js/Pages/Admin/Websites/Created.jsx`, `resources/js/Pages/Admin/Websites/Edit.jsx`, `database/migrations/2026_07_21_120000_add_cloudflare_zone_columns_to_websites.php`, `tests/Feature/CloudflareZoneProvisioningTest.php`
  - Details: New opt-in mode via CLOUDFLARE_ACCOUNT_ID (token additionally needs Zone:Edit + DNS:Edit on all account zones — permission table added to docs). CloudflareService::provisionZone(domain): idempotent end-to-end — finds-or-creates the zone in our account (POST /zones), then ensureCnameRecord() lists the apex records, keeps a correct CNAME, replaces conflicting auto-imported A/AAAA/CNAME records, and creates `@ CNAME -> cname_target` (DNS-only, TTL auto); www. inputs normalize to the bare apex app-wide via TenantResolver. Controller provisionZone() helper persists cloudflare_zone_id/zone_status/name_servers (new columns, cache-purge-irrelevant) on create, edit and retry; created() refreshes zone status live so the page flips to "nameservers detected" without waiting for the scheduler. SyncCustomHostnameStatusJob tracks zone status each poll; cloudflare:sync-hostnames re-runs provisionZone every 5 min so purged zones / lost records self-heal — zone failures never block the classic flow (custom hostname still registers, CNAME instructions remain the fallback when the mode is off). Created.jsx shows a "Point domain at these 2 nameservers" panel (copy buttons, zone-active state) instead of the CNAME table when zone-managed, plus zone rows in provisioning/live status; Edit.jsx Domain & Hosting shows the required nameservers. Zones are deliberately never deleted on domain change/remove (an admin typo must not nuke a customer's DNS). VERIFIED: 7 new feature tests with faked Cloudflare API (create, reuse, conflict replacement, www normalization, zone-create failure, record-failure healing path, disabled mode) all pass; php -l clean on all changed PHP; migration runs; npm run build (client+SSR) passes.

## 2026-07-20

- **[FIX]** "Nobu" purged from tenant sites' meta titles/descriptions/keywords (client: "Remove nobu from all over the meta title or descriptions, this one appears on the home page")
  - Files changed: `resources/js/app.jsx`, `resources/js/ssr.jsx`, `resources/js/Website/Home.jsx`, `app/Models/Website.php`
  - Details: Three leak paths. (1) BIGGEST — the Inertia title callback in app.jsx AND ssr.jsx appended "- {appName}" (= "Nobu Residences") to EVERY page title on every tenant domain (visible even in the client's FUB activity: "Property Search - The Well - Nobu Residences"); page titles already carry their own site name, so the callback now passes titles through untouched (app name only as empty-title fallback). (2) Home.jsx's title fallback "{site} - Luxury Condominiums at Nobu Residences" → "{site} - Luxury Condominiums". (3) DB seeds — landing sites created with the Nobu meta copied in: Website model accessors now self-heal on non-default sites (same pattern as the contact_info fix): meta_title/meta_description exactly matching the legacy Nobu seed read as unset (fall through to site-specific fallbacks), and meta_keywords additionally drops any comma-list item containing "nobu" while keeping the rest. Default Nobu site untouched; tenant custom values pass through. VERIFIED: accessor matrix (seeded → NULL, mixed keywords → nobu items dropped, custom + default untouched); npm run build (client+SSR) passes.

- **[FEATURE]** SEO meta title + description for listing pages with conditional building segment, all landing sites (client spec: "373 Front Street #208 | For Sale | MLS C13582594 | The Well", building part omitted when unmatched; description "%number_of_bedroom%-bed, %number_of_bathroom%-bath %listing_status% at %listing_address%, %cityname% %postal_code%. View photos, pricing, amenities and more at %building_name%.")
  - Files changed: `app/Http/Controllers/WebsiteController.php`, `resources/js/Website/Pages/PropertyDetail.jsx`
  - Details: Property detail pages had a client-side-only title ("{address} - Property Details - {site}") and NO meta description. New server-side builders in WebsiteController (buildPropertyMetaTitle / buildPropertyMetaDescription / propertyStreetLabel) compose the client's exact formats from the already-loaded listing + building data — street without unit + "#unit", For Sale/For Lease, "MLS {number}", building name conditionally; description leads "2-bed, 2-bath For Sale at 373 Front Street, Toronto M5V 2A6." with the "...more at {building}." tail dropping to "...more." when no building matches. Passed as metaTitle/metaDescription props; PropertyDetail.jsx <Head> renders them (loading state included) so the SSR HTML carries the real title + <meta name=description> for crawlers. Works identically on every current and future landing domain (same controller serves all tenants). VERIFIED via reflection with the client's exact example values — all four variants byte-for-byte, lease variant and null-safety included; php -l clean; npm run build (client+SSR) passes.

- **[FIX/FEATURE]** FUB round 3: tour appointments actually book (slot parsing), Ask A Question pushes leads, property price/beds/baths enriched live, For Sale / For Lease second tag (client screenshots: empty Appointments panel despite tour events, Ask A Question modal, Property Details box, tag request)
  - Files changed: `app/Services/FollowUpBossService.php`, `app/Http/Controllers/Api/TourRequestController.php`, `app/Http/Controllers/Api/PropertyQuestionController.php`, `app/Http/Controllers/PropertyEnquiryController.php`
  - Details: (1) APPOINTMENTS BUG — the tour widget submits display strings (selected_date "TUE, JUL 21" with weekday and NO year, selected_time "12PM to 4PM" range); Carbon::parse choked on the combined string so createFubAppointment silently skipped every booking (client's screenshot: events in the timeline, Appointments "No upcoming"). New parseTourSlot(): strips the weekday prefix, parses the day in America/Toronto (year-less dates that already passed roll to next year), splits the slot range so the appointment spans it (12PM-4PM), single-time fallback +1h. Verified with the exact production strings -> 2026-07-21T12:00-04:00 -> 16:00. (2) PROPERTY ENRICHMENT — new FollowUpBossService::propertyContext(mls, fallbackAddress): fetches the listing live from Repliers and builds the FUB property payload (street, city, mlsNumber, price, bedrooms, bathrooms, type, forRent) + a 'For Sale'/'For Lease' tag from the listing type; guarded, falls back to address+MLS. Service now constructor-injects RepliersApiService. (3) HOOKS — tour request, NEW Ask A Question (PropertyQuestionController — previously sent nothing to FUB), property enquiry, and Contact Agent modal all use propertyContext: full property details land on the FUB person (client's Property Details screenshot: address/price/beds/baths) and every property lead now carries TWO tags (site name + For Sale/For Lease — client's WhatsApp request). VERIFIED with faked Repliers+FUB APIs: context {price 1110000, 2 bed, 2 bath, Condo Apartment, forRent false, tag For Sale}; event payload tags ["For Sale","<site>"], property block complete; php -l clean on all 4 files.

- **[FEATURE]** Phone number required everywhere + post-Google-sign-in phone step that completes the FUB lead profile (client: "make phone required; after google sign-in push user to fill phone so it goes to FUB under their profile, match by email; my phone should pop up in the tour modal")
  - Files changed: `app/Http/Controllers/Auth/RegisteredUserController.php`, `resources/js/Pages/Auth/Register.jsx`, `resources/js/Website/Components/PropertyEnquiryModal.jsx`, `app/Http/Controllers/PropertyEnquiryController.php`, `app/Http/Controllers/Auth/CompletePhoneController.php` (new), `resources/js/Pages/Auth/CompletePhone.jsx` (new), `routes/auth.php`, `app/Services/FollowUpBossService.php`, `app/Http/Controllers/Auth/GoogleAuthController.php`
  - Details: (1) REQUIRED PHONE — RegisteredUserController now validates phone required (was read unvalidated); the standalone /register page had NO phone field at all — added (PhoneInput, required); LoginModal's register form already enforced 10+ digits client-side; the property enquiry modal's "(optional)" phone (client's screenshot) is now required with error display, backed by store() validation. (2) GOOGLE PHONE STEP — Google OAuth provides no phone, so all three Google login exit points (direct callback, relayed completeFromToken on the tenant domain, stateless callback) now redirect phone-less users to a new branded /complete-phone page (auth-only, PhoneInput, preserves the original redirect target, self-skips if a phone exists). Saving stores the phone AND pushes it into Follow Up Boss via new updatePersonPhoneByEmail(): GET /v1/people?email= to find the person (the client's requested email matching), PUT /v1/people/{id} with the phone — guarded/logged. (3) TOUR MODAL PREFILL — already implemented: TourScheduling prefills name/email/phone from the signed-in profile and hides fields it has; it only looked empty for Google users because they had no phone — solved by (2). VERIFIED: GET /complete-phone renders for a phone-less user (200) and 302s straight to the destination when a phone exists; FUB lookup+PUT payload asserted against a faked API ({"phones":[{"value":"(647) 702-4404"}]}); php -l clean; npm run build (client+SSR) passes.

- **[FIX]** Google sign-in of an EXISTING account now also pushes the lead to Follow Up Boss (client: "I've signed in via google it didn't come to follow up boss")
  - Files changed: `app/Http/Controllers/Auth/GoogleAuthController.php`
  - Details: The FUB Registration hook only fired inside the create-new-user branch of both Google callback methods; when the Google email matched an already-existing local user (the client's test — the account predated the FUB integration), the sign-in took the `if ($user)` branch: google_id linked, logged in, NO event sent — so the person never reached FUB. Both methods now hoist the tenant-site resolution above the branch and the existing-user path sends the same guarded report ("Signed in with Google on {site} (existing account)") — FUB events dedupe people by email, so users already in FUB just get a timeline entry while missing ones (the complaint) get created and assigned. php -l + app boot clean.

- **[FEATURE]** FUB integration round 2: source/tags = site name, saved-search criteria into FUB Custom Fields, tour requests become FUB Appointments (client screenshots of the working integration with follow-ups)
  - Files changed: `app/Services/FollowUpBossService.php`, `app/Http/Controllers/SavedSearchController.php`, `app/Http/Controllers/Api/TourRequestController.php`, `app/Http/Controllers/Auth/GoogleAuthController.php`
  - Details: (1) SOURCE + TAGS — lead source is now the landing SITE'S NAME ("The Well") instead of the domain: defaultSource() resolves the tenant website via TenantResolver (host fallback), the Google relay path passes websiteName, and sendEvent auto-tags every lead with its source site so FUB can filter/automate per site. (2) CUSTOM FIELDS — formatPerson passes through 'custom*' keys and tags; the saved-search hook now sends customLocation (query), customNumberOfBedrooms, customNumberOfBathrooms, customMinimumPrice, customMaximumPrice matching the account's existing Custom Fields (client's screenshot). (3) APPOINTMENTS — sendEvent now returns the FUB person response (id) instead of bool; new createAppointment() POSTs /v1/appointments (title, ISO start/end, invitees, description, location, guarded+logged). Tour requests book "Property showing — {address}" at the requested slot (parsed in America/Toronto, 1 hour, person as invitee, property address as location) so it appears on the lead's Appointments panel + account calendar; skipped cleanly when the lead push failed or the date is unparseable. (4) PIXEL — nothing to do: the FUB widget tracker + logged-in identify call already ship in app.blade.php (the client's own screenshot shows Website Activity on the lead). VERIFIED with faked FUB API: event returns person id, payload carries tags + custom fields, appointment payload has Toronto-offset ISO times, personId invitee, location; php -l clean on all 4 files.

- **[FIX]** Email From name = the landing site's name + dark-mode-safe logo chip (client: "From name should be Building Name; logo on a background where white and dark logos both visible")
  - Files changed: all 7 notifications, `resources/views/emails/branded.blade.php`, `resources/views/emails/listing-alert.blade.php`
  - Details: (1) FROM NAME — every notification now sets ->from(config('mail.from.address'), $siteName): the address stays the Resend-verified noreply@pcdadmin.com (changing it would break domain verification) but the display name is the site the email belongs to — "The Well <noreply@pcdadmin.com>" for The Well registrations/alerts, verified via pipeline send (From: The Well <noreply@pcdadmin.com>). (2) LOGO CHIP — Gmail dark mode was recoloring the navy #293056 chip to lavender, hiding the white-on-transparent logo. Chip is now mid-tone slate #64748b (legible for BOTH white and dark logos), doubled as a background-image linear-gradient (Gmail dark mode does not recolor backgrounds that carry a background-image), plus color-scheme/supported-color-schemes meta asking clients to keep the light palette; img alt text styled white so even a failed image shows readable text on the chip. (3) Client also noted "emails are being sent to me" — expected: new-registration/contact admin notifications go to every users.role=admin account, and his address is one; adjustable to a fixed list if wanted.

- **[FIX]** Email logo embed hardened: server now downloads-and-embeds the logo when the file isn't on the local filesystem (client: "still logo is broken")
  - Files changed: `app/Support/EmailBranding.php`, `resources/views/emails/branded.blade.php`, `resources/views/emails/listing-alert.blade.php`
  - Details: Follow-up to the cid-embed fix. New `EmailBranding::embedImage($message, $path, $url)` centralizes the strategy: (1) embed the local public file when it exists (admin logo uploads land in public/assets — verified in WebsiteManagementController), (2) otherwise the SERVER fetches the URL (5s timeout) and embeds the raw bytes via embedData — covers absolute/external logo URLs and any filesystem mismatch, and the recipient's mail client never fetches anything remote, (3) plain URL only as last resort, with a warning logged when embedding fails. Both templates + the listing-alert agent photo use the helper. VERIFIED: pointed the local site's logo_url at the LIVE production Well PNG (absolute URL, no local file) and sent through the real pipeline — the server downloaded and embedded it (image/png inline attachment + cid ref, zero embed failures). NOTE for the user: the fix only applies to emails sent AFTER deploying — emails already sitting in an inbox keep their old broken HTML forever; deploy (git pull + php artisan view:clear) and send a fresh registration/verification email to see it.

- **[FEATURE]** Follow Up Boss CRM integration — leads pushed server-side via the FUB Events API (client: "registrations are not coming into my follow up boss account, pixel doesn't work; ab mere paas FUB API key hai, implement karo")
  - Files changed: `app/Services/FollowUpBossService.php` (new), `config/services.php`, `app/Http/Controllers/Auth/RegisteredUserController.php`, `app/Http/Controllers/Auth/GoogleAuthController.php` (both new-user paths), `app/Http/Controllers/PropertyEnquiryController.php` (store + agentEnquiry), `app/Http/Controllers/ContactController.php`, `app/Http/Controllers/Api/TourRequestController.php`, `app/Http/Controllers/SavedSearchController.php`
  - Details: The FUB pixel only tracks activity for contacts that already exist — it cannot create leads from custom forms, which is why registrations never appeared. New FollowUpBossService posts to POST https://api.followupboss.com/v1/events (Basic auth, key as username) — FUB's recommended path since /events dedupes people by email/phone, stamps the lead source, and fires the account's Lead Flow rules/action plans. Source = the landing domain the request came in on (per-site lead routing/reporting in FUB). Hooks (all guarded via static report(), same never-break contract as WelcomeNewUser::send): email registration + BOTH Google sign-up paths → "Registration"; property enquiry → "Property Inquiry" with property street/MLS/price/type; Contact Agent modal → "General Inquiry" with building tag; contact form → "General Inquiry" with category; tour request → "Property Inquiry" with date/time + property; saved search → "Saved Property Search" with criteria. Enabled by FUB_API_KEY in the server .env (config services.followupboss.key; optional FUB_SYSTEM name) — with no key every call is a silent no-op. VERIFIED: disabled path no-ops; with a fake key + Http::fake the payload asserts green (correct URL, Basic auth, firstName/lastName split, emails/phones value objects, source, message, pageUrl). php -l clean on all 8 files. TO GO LIVE: add FUB_API_KEY=<key> to the production .env (+ php artisan config:cache if caching), then register a test account and confirm the lead appears in FUB with source = the landing domain.

- **[FIX]** Email logo no longer shows as a broken image — the header's logo file is now embedded inline (cid:) in every branded email (client screenshot: verification email's navy chip showed a broken image icon while the landing header logo works)
  - Files changed: `app/Support/EmailBranding.php`, `resources/views/emails/branded.blade.php`, `resources/views/emails/listing-alert.blade.php`, all 7 notifications (logoPath passthrough)
  - Details: Header and email already use the SAME websites.logo_url field, and the live file (wellcondostoronto.ca/assets/logo_6a58a08aeb7ba_1784193162.png) is a valid public PNG over HTTPS — so the file wasn't the problem; mail clients fetch remote images through proxies (Gmail image proxy) that Cloudflare on the tenant domains can block, breaking the image in the inbox while the same URL works in a browser. Fix: EmailBranding now also resolves logoPath (absolute public_path for site-relative logo_url values, external URLs/missing files → null) and both email templates embed that file inline via `$message->embed()` at real send time — the logo becomes a cid: attachment inside the email itself, no remote fetch — falling back to the URL when rendering outside a send (previews/tests) or when the file is remote. Agent photo in listing-alert emails gets the same treatment (photoPath). VERIFIED: real send through the mail pipeline (log mailer) shows Content-Type: image/png + Content-Disposition: inline and the img tag referencing `cid:...@symfony`. php -l clean; no frontend changes.

- **[FIX]** Completed password reset on a landing site now returns to the site's home page instead of the bare /login screen (client: "when password reset of landing page is done go to https://wellcondostoronto.ca not login route")
  - Files changed: `app/Http/Controllers/Auth/NewPasswordController.php`
  - Details: The stock Breeze redirect sent users to route('login') after a successful reset. Landing sites sign users in via the header's login modal on the page itself — the standalone /login card is off-brand there. Since reset-password routes are already not.main-domain (404 on the admin host), the success redirect now goes to '/' (the tenant domain the reset happened on), status flash preserved. Verified the reset core (token → new password saved and checkable) still passes in a rolled-back harness; kernel-simulated tenant-host requests confirmed GET /login still renders for the main-domain flow.

- **[FIX]** Repliers saved-search mirror 404 fixed (wrong endpoint + wrong payload shape) + Resend sends now log their message id ("Failed to create Repliers saved search - HTTP 404" + "verification email never arrives")
  - Files changed: `app/Services/RepliersApiService.php`, `app/Http/Controllers/SavedSearchController.php`, `app/Mail/Transport/ResendTransport.php`
  - Details: (1) REPLIERS — createSavedSearch POSTed to /saved-searches (does not exist → 404 on every mirror attempt) with our raw internal filter object nested under "filters". Per docs.repliers.io the resource is POST /searches with FLAT criteria. New mapSavedSearchCriteria() reuses buildRepliersListingsParams (GTA city matching, street parsing, property-type expansion) then translates to saved-search keys: type sale/lease, class [condo], minPrice/maxPrice (required — live API validates "must be greater than 0", so unset minimum sends $1), cities, streetNames/streetNumbers, propertyTypes, minBeds/maxBeds, minBaths, minSqft/maxSqft. delete/get switched to /searches/{id} and /searches?clientId; controller now reads searchId from the create response. Live-verified the endpoint: the exact failing production payload (clientId 184421, "Etobicoke North Go Station") no longer 404s — the only rejection left was the minPrice=0 validation, which the clamp fixes. Full create+delete round-trip on the production Repliers account still worth a one-shot check after deploy (save any search while logged in, then confirm saved_searches.repliers_sync_status = 'synced'). (2) RESEND VISIBILITY — the verify-email resend flow surfaced success, which (transport throws on ANY non-2xx) proves Resend ACCEPTED the message; non-delivery is downstream (suppression/bounce/spam). ResendTransport now logs resend_id + to + subject for every accepted send so missing emails can be looked up in the Resend dashboard by id; `php artisan mail:test <addr>` polls the same events. php -l clean.

- **[FEATURE]** Dynamic personalized email templates with %merge_tags% for the listing alert emails + admin editor (client: "email subject and body template let's make dynamic couple of sections, example %First_lastname% - %building_name% so it'll be personalized")
  - Files changed: `database/migrations/2026_07_20_000002_create_email_templates_table.php` (new), `app/Models/EmailTemplate.php` (new), `app/Support/EmailMergeTags.php` (new), `app/Http/Controllers/Admin/EmailTemplateController.php` (new), `resources/js/Pages/Admin/EmailTemplates/Index.jsx` (new), `app/Notifications/SavedSearchAlertNotification.php`, `app/Notifications/FavouriteListingUpdateNotification.php`, `app/Models/SavedSearch.php`, `resources/js/Layouts/AdminLayout.jsx`, `routes/web.php`
  - Details: The listing-alert emails' subject, headline and intro are now DB-backed templates with per-recipient merge tags instead of hardcoded copy. `email_templates` table: type ('saved_search_alert' / 'favourite_update') + nullable website_id (NULL = global, site row overrides, unique per pair); `EmailTemplate::resolve()` falls through PER FIELD site -> global -> built-in defaults (the defaults ARE the previous copy, so nothing changes until an admin edits) and never throws pre-migration. `EmailMergeTags` replaces %tags% case-insensitively (%First_Name% works), strips unknown/typo'd tags so "%first_nam%" never reaches an inbox, and in the HTML intro escapes both template text and values (XSS-checked) while allowing only the self-built %site_link% anchor raw. Tags: first/last/full name, site_name/domain/link, agent name/phone/email, and per type search_name, search_criteria, listing_count, frequency, building_name (SavedSearch gained a building() relation for building-alert subscriptions). Both notifications now resolve their template by the search/favourite's website_id and run subject/headline/intro through the tags. NEW admin page /admin/email-templates (nav item added): per email type an editor with scope dropdown (All websites / per site), subject/headline/intro fields whose placeholders show the effective defaults, empty-field-falls-back semantics explained inline, and a clickable legend of available tags; saves via PUT admin.email-templates.update (updateOrCreate on type+website_id). VERIFIED via rolled-back harness: defaults reproduce the previous copy byte-for-byte; a global template "%first_name% %last_name% - %building_name% - %listing_count% new listings" renders "Batuhan Serin - The Well - 3 new listings" (the client's exact example); site override beats global while its empty fields fall back to the global headline; favourite subject personalizes; unknown tag stripped; <script> injection escaped. php -l clean, migration run locally, `npm run build` (client+SSR) passes.

- **[STYLE]** Listing alert emails redesigned to the client's example template (agent header + property cards) + one-click signed unsubscribe (client sent a reference email: "this template for email real estate — this is example")
  - Files changed: `resources/views/emails/listing-alert.blade.php` (new), `app/Support/ListingEmailCard.php` (new), `app/Http/Controllers/SavedSearchUnsubscribeController.php` (new), `resources/views/alerts/unsubscribed.blade.php` (new), `app/Support/EmailBranding.php`, `app/Notifications/SavedSearchAlertNotification.php`, `app/Notifications/FavouriteListingUpdateNotification.php`, `app/Services/SavedSearchAlertService.php`, `app/Services/FavouriteUpdateAlertService.php`, `routes/web.php`
  - Details: Client's reference (an incomerealestate.com alert) has: agent header (photo, name, title, phones, Email Me, brokerage), script headline ("Handpicked listings just for you."), thank-you box naming the site, "New or Updated Properties" with 2-up property cards (FOR SALE banner + address link, photo, Schedule an Appointment / Email the Listing / View Details, city, beds/baths, price), and a manage-alerts + unsubscribe footer. Recreated that structure in our navy branding as `emails/listing-alert.blade.php` (600px, email-safe tables) and switched BOTH the saved-search alert and favourite-update emails onto it (auth/admin emails stay on the minimal `branded` template). EmailBranding gained `agentForWebsite()` — site's dashboard AgentInfo → default site's AgentInfo → the frontend's fixed landing-page agent constants (Jatin Gill / Broker / +1 416 669 4755 / info@jatingill.com / Re/Max Your Community Realty), phone/email preferring the site's own contact_info, photo absolutized. Cards get FOR SALE / FOR RENT banners from the Repliers listing `type` (both services now pass transaction_type; favourite fetch adds `type` to fields), "Email the Listing" is a mailto to the branded agent with the address in the subject, favourite-change notes render inside the card. NEW: one-click unsubscribe — signed route `alerts.saved-search.unsubscribe` (signed middleware, no login needed) flips that search's email_alerts off and shows a small branded confirmation page; the saved-search email links it in the footer (favourite emails point to the dashboard instead — removing a favourite is the opt-out). VERIFIED: rendered-pipeline assertions for both emails (agent block, headline, thank-you box, both banner types, all three card links, beds/baths, prices, manage + unsubscribe footer, site logo, no stock markers); signed unsubscribe returns 200 + flips the flag + confirmation page while a tampered signature 403s and changes nothing; a live rolled-back run against Repliers sent a real alert with 6 cards through the new template. php -l clean on all 8 PHP files.

- **[FEATURE]** All emails on one site-branded template (with logo) + favourite-listing update alerts + saved-search alert pipeline verified end to end (client: "no Laravel default template, match landing UI + logo; test registration/forgot-password/listing/fav alerts; set up saved-search notifications")
  - Files changed: `resources/views/emails/branded.blade.php` (new), `app/Support/EmailBranding.php` (new), `app/Notifications/Auth/VerifyEmailNotification.php`, `app/Notifications/Auth/ResetPasswordNotification.php`, `app/Notifications/WelcomeNewUser.php`, `app/Notifications/NewUserRegistered.php`, `app/Notifications/ContactFormReceived.php`, `app/Notifications/SavedSearchAlertNotification.php`, `app/Notifications/FavouriteListingUpdateNotification.php` (new), `app/Services/FavouriteUpdateAlertService.php` (new), `app/Console/Commands/SendFavouriteUpdateAlerts.php` (new), `app/Http/Controllers/SavedSearchController.php`, `app/Http/Controllers/Api/PropertyFavouriteController.php`, `app/Models/SavedSearch.php`, `app/Models/UserPropertyFavourite.php`, `database/migrations/2026_07_20_000001_add_website_context_and_favourite_alert_state.php` (new), `routes/console.php`; deleted dead `resources/views/emails/saved-search-alert.blade.php`
  - Details: (1) TEMPLATES — 5 of 6 notifications (verification, welcome, admin new-user, admin contact-form, saved-search alert) still rendered through Laravel's stock MailMessage markdown layout; only password-reset was custom. Built `emails/branded.blade.php` (shared minimal template modeled on the approved password-reset design: site logo on a navy chip when the site has one, else site-name text; white card; paragraphs; key-value rows; listing cards with image/price/meta/change-note; navy pill button; footnote; © footer) plus `EmailBranding` (resolves tenant site name/logo/home URL via TenantResolver on requests, explicit website or default site for scheduled sends; absolutizes relative logo paths). Converted ALL 6 notifications + kept reset on the same system. (2) SAVED-SEARCH PIPELINE — already existed (SaveSearchModal on /search + building alerts → /api/save-search → scheduled alerts:send-saved-search 8AM/6PM via SavedSearchAlertService matching live Repliers, sale AND rent both supported via status param). Added: `saved_searches.website_id` captured at save time so alert emails brand with and link to the landing site the search was saved on (search URL + property links now use that domain, not app.url); sqft lines in formatted_criteria. (3) FAVOURITE UPDATES — nothing existed. New `alerts:send-favourite-updates` (scheduled daily 08:30): FavouriteUpdateAlertService batch-fetches favourited listings from Repliers (status A+U), detects price changes vs last_notified_price/property_price and status changes (Sold/Leased/Terminated/Expired/Suspended, 'Pc' folded into the price note) vs last_notified_status (first sight seeds the baseline silently), sends ONE bundled branded email per user, snapshots so each change emails exactly once; user_property_favourites gained website_id + last_notified_price/status. VERIFIED: all 7 emails rendered through the real pipeline — subjects correct, logo + navy pill present, zero stock markers (Regards/All rights reserved/trouble-clicking); full live-flow harness (real Repliers listing, log mailer, rolled-back transaction) sent 1 saved-search alert + 1 favourite price-reduced alert; both alert commands run clean and appear in schedule:list; php -l on all 16 files. Local .env has no RESEND_API_KEY so real delivery must be smoke-tested in prod via `php artisan mail:test`.

- **[FIX]** Building "Listing History" page no longer overflows / breaks the layout on mobile ("Unit price history page doesn't fit its own section and page design gets broken" — client, wellcondostoronto.ca)
  - Files changed: `resources/js/Pages/BuildingPriceHistory.jsx`
  - Details: Each history row was a non-wrapping flex row whose columns demand ~430px minimum (24px card padding + 88px thumbnail + `min-w-[160px]` unit info + `min-w-[110px]` date + three 16px gaps) while a 375px phone offers ~343px of content width. The columns can't shrink, so row content spilled out of the gray cards (statuses hanging past the card edge) and widened the document scroll width, letting the whole page shift/clip horizontally — both states visible in the client's screenshots. Fix: rows now `flex-wrap` on mobile with a 72x60 thumbnail, unit info as `flex-1 min-w-0`, a shrink-proof right-aligned date column, and the status + "Sold for $X on date" column dropping to its own full-width line inside the card; all original sizes/layout restored at `md:` so desktop is pixel-identical. The shared PropertyDetail/PriceHistory component's rows were checked too — their minimum (~238px) already fits small phones, so left untouched. `npm run build` (client+SSR) passes.

## 2026-07-17

- **[FIX]** Contact Agent modal no longer yanks users to the home page with a broken-looking layout after submit + WhatsApp button now shows on the search page (client feedback)
  - Files changed: `resources/js/Website/Components/ContactAgentModal.jsx`, `app/Http/Controllers/PropertyEnquiryController.php`, `resources/js/Website/Pages/Search.jsx`
  - Details: (1) CONTACT FORM — the footer's "Contact us" opens ContactAgentModal, which submitted via Inertia `router.post('/agent-enquiry')`; the endpoint returns `back()`, and when the browser sends no usable referer Laravel falls back to `/`, so submitting navigated the user to the HOME page; with `preserveScroll: true` the fresh page was restored to the old pixel offset while footer images were still lazy-loading, producing the clipped/overlapping "design gets broken" screenshot. Fix: the modal now submits via plain axios (no page visit at all — success message shows in-modal, auto-closes after 2s, 422 validation errors mapped per-field), and `agentEnquiry` answers JSON to `expectsJson()` callers while keeping the `back()` redirect for non-XHR submitters. Applies everywhere the shared modal is used (footer on all pages, school pages, compare, building gallery). (2) WHATSAPP — the floating WhatsAppButton was only rendered by Home and PropertyDetail, so it vanished on /search ("search all page"); Search.jsx now renders it inside MainLayout with a listings-availability message using siteName, standard bottom-6 offset (no fixed mobile bottom bar on search). `php -l` + `npm run build` (client+SSR) pass.

- **[FIX]** Schedule a Viewing section: contact details no longer show Nobu's data on tenant landing sites + misleading "AI Concierge Available" card removed (client feedback, wellcondostoronto.ca)
  - Files changed: `app/Models/Website.php`, `resources/js/Website/Sections/Home/ContactSection.jsx`
  - Details: (1) CONTACTS — ContactSection already read website.contact_info ("the dashboard"), but every landing site's DB record was seeded with the Nobu site's values copied in (verified live: The Well's own contact_info = 647-490-1532 / info@noburesidences.com / 55 Mercer Street), so the section faithfully rendered another site's contacts. Fix mirrors the footer's approach at the data layer: `Website::getContactInfo()` now treats those exact legacy seed values as unset on non-default websites (per-field, case-insensitive) — dashboard-entered values pass through untouched, the default Nobu site is unaffected, and every consumer of contact_info/globalWebsite self-heals. ContactSection's fallback chain extended per field: agent_info/contact_info -> building agent/address -> the footer's fixed landing-page contacts (+1 416 669 4755 / info@jatingill.com) and globalWebsite.building_address for the address, so tenant pages always show real reachable contacts. Verified via harness: tenant+seed -> all blank, default site -> unchanged, tenant+custom -> passes through. (2) AI CONCIERGE — the dark card promised an "AI-powered chat assistant (bottom right)" but no chat widget exists anywhere in the codebase (grep hits are amenity labels only); card removed. If the client wants an actual AI concierge chat, that is a new feature to scope separately. `php -l` + `npm run build` (client+SSR) pass.

- **[CHORE]** `php artisan mail:test {to}` — mail pipeline diagnostic for "reset says Email Sent but nothing arrives" (Resend)
  - Files changed: `app/Console/Commands/TestMail.php` (new)
  - Details: The reset modal's success state only proves the mailer accepted the message without throwing (ResendTransport throws on any non-2xx, and the reset path has no guard), so a silent non-delivery means either mail.default fell back to `log` (RESEND_API_KEY empty — AppServiceProvider only forces resend when a key is present) or Resend accepted the email but didn't deliver (unverified from-domain, onboarding@resend.dev restrictions, suppression, spam). The command prints mail.default / from address / masked key presence, warns on `log` mailer and placeholder example.com from-address, then sends a raw test email surfacing the transport's exact error. Verified locally: correctly reports NOT SET key + placeholder from and fails loudly with the transport's message.

- **[FIX]** Unit Types & Price Ranges: clicking an AVAILABLE count badge now shows the building's matching listings ("when I click on these nothing appears" — client, wellcondostoronto.ca)
  - Files changed: `app/Http/Controllers/PropertySearchController.php`, `resources/js/Website/Sections/Home/UnitTypesTable.jsx`
  - Details: The badges already linked to /search scoped via street_addresses (SA1,SA2) + building_id, but the results were always empty for The Well. Two causes, same compound-address family as the buildings-counts fix: (1) PropertySearchController's building-scope parser fed each street_addresses segment / DB address field straight into its number+name regex, so The Well's "&"-joined values ("455 Front St W & 455 Wellington St W") produced garbage streetName "Front St W & 455 Wellington" and Repliers returned zero — verified against production: 1BR For Sale probes returned total:0 via BOTH the street_addresses AND building_id paths; (2) even parsed correctly, SA1/SA2 alone cover only 3 of the building's 26 street numbers, so results could never match the badge counts (computed from ALL addresses). Fix: the controller now expands every raw address through `Building::splitCompoundAddress()` (the same expander the count paths use) before parsing — fixes both scope paths and MerchandiseLofts' Sale/Rent CTAs on property pages too; UnitTypesTable prefers `building_id` alone (server fans out the full DB address set) and only falls back to street_addresses when there is no id. Search.jsx already forwards building_id from the URL (verified). Verified via harness with The Well's exact production data (temp Building row, transaction rolled back): street_addresses path parses to clean [455 Front, 455 Wellington, 456 Front] + direction W; building_id path fans out 27 number/name pairs with names only [Front, Wellington]; live Repliers call with those params returns 5 one-bedroom For Sale listings (was 0). Note: badge counts and search results can differ slightly — badges come from the ICE/local grouping, search from live MLS at click time. Studio rows pass bedrooms=0 which the search treats as "any beds" (shows the whole building — a known lesser quirk, not "nothing"). `php -l` + `npm run build` (client+SSR) pass.

- **[FIX]** Password reset on landing pages: site-branded minimal reset email (replaces stock Laravel notification) + guest auth pages carry the tenant logo instead of the Laravel framework logo
  - Files changed: `app/Notifications/Auth/ResetPasswordNotification.php` (new), `resources/views/emails/password-reset.blade.php` (new), `app/Models/User.php`, `resources/js/Layouts/GuestLayout.jsx`
  - Details: Client: "password reset doesn't work proper on landing page" + wants a minimal clean email, not the Laravel default. Diagnosed the live flow end-to-end on wellcondostoronto.ca: /forgot-password and /reset-password/{token} render (200, correct Inertia components), CSRF (renamed `ct` cookie) verified working via a live POST that correctly 422'd for an unknown email, Ziggy's public group exposes all password.* routes, and the emailed link keeps the tenant host (sync send, url() from the request — same mechanism as the stock notification). No functional break found server-side; what the client actually hit is presentation: (1) the reset email was the STOCK Laravel notification — verbose boilerplate ("You are receiving this email because…", "Regards", raw trouble-clicking URL block) branded with the app/default-site name rather than the landing site; (2) the forgot/reset pages showed the Laravel framework logo (ApplicationLogo SVG) on a generic gray Breeze card. Fix: `User::sendPasswordResetNotification()` now sends `ResetPasswordNotification` (extends the framework class, token semantics stock) which resolves the CURRENT tenant site name via TenantResolver (fallback default website → app.name, same chain as VerifyEmailNotification) and renders a dedicated minimal template `emails/password-reset.blade.php` — site name header, one line, navy pill button, expiry + ignore note, © footer; no Laravel layout/boilerplate. GuestLayout now renders globalWebsite.logo_url on a navy chip (site logos are white-on-transparent for the navy header) with site-name text fallback — Laravel SVG gone from auth pages. Verified: notification rendered through the real pipeline (subject "Reset your password — {site}", link `/reset-password/{token}?email=…`, "NO LARAVEL BOILERPLATE" assertion), php -l on both PHP files, `npm run build` (client+SSR) passes. NOTE for prod: the live 419 probe returned a full stack trace JSON — APP_DEBUG appears to be ON in production; should be disabled in the server .env.

- **[FIX]** Building card counts still 0/0 on tenant landing domains (The Well) — /api/buildings-counts server path now parses compound "&"-joined addresses
  - Files changed: `app/Http/Controllers/Admin/BuildingController.php`
  - Details: Client screenshot (wellcondostoronto.ca property page) showed the MerchandiseLofts card at "0 Condos for Rent / 0 Condos for Sale" for The Well after the earlier frontend fix routed counts through POST /api/buildings-counts. Reproduced against production: the endpoint itself returns {sale:0, rent:0} for The Well on every domain (not tenant-specific) while the legacy single-address GET returns 7/4 for one address. Root cause: `computeCountsConcurrently()`'s helper `buildingStreetGroups()` claimed to mirror `Building::parsedStreetAddresses()` but skipped `splitCompoundAddress()`, feeding raw fields straight to `parseStreetAddress()`. The Well's stored fields are ALL compound ("455 Front St W & 455 Wellington St W", plus 24 additional_addresses in the same glued shape from the legacy range-expansion bug), so the parser swallowed the "&" tail into the street name ("Front St W & 455 Wellington"), every Repliers query matched nothing, and the 0/0 result was cached 600s under the shared `building_listing_counts:{id}` key — poisoning the serial `getLiveListingCounts()` path (homepage, building pages) too. Fix: `buildingStreetGroups()` now groups over `$building->parsedStreetAddresses()` (the compound-aware single source of truth) instead of re-parsing raw fields. Verified with The Well's exact production field values via a reflection harness: old parse yielded `{455, "Front St W & 455 Wellington"}`; fixed method yields groups Front=[455..480], Wellington=[455] — identical to the model path previously tinker-verified at 26 sale/15 rent. `php -l` passes; frontend untouched (no rebuild needed). Stale zero caches expire on their own within 10 minutes of deploy.

## 2026-07-16

- **[STYLE]** Market Data section replaced with the client's Market Sentiment card only (no trends chart)
  - Files changed: `resources/js/Website/Components/PropertyDetail/MarketData.jsx`
  - Details: The client's reference UI is a single "Market Sentiment" card — gradient Buyer's/Balanced/Seller's gauge with pointer, bold stats (months of inventory, median DOM, sale-to-list %), and a "Based on X active listings · Y sales in {area} (last 90 days)" caption. Our version had added the gauge but KEPT the Price Trends/Sales Trends chart as a second card; per client, the chart is removed and the section is exactly the sentiment card (guest gate reworded to match). Backend /api/market-stats unchanged (still returns trends — simply unused, easy to restore if the client wants the chart back). `npm run build` (client+SSR) passes.

- **[FIX]** Filters modal price inputs (No min / No max) no longer flow outside the modal on phones
  - Files changed: `resources/js/Website/Components/FiltersModal.jsx`
  - Details: An `<input>`'s intrinsic default width (~170px from the default size attribute) acts as its automatic minimum inside a flex row, so `flex-1` never shrank the No min / No max fields below ~170px each — the row (2 inputs + "to" + gaps) exceeded the modal width on phones and the No max field rendered outside/flush with the modal edge. Fix: `min-w-0` on both inputs (allows them to split the available space), `gap-2 sm:gap-4` and `px-3 sm:px-4` for mobile, `flex-shrink-0` on the "to" label. `npm run build` (client+SSR) passes.

- **[FIX]** Filters modal footer no longer overflows on phones + Market Sentiment gauge added to Market Data (client reference)
  - Files changed: `resources/js/Website/Components/FiltersModal.jsx`, `resources/js/Website/Components/PropertyDetail/MarketData.jsx`, `app/Http/Controllers/WebsiteController.php`
  - Details: (1) FILTERS FOOTER — "Reset filters" + "Cancel" + "See N,NNN properties" (all whitespace-nowrap, See min-w-120px) exceeded narrow phone widths, so the row overflowed horizontally past the modal edge (client screenshot). Mobile now stacks: Reset/Cancel share a row (flex-1 each) with a full-width See button below; desktop keeps the single row (Reset+Cancel left, See right). Also `overflow-hidden` on the modal container so nothing can bleed past the rounded corners. (2) MARKET SENTIMENT — getMarketStats already computed a `sentiment` object (active vs sold-90d ratio, label, gauge position) but the frontend never rendered it. Backend now also returns `monthsInventory` (active / monthly absorption), `medianDom`, and `saleToList` % via Repliers statistics `med-daysOnMarket,med-soldPrice,med-listPrice` on the sold-90d query. MarketData renders the reference-style card above the trends chart: "Market Sentiment" + label, blue→gray→red Buyer's/Balanced/Seller's gradient with triangle pointer, bold stats row ("6.1 mo inventory · 24 median DOM · 99% sale-to-list"), and "Based on X active listings · Y sales in {area} (last 90 days)". Sentiment renders even when there aren't enough yearly data points for the chart. Verified live for Waterfront Communities C1: {active:782, sold90:383, label:Buyer's Market, monthsInventory:6.1, medianDom:24, saleToList:99}. `php -l` + `npm run build` (client+SSR) pass.

- **[FEATURE]** Client feedback batch (3 items): registration asks "Are you a real estate agent?", Google sign-in works on tenant landing domains (central relay), Request a Viewing modal redesigned with date/time picker + consent
  - Files changed: `database/migrations/2026_07_16_200000_add_is_agent_to_users_table.php` (new), `app/Models/User.php`, `app/Http/Controllers/Auth/RegisteredUserController.php`, `app/Http/Controllers/Auth/GoogleAuthController.php`, `routes/auth.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `resources/js/Website/Global/Components/LoginModal.jsx`, `resources/js/Pages/Auth/Register.jsx`, `resources/js/Website/Global/Components/ViewingRequestModal.jsx` (rewritten), `resources/js/Website/Sections/PropertyDetail/MobileBottomBar.jsx` (rewritten), `resources/js/Website/Pages/PropertyDetail.jsx`
  - Details: (1) AGENT QUESTION — new `users.is_agent` boolean (migration run), fillable + boolean cast, validated/stored in RegisteredUserController (which now also persists `phone` — collected but never saved before); No/Yes radios in the LoginModal Create Account tab and the full /register page. (2) GOOGLE ON TENANT DOMAINS — Google's callback URI is registered for ONE host, so tenant sign-ins now RELAY: tenant /auth/google detects its host differs from the configured redirect host and hops to `{callbackHost}/auth/google?origin={tenant}`; the relay start validates origin against active website domains (404 otherwise, and plain /auth/google on the admin host still 404s to keep the bare login), stashes origin+redirect_to in session, runs Socialite; the callback, for relayed flows, does NOT log in locally — it mints a single-use 5-min cache token and redirects to `{tenant}/auth/google/complete?token=...` (new route) which exchanges the token for a local session (Auth::login + session regenerate + safe redirect). auth/google moved OUT of the not.main-domain group (controller enforces the same policy); `googleOAuthEnabled` prop no longer requires `is_default`, so the LoginModal's existing Google button now shows on every tenant site. New-user notifications report the tenant host/site name, not the callback host. (3) VIEWING MODAL — ViewingRequestModal rewritten to the client's reference: In-Person/Virtual Tour segmented toggle, horizontal next-14-days date strip (scroll-snap), Morning/Afternoon/Evening slots, Full Name/Email/Phone, required consent checkbox (site-branded copy + privacy link), and it now actually submits to POST /api/tour-requests (existing endpoint + admin Tour Requests page; viewing type carried in message, date/time in selected_date/selected_time) — the old MobileBottomBar inline copy was a dead console.log form; MobileBottomBar now opens the shared modal (accepts a `property` prop, PropertyDetail passes its listing so the address/MLS goes with the request). Verified: migration ran, route:list shows auth/google + callback + complete, php -l all files, `npm run build` (client+SSR) passes. NOTE: requires GOOGLE_REDIRECT_URI host to be a registered redirect URI in Google Cloud Console (already is) — no per-domain Google config needed anymore.

- **[FIX]** Client feedback batch (5 items): filters modal fits one mobile screen, property tabs wrap (no slide), footer "Get in touch" pill removed, property FAQs fixed + dynamic per listing
  - Files changed: `resources/js/Website/Components/FiltersModal.jsx`, `resources/js/Website/Components/PropertyDetail/PropertyDetailsSections.jsx`, `resources/js/Website/Global/Footer.jsx`, `app/Services/GeminiAIService.php`, `app/Http/Controllers/WebsiteController.php`, `app/Http/Controllers/Api/PropertyAiController.php`
  - Details: (1) FILTERS MODAL — compact mobile sizing (h-9 controls, mb-3 sections, tighter paddings, sm: restores desktop), `max-h-[92dvh]`, and the secondary sections (Home Type, Days on Market, Condo Includes, Amenities, Keywords) collapse behind a mobile-only "More filters" accordion so Status/Price/Type/Beds/Baths + footer buttons fit one phone screen; removed a console.log. (2) PROPERTY TABS — the tab row was `overflow-x-auto` (horizontal slide, tabs cut off on phones); now `flex-wrap` on all breakpoints with compact mobile sizing (h-10, text-base; md restores) so every tab is visible with no slide. (3) FOOTER — decorative non-functional "Get in touch" pill removed. (4+5) PROPERTY FAQs — root cause: callers pass the RAW Repliers listing (nested address/details) but the FAQ prompt AND `getFallbackFaqs()` read flat AMPRE keys, so every field defaulted (0 bedroom, 0 bathroom, $0) and — worse — `parseFaqResponse()` fell back with an EMPTY array, then those junk FAQs were stored per-MLS in `property_faqs` and served as "cached" forever. Fix: new `normalizeFaqData()` maps Repliers→flat keys (beds+plus, midpoint of sqft ranges, taxes assoc/indexed, lease→For Rent) at the `generatePropertyFaqs()` entry; `parseFaqResponse($response, $data)` falls back with real data; zero-guards in all fallback copy (no more "0 bedroom"/"$0" — price omitted or "available on request"); and `faqsLookStale()` detects legacy zero-data rows which are purged + regenerated automatically on the property page read path, the API cached branch, and the service cached branch — production self-heals without a manual step. Verified via tinker against C13540184: normalizes to 3 bed/2 bath/$6,500/1300sqft/Waterfront Communities C1/For Rent, fallback FAQ text is listing-specific, stale detector flags legacy rows and passes new ones. `php -l` + `npm run build` (client+SSR) pass.

- **[FIX]** Footer logo broken on production (header fine): footer now uses website.logo_url first — the exact source the header renders; legacy '/assets/logo.png' seeds fixed
  - Files changed: `resources/js/Website/Global/Footer.jsx`, `app/Models/WebsitePage.php`, `resources/js/Pages/Admin/Websites/EditHomePage.jsx`, `app/Console/Commands/InitializeWebsite.php`, `app/Http/Controllers/Admin/AdminController.php`, `database/seeders/WebsiteSeeder.php`, `database/seeders/HomePageSeeder.php`
  - Details: The footer preferred `footerData.logo_url` from saved page content, which holds the legacy default `/assets/logo.png` — but the tracked file is `/assets/Logo.png` (capital L). Case-insensitive Windows dev never noticed; the case-sensitive production Linux box 404s it, so the footer logo rendered broken while the header (which reads `website.logo_url`, the site's real uploaded logo) loaded fine. Fix: footer logo chain flipped to `effectiveWebsite.logo_url || footerData.logo_url` so the footer always carries the header's logo per client request (page-content value is now only a no-website-logo fallback); footer seeds in WebsitePage/EditHomePage default to '' ; the four website.logo_url seeds/fallbacks (InitializeWebsite, AdminController, WebsiteSeeder, HomePageSeeder) corrected to the real-cased `/assets/Logo.png`. `php -l` on all + `npm run build` (client+SSR) pass.

- **[FEATURE]** Footer CONTACT US per client spec: fixed phone +1 416 669 4755, fixed email info@jatingill.com, address = the domain's building address
  - Files changed: `resources/js/Website/Global/Footer.jsx`, `app/Http/Middleware/HandleInertiaRequests.php`
  - Details: Footers on landing pages showed the shared Nobu website contact_info (647-490-1532 / info@noburesidences.com / 55 Mercer Street) on every domain. Per client: phone and email are fixed for all landing pages (same number as the WhatsApp button, +14166694755; tel: link strips formatting automatically), and the address must be each domain's building address. HandleInertiaRequests now selects `address` into the cached footer_building payload and shares `globalWebsite.building_address`; Footer's contact block uses the fixed CONTACT_PHONE/CONTACT_EMAIL constants and `building_address` (website contact_info address as fallback when the site has no linked building). Admin per-page overrides (use_global_contact=false + custom_* fields) still take precedence, unchanged. The 60s footer_building cache self-refreshes so no invalidation is needed post-deploy. `php -l` + `npm run build` (client+SSR) pass.

- **[FIX]** Footer brand shows the same logo image as the header (client feedback)
  - Files changed: `resources/js/Website/Global/Footer.jsx`
  - Details: The footer's brand block rendered `{siteName.toUpperCase()}` as plain text ("THE WELL") even though `footerContent.logo_url` (admin footer override → website logo, the same source the header Navbar uses) was already computed but never rendered. The brand block now shows the logo image (object-contain, 160x55 mobile / 220x60 desktop — matching the header sizes) and keeps the uppercase text name only as the no-logo fallback. White-on-navy logos read correctly since the footer background is the same navy family. `npm run build` (client+SSR) passes. Also CONFIRMED for the client that the WhatsApp pre-filled messages match their templates: "Hi, I'd like to get more information about the {For Sale|For Rent} listing {MLS} at {address}, {building name}." — real building name substituted, omitted when no building matches (no code change).

- **[FIX]** Property detail building card (MerchandiseLofts) showed wrong counts ("0 Condos for Sale / 1 for Rent" for The Well) — now uses the live-counts endpoint
  - Files changed: `resources/js/Website/Components/PropertyDetail/MerchandiseLofts.jsx`
  - Details: The card fetched counts client-side: it re-parsed the building's street addresses with a `/^(\d+)\s+/` regex and called /api/buildings/count-mls-listings per parsed address. Ranged addresses like "455-480 Front St W" don't match that regex (no whitespace after the first number), so every Front St address was silently dropped and only "455 Wellington St W" got counted — hence 0 sale / 1 rent while the homepage (which uses `Building::getLiveListingCounts()`) correctly showed 26/15. Fix: after find-by-address resolves the building, the card now POSTs its id to /api/buildings-counts — the same compound-address-aware, 10-min-cached backend path the homepage and building cards use (verified via tinker: The Well's compound address returns {sale:26, rent:15}). The old single-address GET remains only as a fallback when no building id is available; the client-side address fan-out is deleted. Also removed a leftover console.log on image load. `npm run build` (client+SSR) passes.

- **[FIX]** Room Dimensions no longer shows "0 x 0 m" for unmeasured rooms (client feedback, e.g. C13540184 Bathroom/Foyer)
  - Files changed: `app/Http/Controllers/WebsiteController.php`, `resources/js/Website/Components/PropertyDetail/PropertyDetailsSections.jsx`, `resources/js/Website/Components/PropertyDetail/PropertyRooms.jsx`
  - Details: Root cause verified against the live Repliers API — unmeasured rooms (bathrooms, foyers) come back with `length: "0.0", width: "0.0"`, and `"0.0"` is a TRUTHY string in PHP, so `formatRooms()`'s `($length && $width)` check passed and baked a literal "0.0 x 0.0 m" into the `dimensions` field, which both frontend renderers trusted (`if (room.dimensions) return room.dimensions`) and re-formatted to "0 x 0 m". Fix: (1) backend `formatRooms()` gates on `(float) $length > 0 && (float) $width > 0`; (2) `PropertyDetailsSections.roomDims()` now prefers the numeric length/width fields and rejects any pre-formatted dimensions string that parses to zeros (covers cached/legacy AMPRE `RoomDimensions` too); (3) same zero-string guard on `PropertyDetail/PropertyRooms.jsx`'s fallback (legacy tabbed layout). Missing dimensions render as a subtle gray em-dash so the row (room name + features like "3 Pc Bath") still reads cleanly. The `Components/Property/PropertyRooms.jsx` copy already parseFloat-guards — untouched. `php -l` + `npm run build` (client+SSR) pass.

- **[FIX]** Property Enquiry modal fits the mobile viewport without scrolling (client feedback)
  - Files changed: `resources/js/Website/Components/PropertyEnquiryModal.jsx`
  - Details: On phones the modal's content stack (p-6 header/form, py-3 inputs, 4-row textarea, full labelled Property Details list) totalled ~950px vs a ~660px visual viewport, so the Send Enquiry button sat below the fold behind `max-h-[90vh] overflow-y-auto`. Mobile-only compaction (desktop `sm:` values unchanged): header/form `p-4 sm:p-6`, title `text-xl sm:text-2xl`, field labels `text-sm mb-1` (sm restores), inputs/textarea `py-2 sm:py-3`, field gaps `mb-3 sm:mb-4`, textarea `rows={3}`, and the Property Details box collapses to a single wrapped Price/Bedrooms/Bathrooms line (heading + Address row `hidden sm:block` — the address already sits in the modal header). Container `max-h-[90vh]` → `max-h-[92dvh]` so the cap tracks the real visible height under mobile browser chrome; `overflow-y-auto` kept as a safety net for very short/landscape screens. Mobile stack now ~560px — fits with room for a validation error. `npm run build` (client+SSR) passes.

- **[FEATURE]** Listing card images are slidable left/right (client feedback): swipe carousel on mobile, hover arrows on desktop, dot indicators
  - Files changed: `resources/js/Website/Global/Components/PropertyCards/PropertyCardImageCarousel.jsx` (new), `resources/js/Website/Global/Components/PropertyCards/PropertyCardV5.jsx`
  - Details: PropertyCardV5 (used on /toronto/for-rent, /toronto/for-sale, /search, similar/nearby/comparables, favourites) rendered only `property.imageUrl` even though the search API already returns every listing photo (`Images` array, now `?class=medium`). New `PropertyCardImageCarousel` renders the photos as a native horizontal scroll-snap strip (swipe on touch, `overscroll-x-contain`), desktop prev/next arrows that appear on card hover (preventDefault+stopPropagation so the card link doesn't fire), and white dot indicators; capped at 8 slides, first slide keeps the existing PluginStyleImageLoader skeleton/lazy behavior, slides 2+ are plain `loading="lazy"` imgs, single-image listings fall back to the exact previous rendering (incl. hover zoom). KEY FIX for swipe: the card's chips overlay (`absolute inset-2`) covered the whole image and would have swallowed touch pans — it's now `pointer-events-none` with `pointer-events-auto` restored on the Compare and heart buttons (chip spans and image clicks still navigate via the card `<a>` as before). Building cards keep a single image. `npm run build` (client+SSR) passes.

- **[PERF]** Core Web Vitals batch (PageSpeed mobile+desktop): static asset cache headers, async fonts, on-demand Google Maps loader, es2022 build target; Building SEO og:image + ApartmentComplex JSON-LD use the building hero image
  - Files changed: `public/.htaccess`, `resources/views/app.blade.php`, `vite.config.js`, `package-lock.json`, `app/Support/Schema.php`, `app/Http/Controllers/WebsiteController.php`, `resources/js/utils/googleMaps.js` (new), `resources/js/hooks/useGooglePlacesAutocomplete.js`, `resources/js/Website/Components/PropertySearch/IDXAmpreSearchBar.jsx`, `resources/js/Website/Components/SimplePropertyMap.jsx`, `resources/js/Website/Components/ClusteredPropertyMap.jsx`, `resources/js/Components/EnhancedPropertyMap.jsx`, `resources/js/Components/EnhancedSearchBar.jsx`, `resources/js/Components/ModernSearchBar.jsx`
  - Details: (1) CACHE LIFETIMES — .htaccess now sends `Cache-Control: public, max-age=31536000, immutable` for hashed /build assets and 30 days for images/fonts/css/js (mod_headers + mod_expires; verified locally). (2) RENDER-BLOCKING — bunny.net fonts stylesheet loads via media="print" onload swap + noscript fallback, preconnect kept. (3) UNUSED JS — the Maps JS API `<script>` was removed from app.blade.php; new shared `loadGoogleMaps()` util injects it once on demand (superset libraries places,drawing,geometry,marker, loading=async, promise-cached, reuses a foreign script tag if present). All 7 consumers (IDXAmpreSearchBar, Simple/Clustered/Enhanced property maps, Enhanced/Modern search bars, useGooglePlacesAutocomplete admin hook) now await the loader instead of injecting their own conflicting script tags. BuildingDetail (= tenant landing page) uses no maps, so those pages no longer download ~250KB of Maps JS at all. (4) LEGACY JS — vite build.target es2022 + update-browserslist-db (caniuse-lite was 13 months old). (5) BUILDING SEO/SCHEMA — new `Schema::building()` emits ApartmentComplex JSON-LD (name, url, hero image(s), description, PostalAddress, geo, units, yearBuilt); renderBuildingDetailPage and home() now set `seo.image` = building main_image (absolute), so og:image/twitter:image = building hero instead of site logo. Verified: homepage + /toronto/the-skyline-tower render og:image + ApartmentComplex; no maps.googleapis in HTML; fonts async; build-asset Cache-Control immutable, images 30d; php -l + `npm run build` (client+SSR) pass. NOT addressed (out of scope/3rd-party): widgetbe.com tracker cache TTL, "improve image delivery" beyond earlier hero work (needs an image resize/WebP pipeline), residual unused CSS (~22KiB tailwind), forced reflow (third-party script).

- **[FIX]** Client feedback batch: slow images (Repliers CDN size classes), per-domain logo on generic pages, centered mobile header logo, compare page mobile layout
  - Files changed: `app/Services/RepliersApiService.php`, `app/Http/Controllers/Api/PropertyDetailController.php`, `app/Http/Controllers/PropertySearchController.php`, `app/Http/Controllers/WebsiteController.php`, `app/Http/Controllers/Api/AlertHistoryController.php`, `app/Http/Controllers/Api/OptimizedPropertyDetailController.php`, `app/Http/Controllers/Api/HomepagePropertiesController.php`, `app/Http/Controllers/Api/PropertyFavouriteController.php`, `app/Http/Controllers/Api/PropertyImageController.php`, `app/Services/SavedSearchAlertService.php`, `routes/web.php`, `resources/js/Website/Global/Navbar.jsx`, `resources/js/Pages/CompareListings.jsx`
  - Details: (1) SLOW IMAGES — every MLS image URL was the full-resolution cdn.repliers.io original (often MBs). `getImageUrl()`/`getListingImageUrls()` now take an optional Repliers CDN size class (`?class=small|medium|large`); all card/thumbnail call sites pass `medium` (54KB vs 184KB+ verified via HEAD requests on a live CDN image), tiny thumbs (map popups, autocomplete, alert rows) pass `small`, detail-page galleries pass `large`. (2) WRONG LOGO ON GENERIC PAGES — `/compare-listings`, `/dashboard`, `/user/favourites`, `/user/alerts` route closures hardcoded the DEFAULT website (`is_default=true`), so on tenant domains (e.g. wellcondostoronto.ca) these pages rendered the Nobu logo/branding instead of the building site's. They now resolve via `TenantResolver::resolve(request())` with `defaultWebsite()` fallback, matching every other public page — the building's own logo/branding follows onto generic pages. Navbar also reads logo/name from `effectiveWebsite` (falls back to domain-resolved `globalWebsite`) for robustness. (3) MOBILE HEADER — in the dark-band header the mobile logo is now horizontally centered (absolute left-1/2, hamburger pinned right); white-pill variant unchanged. (4) COMPARE PAGE MOBILE — thumbnails were `grid-cols-1` on phones so cards stacked and didn't correspond to the 3-column value grids below; now always 3-up with compact mobile sizing (90px images, smaller badge/remove button/text), plus `loading="lazy"` (first slot eager) and `decoding="async"`. `php -l` on all edited PHP files and `npm run build` (client+SSR) pass.

- **[FIX]** Landing pages hardened against scraping and stack/owner fingerprinting (round 2): admin route map no longer embedded in public HTML, CSRF cookie renamed, scraper/AI-bot blocking, generic error pages
  - Files changed: `config/ziggy.php` (new), `app/Http/Middleware/BlockScrapers.php` (new), `app/Http/Middleware/ValidateCsrfToken.php` (new), `resources/views/errors/` (new: minimal + 401/403/404/419/429/500/503), `bootstrap/app.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `resources/views/app.blade.php`, `resources/js/bootstrap.js`, `resources/js/utils/csrf.js`, `public/robots.txt`
  - Details: (1) ZIGGY ROUTE LEAK — `@routes` + the shared `ziggy` prop embedded ALL 171 named routes (116 `admin.*`, `sanctum.csrf-cookie`, `storage.local`) into every landing page's HTML, exposing the whole admin panel structure and the framework. New `config/ziggy.php` "public" group (`!admin.*`, `!sanctum.*`, `!storage*`); app.blade.php `@routes(...)` and HandleInertiaRequests pass the group for non-`admin*` paths (admin pages keep the full list for `route('admin.*')`). Verified: public page JSON now carries 53 routes, zero admin/sanctum/storage. (2) XSRF-TOKEN COOKIE (Laravel fingerprint, flagged as "inherent" last round — it isn't): `App\Http\Middleware\ValidateCsrfToken` overrides `newCookie()`/`serialized()` to issue the token as `ct`, swapped into the web group via `$middleware->web(replace:)` (the `validateCsrfTokens(except:)` list still applies — static on the parent). axios told `xsrfCookieName='ct'` in bootstrap.js (Inertia dedupes to the same axios instance — verified in node_modules) and `utils/csrf.js` reads `ct`. E2E verified: POST /login with the ct-derived X-XSRF-TOKEN header → 422 wrong-credentials (not 419); without token → 419. (3) ANTI-SCRAPING — new global `BlockScrapers` middleware 403s HTTP libraries (python-requests/curl/wget/scrapy/okhttp...), AI-training crawlers (GPTBot/ClaudeBot/CCBot/Bytespider/PerplexityBot...), SEO-intelligence bots that power owner-correlation tools (AhrefsBot/SemrushBot/MJ12bot/BuiltWith...), security scanners (sqlmap/nuclei/wpscan...) and empty User-Agents; search engines (Googlebot/Bingbot) and social link-preview bots (WhatsApp/facebookexternalhit/Twitterbot) explicitly untouched — WhatsApp sharing + SEO keep working (all verified with curl -A). robots.txt mirrors the blocks and disallows auth pages for everyone (advisory layer; /admin deliberately NOT listed to avoid advertising it). (4) OWNER LEAK — `config('app.name')` ("Nobu Residences") was the fallback `<title>`/`og:site_name` on any tenant domain without meta; fallback is now the request host. (5) ERROR PAGES — Laravel's recognizable default error screens replaced with neutral minimal pages (also `noindex`). `npm run build` (client+SSR) passes. REMAINING (server-level, not code): `Server: Apache/2.4.54 ... PHP/8.4.16` header must be silenced on the production box (Apache `ServerTokens Prod` / nginx `server_tokens off`), APP_DEBUG must stay false in production, WHOIS privacy; the Inertia `data-page` attribute itself stays detectable (architectural), and the hardcoded Follow Up Boss tracker ID `WT-AZDKVTDF` + shared Google Maps key still correlate all tenant domains to one owner for anyone comparing sources.

- **[FIX]** Footer CTA image: removed generic stock house photo; footer image now defaults to the domain's linked building image
  - Files changed: `resources/js/Website/Global/Footer.jsx`, `app/Models/WebsitePage.php`, `resources/js/Pages/Admin/Websites/EditHomePage.jsx`
  - Details: The per-domain building-image fallback shipped earlier never showed because saved page content (and the WebsitePage/EditHomePage defaults) stored `/assets/house-img.jpg` as `footer.background_image`, which won the fallback chain — so wellcondostoronto.ca still rendered the stock house. Footer.jsx now treats the legacy `/assets/house-img.jpg` value as "no override": chain is admin custom image → `globalWebsite.building_image` → `building_logo` → hide the image block entirely (stock photo removed). Both default-content seeds now use `''` so new pages don't re-store the stock path. Existing DB rows need no migration — the render-time filter handles them. Verified locally (the one seeded website's building has main_image) and `npm run build` (client+SSR) passes.

- **[FIX]** Websites now reflect building edits immediately: Building saved() hook busts the listing-counts and footer-building caches
  - Files changed: `app/Models/Building.php`
  - Details: Tenant sites render building data live but behind caches (`building_listing_counts:{id}` 10 min, `website.{id}.footer_building` 60s), so an admin edit didn't show on the site until expiry. A `static::saved()` hook now forgets both on every save (admin edits, image uploads, CSV imports). Repliers search cache (5 min, param-keyed) rolls over on its own — address changes produce new params anyway. Also VERIFIED the deployed address-expansion fix live on wellcondostoronto.ca: /api/homepage-properties now returns 26 for-sale and 15 for-rent listings for The Well (was 0/0), and building images serve same-origin (relative /images/... paths).

- **[FIX]** Compound street addresses ("455-480 Front St W & 455 Wellington St W") expanded to garbage, so Repliers matched nothing and For Sale/For Rent showed 0
  - Files changed: `app/Models/Building.php`, `app/Http/Controllers/WebsiteController.php`, `app/Services/BuildingCsvImportService.php`, `resources/js/Pages/Admin/Buildings/Create.jsx`, `resources/js/Pages/Admin/Buildings/Edit.jsx`
  - Details: Root cause — every expander ran the hyphen-range regex on the WHOLE address before splitting on "&", so the "& 455 Wellington St W" tail was glued onto every generated number ("456 Front St W & 455 Wellington St W" x26), and the backend parsers (parsedStreetAddresses / collectStreetAddresses) never split "&" inside street_address/additional_addresses values either — parseStreetAddress turned them into garbage street names that match no MLS listing. Fix: (1) new `Building::splitCompoundAddress()` — splits on ","/"&" FIRST, then expands hyphen ranges per segment (unit prefixes like "1408-123 Main St" where end<=start never expand; combined cap ~60); (2) `parsedStreetAddresses()` (live counts + homepage carousel) and WebsiteController `collectStreetAddresses()` (building page listings) now pass every raw candidate through it — so buildings already saved with the buggy expanded rows resolve correctly WITHOUT re-saving; (3) frontend `detectAddressRange` in Buildings Create/Edit and the CSV import's PHP port rewritten segment-first ("455-480 Front St W & 455 Wellington St W" → 26 Front numbers + 455 Wellington = 27). Verified via tinker: compound → 27 clean entries; saved-garbage row → ["456 Front St W","455 Wellington St W"]; "1408-123 Main St" unexpanded; parsedStreetAddresses returns clean {number,name} pairs (Front 455.., Wellington 455). `php -l` + `npm run build` pass. Note: listing counts cache (`building_listing_counts:{id}`, 10 min) refreshes itself after deploy.

- **[FEATURE]** Client feedback batch: per-domain footer building image, separate mobile/desktop hero images, landing-page image/perf fixes
  - Files changed: `app/Models/Building.php`, `app/Http/Controllers/Api/BuildingController.php`, `app/Http/Controllers/Admin/BuildingController.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `database/migrations/2026_07_16_120000_add_hero_image_mobile_to_buildings_table.php` (new), `resources/js/Website/Global/Footer.jsx`, `resources/js/Website/Sections/Home/HeroSection.jsx`, `resources/js/Website/Sections/BuildingDetail/BuildingGallery.jsx`, `resources/js/Website/Pages/BuildingDetail.jsx`, `resources/js/Pages/Admin/Buildings/Create.jsx`, `resources/js/Pages/Admin/Buildings/Edit.jsx`
  - Details: (1) FOOTER IMAGE PER DOMAIN — the footer CTA image was a hardcoded stock `/assets/house-img.jpg`. HandleInertiaRequests now shares the site's linked building (`homepage_building_id`) name/image/logo on `globalWebsite` (60s cache); Footer.jsx fallback chain is admin override → building photo → building logo → stock. (2) MOBILE VS DESKTOP HERO — new nullable `buildings.hero_image_mobile` column (migration run), fillable + host-strip accessor + getDisplayData, `image_type=hero_mobile` in the upload API, "Mobile Hero Image" upload UI on Buildings Create/Edit, and `<picture>` with `(max-width:767px)` source in both HeroSection (ICE home, also honors `pageContent.hero.background_image_mobile`) and BuildingGallery's main image (building-as-homepage sites). (3) PERF/OWNER LEAK — production uploads stored ABSOLUTE `https://pcdadmin.com/images/...` URLs (asset() at upload), so every tenant landing page hotlinked the admin domain (extra DNS/TLS + owner leak). `stripLocalHost()` now strips any host from own `/images/` paths (external URLs untouched — verified Unsplash URL preserved) and uploadImage stores root-relative paths going forward. Also removed 4 debug console.log calls dumping full building data on every BuildingDetail render, added `fetchPriority="high"` to both hero images (LCP) and `loading="lazy"` to gallery modal/thumbnail images. Verified locally: migration ran, accessor strips pcdadmin URLs to `/images/...`, preview page props carry `building_name`/`building_image` and relative `hero_image_mobile`; `npm run build` (client+SSR) passes. NOTE: needs push + deploy; Repliers listing fetches were already cached server-side (TTFB warm ~0.8s).

- **[FEATURE]** Floating WhatsApp icon (bottom-right) on the public landing pages with page-aware pre-filled messages (client feedback)
  - Files changed: `resources/js/Website/Global/Components/WhatsAppButton.jsx` (new), `resources/js/Website/Global/Components/index.js`, `resources/js/Website/Home.jsx`, `resources/js/Website/Pages/PropertyDetail.jsx`
  - Details: New reusable `WhatsAppButton` component — fixed bottom-right green circle (official WhatsApp glyph, z-50, hover scale) linking to `https://wa.me/14166694755?text=...` (client number +1 416 669-4755) in a new tab; renders nothing without a message. Homepage (Home.jsx) message: "Hi, I'd like to get information about unit availability at {building_name}." using `buildingData.name` (the admin-configured homepage building) with siteName fallback. Property detail message: "Hi, I'd like to get more information about the {For Sale|For Rent} listing {mls_id} at {address}, {building_name}." — For Rent is detected via the same `/rent|lease/i` TransactionType test PropertyHeader uses (Repliers sends "For Lease" for rentals), MLS ID from `mlsNumber` (falls back to listingKey), address is the formatted unparsed address, and the building-name suffix is omitted when the listing isn't matched to a building in our DB. Button hidden while property data is still loading, and lifted to `bottom-24` on mobile so it clears the fixed MobileBottomBar (z-40). Landing pages only — no admin surface touched. `npm run build` (client+SSR) passes.

- **[FIX]** Landing pages no longer fingerprint the stack or the owner: removed Laravel/PHP version props, X-Powered-By header, and the branded session cookie name
  - Files changed: `app/Http/Controllers/WebsiteController.php`, `resources/js/Website/Home.jsx`, `public/index.php`, `config/session.php`
  - Details: Verification of "landing pages must not be scrapable / must not reveal owner or tech" found three leaks on every tenant page: (1) `laravelVersion` + `phpVersion` shared in getWebsiteSettings() page props (both the fallback and main return) — removed, and the unused destructured props dropped from Home.jsx; (2) `X-Powered-By: PHP/x.y.z` response header — removed via header_remove() at the top of public/index.php; (3) session cookie named `nobu_residences_session` (APP_NAME-derived) leaked the owner brand on all tenant domains — config/session.php default is now a generic `sid` (SESSION_COOKIE env still overrides; changing the name logs out existing sessions once). Also verified on a simulated tenant domain (Host header vs artisan serve): /register and /forgot-password work (200) on tenant sites and 404 on the main domain; Google signup on tenant sites is still hidden — GOOGLE_REDIRECT_URI is pinned to the main domain and each landing domain would need its own authorized redirect URI in Google Cloud Console before it can be enabled. Remaining server-level items (not code): Apache `Server` header/ServerTokens, WHOIS privacy for owner anonymity, XSRF-TOKEN cookie is inherent to the stack.

- **[CHORE]** New `buildings:debug-counts` artisan command to trace why Available Units (Live MLS) shows 0
  - Files changed: `app/Console/Commands/DebugBuildingCounts.php` (new)
  - Details: Prints every stage of `Building::getLiveListingCounts()` for one building: raw address fields, `parsedStreetAddresses()` output, Repliers API key/url, the `building_listing_counts:{id}` cache value, then runs the exact same grouped street queries verbosely — per street it shows the wanted street numbers, how many listings the API returned, which street numbers appeared in the response, and how many matched. `--fresh` clears the counts cache and bypasses the listings cache. Accepts a bare UUID or the admin `name-slug-uuid` form. Diagnoses the three 0-causes: no parsed addresses (range never expanded because the building was saved before the URL-blocker fix), street-number mismatch, or API failure. Verified locally against the seeded Skyline Tower (live API round-trip works, matcher output correct).

- **[FEATURE]** Main domain login screen is now bare: removed Sign up link, Forgot password link, and Google login — and their URLs now 404 on the main domain (tenant sites keep their full auth UI)
  - Files changed: `app/Http/Middleware/HandleInertiaRequests.php`, `resources/js/Pages/Auth/Login.jsx`, `app/Http/Middleware/NotOnMainDomain.php` (new), `bootstrap/app.php`, `routes/auth.php`
  - Details: Middleware now shares an `isMainDomain` prop (TenantResolver::isAdminHost on the request host). Login.jsx hides the Forgot password link, the Google OAuth divider+button, and the Register/Sign up block when `isMainDomain` is true. `googleOAuthEnabled` no longer keys off isAdminHost — it's back to `$website && $website->is_default` AND not-main-domain. New `not.main-domain` route middleware (NotOnMainDomain, aliased in bootstrap/app.php) 404s register (GET/POST), forgot-password (GET/POST), reset-password (GET/POST), and auth/google on the admin/main host; login/logout untouched. Consequence: admins on the main domain cannot self-reset passwords — resets must go via admin panel/artisan. `npm run build` (client+SSR) passes; verified on nobu.test: /register, /forgot-password, /reset-password/x, /auth/google → 404, /login → 200, props isMainDomain=true, googleOAuthEnabled=false.

- **[FIX]** Building Edit form could not be saved when Website URL was a bare domain; Building view page always showed Available Units 0
  - Files changed: `resources/js/Pages/Admin/Buildings/Edit.jsx`, `resources/js/Pages/Admin/Buildings/Show.jsx`, `app/Http/Controllers/Admin/BuildingController.php`
  - Details: (1) Website URL / Virtual Tour URL inputs were `type="url"` — typing a bare domain like "wellcondostoronto.ca" triggered the browser's native "Please enter a URL." bubble and BLOCKED the whole UPDATE BUILDING submit, which is why management/amenities edits silently never saved. Changed both to `type="text"` (inputMode="url") and the submit now normalizes a missing scheme to `https://` via Inertia `transform()` (which runs at submit time — the old `put(url, formData)` second-arg pattern was a no-op since useForm treats it as options; amenity ids were already synced into form data by toggleAmenity, so transform also carries them for correctness). Server-side `normalizeUrlFields()` in store()/update() prepends `https://` too so bare domains never render as relative links. (2) Building Show "Available Units" read `building.available_units_count`, an accessor without `$appends` that never serializes — the page always rendered 0. show() now sends live MLS counts from `getLiveListingCounts()` (10-min cache, fails soft to 0/0): `available_units_count` = sale+rent, plus `units_for_sale`/`units_for_rent`, and the page shows "N (X for sale, Y for rent)" labeled "Available Units (Live MLS)". Amenities on the Show page verified working — relation serializes correctly (tested locally); "No amenities listed" was real data emptiness caused by the blocked submit. `php -l` + `npm run build` (client+SSR) pass.

- **[FIX]** Website detail page (Admin > Websites > Show) showed nothing about the linked building, and its "View Live Site" opened the wrong site
  - Files changed: `app/Http/Controllers/Admin/WebsiteManagementController.php`, `resources/js/Pages/Admin/Websites/Show.jsx`
  - Details: show() never loaded the `homepageBuilding` relation and the page had no building section, so Total Units/Floors/Year Built etc. were invisible no matter what was saved on the building. Now show() eager-loads a trimmed homepageBuilding (id, name, slug, address, city, main_image, total_units, floors, year_built, units_for_sale, units_for_rent, developer_name, management_name, corp_number) and Show.jsx renders a "Linked Building" card with those facts + a View Building link (same `{name-slug}-{id}` route key helper as Buildings/Index). Also, "View Live Site" linked to plain `/` — on the admin host that never serves the tenant site (now redirects to login per the main-domain change), so previewing a new site showed the wrong thing; it now uses `https://{domain}` when a domain is set, else the `/?website={slug}` preview override (same logic as Index/Created). The public landing page itself (Home hero stats + BuildingInfo) already reads live `buildingData.total_units` from `homepage_building_id`, so no public-site change was needed.

- **[FEATURE]** Main domain (pcdadmin.com) no longer serves a landing page or default site — root redirects to login; sites only served on their own domains or via ?website= preview
  - Files changed: `app/Services/Tenancy/TenantResolver.php`, `app/Http/Controllers/WebsiteController.php`, `app/Http/Middleware/HandleInertiaRequests.php`
  - Details: TenantResolver no longer returns the default website for admin/main hosts — it returns null, so every public site route on the main domain 404s (the ?website={slug} admin preview override still works and is checked first). WebsiteController@home redirects admin-host "/" to the login screen (dashboard if already authenticated) before website resolution. googleOAuthEnabled in HandleInertiaRequests now keys off isAdminHost() instead of `$website->is_default` so the Google login button stays on the main-domain login screen where the OAuth redirect URI is registered. Admin panel pages keep the defaultWebsite() fallback purely for panel branding. Verified locally: `/` → 302 `/login`, `/rent` → 404, `/login` → 200, `/?website=nobu-residences` → 200.

- **[FIX]** Building detail page showed N/A for Management Company and Architect even after filling the form
  - Files changed: `resources/js/Pages/Admin/Buildings/Show.jsx`, `resources/js/Pages/Admin/Buildings/Edit.jsx`, `resources/js/Pages/Admin/Buildings/Create.jsx`, `app/Http/Controllers/Admin/BuildingController.php`
  - Details: Two root causes. (1) Column mismatch: the Create/Edit forms save the input to `management_name`, but Show.jsx displayed the legacy never-written `management_company` column — now shows `management_name` with `management_company` as fallback. (2) Missing input: `architect` existed in both forms' useForm state but no input was ever rendered, so the column could never be filled from the UI — added an Architect TextInput to Create and Edit (next to Management Company) and added the missing `'architect' => 'nullable|string'` rule to `store()` validation (update() already had it). `npm run build` (client+SSR) passes.

## 2026-07-15

- **[FEATURE]** Auto-generate a favicon from the site logo on Website Create (or upload a custom one)
  - Files changed: `app/Services/FaviconGenerator.php` (new), `app/Http/Controllers/Admin/WebsiteManagementController.php`, `resources/js/Pages/Admin/Websites/Create.jsx`
  - Details: `FaviconGenerator` turns the site logo into a 64×64 transparent PNG favicon (GD contain-fit, never stretched; SVG logos are passed through since browsers accept an SVG favicon) saved to `public/assets`. Reads the logo from local public storage or a remote URL, fails soft to the inherited favicon on any error. The Websites → Create branding step gains a "Favicon (browser tab icon)" block: "Generate the favicon from the logo automatically" (default on) or upload a custom favicon (PNG/ICO/SVG/WebP), with a live preview. `store()` auto-derives the favicon from the finalized logo when no file was uploaded and the flag is on — running after the logo is resolved and overriding any favicon inherited from the default site. `favicon_file` validation now also allows `webp`. Untouched branding (the one-click flow) still strips logo/colors/favicon so default-site inheritance is unchanged. `npm run build` (client+SSR) and `php -l` pass.
  - Note: An earlier building-logo-scraper feature (auto-scrape logos from marketing sites + precompute brand_colors) was pushed together with this in commit 1dbfb9f, then removed from main per request — only the favicon feature is kept.

- **[FEATURE]** Buildings now own a logo, and the website color theme is auto-detected from the selected building's logo on Website Create
  - Files changed: `database/migrations/2026_07_15_000000_add_logo_to_buildings_table.php`, `app/Models/Building.php`, `app/Http/Controllers/Api/BuildingController.php`, `app/Http/Controllers/Admin/BuildingController.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `resources/js/Pages/Admin/Buildings/Create.jsx`, `resources/js/Pages/Admin/Buildings/Edit.jsx`, `resources/js/Pages/Admin/Websites/Create.jsx`, `resources/js/lib/extractLogoColors.js`
  - Details: Reworked the earlier "website logo" step into a proper **building logo**. Added a `logo` column to buildings (migration + fillable + `getLogoAttribute` localhost-strip). Building Create/Edit pages get a "Building Logo" upload section (reuses `/api/buildings/upload-image` with new `image_type=logo`, which writes `$building->logo`). Admin BuildingController store/update validate `logo` and the edit() payload hydrates it. On **Websites → Create**, the buildings list now includes `logo`; picking a building auto-loads ITS logo, runs the in-browser canvas color extraction on it, and fills the (still-editable) Core/Button/Footer/Link pickers — the manual pickers from the prior version are kept. The building logo becomes the new site's `logo`/`logo_url`. If the building has no logo, an upload in the Create step saves it back to the building and re-detects; if branding is left untouched, a `transform()` strips `logo`/`logo_url`/`brand_colors.*` so the default-site inheritance still applies. extractLogoColors now sets `crossOrigin` for URL sources so building-logo URLs can be read from canvas. Migration ran locally (also applied two pre-existing pending building migrations); `npm run build` (client+SSR) and `php -l` all pass.

- **[FEATURE]** Added a Website Branding step to Admin > Websites > Create: a simple building logo upload that auto-detects a brand color palette in-browser, plus the same editable color pickers (Core/Button/Footer/Link) as the Edit page, expanded by default
  - Files changed: `resources/js/Pages/Admin/Websites/Create.jsx`, `resources/js/lib/extractLogoColors.js`
  - Details: New `resources/js/lib/extractLogoColors.js` — canvas-based dominant-color extraction (no new dependency); buckets logo pixels (4-bit/channel quantize, skips transparent/near-white/near-black), weights vivid colors, and maps the result into the full `brand_colors.*` palette with WCAG-readable text choices. Create.jsx now seeds the same defaults as Edit, shows a logo dropzone + preview, and on upload fills all color pickers ("Detecting colors…" → "Colors detected"). Each swatch stays editable. Branding fields are only submitted when the admin uploads a logo or edits a swatch — a `transform()` strips untouched `brand_colors.*` + `logo_file`, so the existing server-side inheritance of the default site's palette still applies for the untouched one-click flow. Submit switched to `forceFormData: true` for the multipart logo upload. Backend `WebsiteManagementController@store` already validated/handled `logo_file` + dotted `brand_colors.*` keys, so no backend change was needed. `npm run build` (client + SSR) passes.

## 2026-07-14

- **[REFACTOR]** Google Maps: drop browser→server key fallback so the IP-locked server key can never leak to the browser
  - Files changed: `config/repliers.php`, `.env.example`, `docs/GOOGLE_MAPS_KEYS_SETUP.md`
  - Details: `google_maps_browser_key` was `env('GOOGLE_MAPS_BROWSER_KEY') ?: env('GOOGLE_MAPS_API_KEY')`. Once the server key is IP-restricted to 16.59.7.138, that fallback silently ships the IP-locked key to the browser and breaks every map. Changed to `env('GOOGLE_MAPS_BROWSER_KEY', '')` (fail-safe: no browser key → map just doesn't render, key never leaks). BROWSER key is now REQUIRED. Added docs/GOOGLE_MAPS_KEYS_SETUP.md: two-key setup for pcdadmin.com — BROWSER key = API-restricted (Maps JS + Places + Geocoding) + quota cap, NO IP/referrer lock so customer custom domains auto-work; SERVER key = IP-restricted to 16.59.7.138 (Geocoding + Places). Includes local-dev caveat (server key won't work from localhost) and verification steps.

- **[FEATURE]** Separate browser vs server Google Maps API keys (fixes RefererNotAllowedMapError on custom domains)
  - Files changed: `config/repliers.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `app/Providers/AppServiceProvider.php`, `.env.example`
  - Details: One shared GOOGLE_MAPS_API_KEY was injected into the browser (Maps JS via app.blade.php + Inertia googleMapsApiKey) AND used server-side (Geocoding/Places). A referrer-restricted key breaks unlimited customer custom domains (RefererNotAllowedMapError); an IP-restricted key breaks the browser map (the map loads from the visitor's browser, not the server IP). Split into two: NEW config `google_maps_browser_key` = env('GOOGLE_MAPS_BROWSER_KEY') ?: GOOGLE_MAPS_API_KEY (fallback so nothing breaks until set); the two browser injection points now use it, while GeocodingService/GooglePlacesService/geocode cron keep using GOOGLE_MAPS_API_KEY. Console setup: browser key = API-restricted (Maps JS + Places) + daily quota cap + no referrer lock; server key = IP-restricted to 16.59.7.138. Set GOOGLE_MAPS_BROWSER_KEY in .env + config:cache after creating the browser key. php -l clean on all three.

- **[FIX]** Gemini AI content failing with "cut off (MAX_TOKENS)"
  - Files changed: `app/Services/GeminiAIService.php`
  - Details: Model gemini-flash-latest (Gemini 2.5 Flash) spends "thinking" tokens from the same maxOutputTokens budget, so generations (incl. the website home-content JSON: hero + overview + 6 FAQs) hit finishReason=MAX_TOKENS before emitting usable text. Fix: added generationConfig.thinkingConfig.thinkingBudget=0 in generateContent() so the full budget goes to the answer — covers all three call sites (SEO meta, building description, home content). Remote already had the multi-part concat + MAX_TOKENS detection; thinkingBudget was the missing piece. NOTE: local main was 5 commits behind remote — reset --hard to origin/main, re-applied the fix on the current code, committed (5f53d3a) and pushed. Local GEMINI_API_KEY is dead (Google 403 "leaked") so verify on production via the status page "Regenerate AI content" button. The 503 "high demand" is a transient Google-side error — just retry.

## 2026-07-10

- **[FIX]** Default agent photo follow-up: broken inherited paths replaced, existing rows healed by migration
  - Files changed: `app/Http/Controllers/Admin/WebsiteManagementController.php`, NEW `database/migrations/2026_07_10_130000_default_agent_profile_images.php`
  - Details: New websites still showed a broken agent image because the fallback chain inherited the default site's agentInfo INCLUDING its stale profile_image path — the creation-time default only kicked in when the value was empty, not when the file was missing. store() now also replaces local paths whose file doesn't exist (public_path check) with /assets/default-agent-profile.png. New migration heals EXISTING agent_infos rows on deploy: NULL/empty or dead local paths → the default photo (external http URLs untouched). Ran locally: website 1's NULL row now carries the default photo. DEPLOY: git pull + php artisan migrate on the server — that fixes the broken image on already-created sites like website 13 too.

- **[FEATURE]** Default agent profile photo on every new website
  - Files changed: `app/Http/Controllers/Admin/WebsiteManagementController.php`, NEW `public/assets/default-agent-profile.png`
  - Details: The photo from https://www.noburesidences.co/files/agent_profile_pics/1733862036.png (240KB, verified PNG) was downloaded and stored locally at public/assets/default-agent-profile.png (git-tracked, so production gets it via pull — no external hotlink dependency). In store(), after the upload / linked-building / default-site fallbacks, an empty profile_image now defaults to /assets/default-agent-profile.png — every newly created website's AgentInfo has a profile photo out of the box, editable later on Websites → Edit. Existing websites untouched (creation-time only). php -l clean.

- **[STYLE]** Websites Edit + Create: sticky bottom action bar (Save Changes / Create Website)
  - Files changed: `resources/js/Pages/Admin/Websites/{Edit,Create}.jsx`
  - Details: Same pattern as the Buildings forms (sticky bottom-0 z-10 border-t bg-white/95 backdrop-blur; works since body overflow-x was switched to clip). Edit: the Cancel/Save Changes row is now sticky at the viewport bottom on the long form. Create: the Create Website button row was moved OUT of the confirm-panel card (the card has overflow-hidden, which makes sticky inert inside it) into a sticky bar at the end of the form. npm run build green.

- **[FIX]** Buildings list: actions dropdown no longer clipped by the table's overflow container
  - Files changed: `resources/js/Pages/Admin/Buildings/Index.jsx`
  - Details: The 3-dot View/Edit/Delete menu was absolute-positioned inside the table's overflow-x-auto wrapper, so it got cut off at the table edge (z-index can't escape an overflow clip). The menu now renders position:fixed at viewport coordinates computed from the trigger button's getBoundingClientRect (top: rect.bottom+4, right-aligned to the button) with z-50 — it floats above everything outside the table. Any scroll (capture-phase, so the table's own horizontal scroll counts too) or resize closes the menu so it can never float detached from its row; outside-click and Escape close behavior unchanged. npm run build green.

- **[CHORE]** Building forms: removed Parking Spots and Locker Spots inputs (frontend only)
  - Files changed: `resources/js/Pages/Admin/Buildings/{Create,Edit}.jsx`
  - Details: Both number inputs removed from the Create AND Edit forms per user request — frontend only. The useForm keys stay (Create: empty defaults; Edit: initialized from the building row and submitted unchanged), so backend validation, existing DB values, and the AI-description payload are all unaffected. npm run build green.

- **[FIX]** Website AI content stuck on "pending" in production — job now runs without a queue worker
  - Files changed: `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Jobs/GenerateWebsiteAiContentJob.php` (docblock), `resources/js/Pages/Admin/Websites/Created.jsx`
  - Details: Website 12 (theatredistrictresidences.ca) sat on ai_content_status=pending forever because GenerateWebsiteAiContentJob was dispatched to the database queue and PRODUCTION RUNS NO QUEUE WORKER (and the schedule:run cron with its queue:work fallback isn't installed either) — the job never processed. Switched both dispatch points (store() + retryAiContent()) to dispatchAfterResponse(), the same worker-less pattern GeneratePropertyAiContentJob already uses successfully in production: the job runs right after the redirect response is sent. Created.jsx: the retry button now also appears while status is pending (after 2 auto-refresh polls) labelled "Run AI generation now" — clicking it resolves any site stuck from before this fix (e.g. website 12 after deploy); the stale "start the queue worker" hint reworded. NOTE: production's GEMINI_API_KEY is presumably the same dead (leaked) key — after deploy the run will complete but mark "failed" with the 403 until a fresh key is set in the server .env; template content stays live either way.

- **[FIX]** Building Show page: Maintenance Fee Range showed N/A despite having a value
  - Files changed: `resources/js/Pages/Admin/Buildings/Show.jsx`
  - Details: The Financial Details card read nonexistent columns `maintenance_fee_range_min`/`_max` (and `property_tax_range_min`/`_max` — those columns don't exist on buildings at all), so it always rendered N/A even when Edit showed the value. Now reads the real string column `building.maintenance_fee_range`; the dead Property Tax Range row was replaced with the real fee fields Parking Maintenance ($X/month) and Locker Maintenance ($X/month), plus Avg Price Per Sqft. npm run build green.

- **[FIX]** Live MLS fill: address ranges ("8-30 Widmer St") now match; fallback building description rewritten to professional SEO standard
  - Files changed: `app/Models/Building.php`, `app/Services/GeminiAIService.php`
  - Details: User's payload {address: "8-30 Widmer St, Toronto"} returned facts:null because mlsFactsForAddress() only parsed single street numbers. Now a leading "N-M" range parses (suffix/city stripping reused via parseStreetAddress on "N rest") and matching accepts ANY numeric street number within [N,M] — verified live: 8-30 Widmer → 21 active listings, corp TSCC 3079, First Service Residential, $0.71-$1.04/sqft, $583/month; single-address path regression-tested (15 Mercer unchanged). Second complaint "description not professional": root cause is STILL the dead GEMINI_API_KEY (fresh 403 "reported as leaked" in the log at 14:42) — every Generate with AI returns the hardcoded fallback, and that fallback contained the exact clichés the prompt bans ("perfect blend of luxury..."). getFallbackBuildingDescription() rewritten to the same standard the AI prompt demands: 3 short paragraphs (what/where with "{name} condos" phrase → facts-only living section (storeys/units/year, amenities, managed-by + corp) → price/fee numbers + explore-listings CTA), builds only from provided facts, no invented details, no clichés. PENDING: real AI copy still needs a fresh GEMINI_API_KEY in .env (aistudio.google.com) + config:clear.

- **[FEATURE]** Live MLS auto-fill on address select in the building form (no save needed)
  - Files changed: `app/Models/Building.php`, `app/Http/Controllers/Api/BuildingController.php`, `routes/web.php`, `resources/js/Pages/Admin/Buildings/{Create,Edit}.jsx`
  - Details: Follow-up to the after-save MLS auto-fill — the user wanted the fields to fill the moment the address is selected, not after saving. New public static Building::mlsFactsForAddress(?address, ?city): one cached Repliers query by parsed street name, exact street-number match, returns {corp_number, management_name, year_built (real years only), parking_maintenance_fee, maintenance_fee_range ($/sqft derived), typical_monthly_fee, active_listing_count} or null (unparseable address / zero matches). New authed endpoint POST /api/buildings/mls-facts → Api\BuildingController::mlsFacts(). enrichBuildingDataFromMls() (AI description grounding) refactored to reuse the same model method — one source of truth. Frontend (Create + Edit): fetchMlsFactsForAddress() fires from the Google Places selection callback AND address onBlur (deduped via lastMlsAddressRef; Edit seeds the ref with the current address so merely tabbing through doesn't refetch); fills ONLY empty fields (corp, management, year built, parking fee, maintenance fee range) and shows a green notice "MLS match: N active listings — empty fields auto-filled" under the address input; silent on failure. Address ranges ("8-30 Widmer") don't parse to a single number so live fill skips them — the after-save refresh job still covers those. Verified: tinker (15 Mercer → TSCC 3043 / Forest Hill Kipling / $0.71-$0.83 per sq ft / $625/month / 14 listings; no-match and unparseable → null), php -l clean, route registered, npm run build green.

- **[FEATURE]** MLS auto-fill for building condo facts + SEO-stronger AI building description
  - Files changed: `app/Models/Building.php`, `app/Http/Controllers/Api/BuildingController.php`, `resources/js/Pages/Admin/Buildings/{Create,Edit}.jsx`
  - Details: (1) Building::refreshPriceRangeFromMls() (already dispatched via RefreshBuildingPriceRangeJob::dispatchAfterResponse on every building create/save) now also collects condo facts from matched listings via new public static Building::extractCondoFactsFromListing(): corp_number ← condominium.condoCorp+condoCorpNum ("TSCC 3043"), management_name ← condominium.propertyMgr, parking_maintenance_fee ← details.parkCostMonthly (sparsely populated but picked up when present), year_built ← details.yearBuilt ONLY when a real 4-digit year (TRREB usually sends age bands like "11-15"/"New" — those are skipped, field left blank). Fills use the MODE across listings and ONLY blank columns — admin-entered values are never overwritten (verified). Also fixed the existing maintenance-fee derivation to fall back to condominium.fees.maintenance when details.maintenanceFee is null (real API showed boards populating only the condo block). Fields MLS does NOT carry are deliberately left blank per user spec: locker_maintenance_fee, building-total parking_spots/locker_spots (MLS is per-unit), date_registered — verified against live Repliers responses (50-listing sample). No migration/routes/jobs needed. (2) AI description SEO upgrade in Api\BuildingController: new private enrichBuildingDataFromMls() grounds "Generate with AI" in live MLS facts even on an unsaved building (one cached searchListings call by parsed street address; merges corp/mgmt/year/typical monthly fee ($avg/month)/active-listing count; typed values always win; try/catch → typed-data-only on failure). buildAiDescriptionPrompt(): 200-260 words in 3 structured paragraphs (what/where → living there+transit → the numbers+CTA), expanded keyword set ("{name} for sale", "{name} floor plans and prices", "{name} maintenance fees", "condos for rent in {area}" — once each, natural only), required neighbourhood+transit sentence and concrete-facts sentence, new facts in the prompt (corp, sqft_range, avg $/sqft, typical monthly fee, active listing count, nearby transit — flattened from the {name,type,distance} array shape). Create/Edit payloads send corp_number + nearby_transit (+ sqft_range/avg_price_per_sqft on Edit). Forms: "Leave blank to auto-fill from live MLS listings" hints under Year Built, Management Company, Corp Number, Parking Maintenance. Verified live on 15 Mercer St: corp TSCC 3043 + mgmt Forest Hill Kipling filled, year_built correctly left NULL (age bands only), fee range $0.71-$0.83/sqft, manual corp value survived a re-run; enrichment reflection-test returned corp/mgmt/$625/month/14 active listings, typed mgmt won, no-address passthrough clean. php -l + npm run build green. NOTE: Gemini API key is still dead (403 leaked) — AI descriptions return fallback text until a new GEMINI_API_KEY is set.

- **[CHORE]** Building create form: removed Website URL, Virtual Tour URL and the Status & Settings section (frontend only)
  - Files changed: `resources/js/Pages/Admin/Buildings/Create.jsx`
  - Details: Removed the Website Links block (website_url + virtual_tour_url inputs) and the whole Status & Settings card (Visibility Status select + Featured Building checkbox) from the CREATE form only — the Edit form keeps all of them. Frontend-only per user request: the useForm keys stay with their defaults (status 'active', is_featured false, empty URLs) so store() behavior is unchanged and new buildings publish as Active. The Construction Stage hint that referenced "Visibility Status under Status & Settings" was reworded since that section no longer exists on Create. Verified: npm run build green. Committed + pushed to main in 31c676d together with the bulk-delete and admin-cleanup work.

- **[FEATURE]** Bulk delete on the admin buildings list (checkbox multi-select)
  - Files changed: `app/Http/Controllers/Admin/BuildingController.php`, `routes/web.php`, `resources/js/Pages/Admin/Buildings/Index.jsx`
  - Details: Buildings list at /admin/buildings gained a checkbox column (select-all in the header covers the currently filtered rows), a blue toolbar that appears when rows are selected ("N buildings selected" + red Delete selected + Clear), and a ConfirmDialog before deleting. New POST admin/buildings/bulk-delete → BuildingController::bulkDestroy() validates `buildings[]` (exists:buildings,id) and soft-deletes per model (same behavior as single destroy(); iterating models keeps future delete hooks working). Returns redirect back with a success flash (deliberately NOT the Contacts JSON-response anti-pattern). Selection auto-clears when search/filters change so a bulk delete can never touch rows the admin can't see; selection spans the current page only (server-side pagination, 10/page). Selected rows highlight blue; empty-state colSpan bumped 6→7. Scope confirmed with user: delete only (no bulk feature/status actions). Verified: php -l clean, route:list shows bulk-delete, npm run build green, tinker smoke test (2 throwaway buildings whereIn→delete → both soft-deleted/trashed, then force-deleted for cleanup). Websites linked via homepage_building_id keep working — the building just stops resolving (identical to existing single delete).

- **[CHORE]** Admin cleanup batch: homepage toggle + description removed, pages route gone, Globals settings tab, RE/MAX brokerage, preview links use the real domain
  - Files changed: `resources/js/Pages/Admin/Websites/{Edit,Show,Index,Created}.jsx`, `resources/js/Pages/Admin/Settings/Index.jsx`, `resources/js/Pages/DeveloperDetail.jsx`, `app/Http/Controllers/Admin/{WebsiteManagementController,SettingsController}.php`, `routes/web.php`, NEW `database/migrations/2026_07_10_110000_replace_property_ca_brokerage_with_remax.php`; DELETED `resources/js/Pages/Admin/Websites/Pages.jsx`
  - Details: (1) Edit page: removed the "Use Building Page as Homepage" checkbox (form key dropped too — update() only touches submitted keys, so the DB flag stays false) and the Description textarea; the section was retitled "Linked Building" and keeps the building selector (the change-building feature). Create page already had neither after the one-click rewrite. (2) Removed the Manage Pages feature: route admin.websites.pages deleted, controller pages() method deleted, Pages.jsx deleted, "Manage Pages" dropdown item removed from Websites Index, and the "Website Pages" card removed from the Show page. Home page editing remains via Edit Home Page. (3) Settings: global_tracking_scripts moved from the General tab into its own "Globals" tab (schema group 'globals' + new GlobalsTab form) — global tags like the Follow Up Boss pixel default onto every public website; per-site snippets still exist on Website Edit and both render together in app.blade.php (already wired via AppServiceProvider/Setting). (4) Brokerage: new migration replaced every stored "%Property.ca%" in buildings.agent_brokerage and agent_infos.brokerage with "RE/MAX" (5 building rows locally) and changed the buildings.agent_brokerage column default from 'Property.ca Inc., Brokerage' to 'RE/MAX'; code fallbacks updated in DeveloperDetail.jsx (+ the BuildingGallery .backup). (5) Preview Website links now open https://{domain} when a domain is set (Websites Index getPreviewUrl; Created page only once the hostname is live since DNS is usually still pending there), falling back to /?website={slug}. Verified: migration ran, DB scan shows zero Property.ca left (buildings/agent_infos/pages/websites), php -l clean, npm run build green, route:list confirms pages route gone. DEPLOY: git pull + php artisan migrate on the server (the brokerage replacement runs there via the migration).

- **[FIX+FEATURE]** Client feedback batch: live custom domains update in real time (Cloudflare edge purge), building change now rebuilds website content, Condos.ca Developers API integration (auto-match / search / import)
  - Files changed: NEW `app/Jobs/PurgeCloudflareCacheJob.php`, `app/Services/DevelopersApiService.php`, `app/Http/Controllers/Admin/DeveloperApiController.php`, `resources/js/Components/Admin/DeveloperApiSearch.jsx`, `resources/js/utils/developersApi.js`; UPDATED `app/Services/CloudflareService.php`, `app/Models/Website.php`, `app/Models/WebsitePage.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `resources/js/Pages/Admin/Websites/Edit.jsx`, `resources/js/Pages/Admin/Buildings/{Create,Edit}.jsx`, `resources/js/Components/Admin/DeveloperModal.jsx`, `resources/js/Pages/Admin/Developers/Index.jsx`, `routes/web.php`, `config/services.php`, `.env`, `.env.example`
  - Details: (1) STALE LIVE SITES: content/settings changes never showed on Cloudflare-served custom domains in real time because NOTHING purged Cloudflare's edge cache after publish (the SaaS integration only handled hostnames/SSL). New CloudflareService::purgeDomainCache() (purge_cache by hosts incl. www, falls back to purge-by-URL for plans without hosts purge) + PurgeCloudflareCacheJob dispatched via dispatchAfterResponse (no queue-worker dependency, zero added request latency). Wired as model hooks: Website::saved/deleted purge the old+new domain (Cloudflare/AI bookkeeping columns are excluded via CACHE_IRRELEVANT_FIELDS so the 5-min status sync job can't purge-spam) and WebsitePage::saved/deleted purge the parent site's domain — every publish path (home page editor, settings, branding, page CRUD) is covered without touching controllers. (2) BUILDING CHANGE DID NOTHING: the Websites Edit building selector was hidden behind the "Use Building Page as Homepage" checkbox and changing homepage_building_id only updated the FK — the stored home-page JSON (hero, key facts, FAQ, footer, images generated at create time) kept the OLD building's data. The selector is now always visible (with an amber "saving rebuilds homepage content" warning when it differs from the saved value), and update() calls new regenerateHomePageForBuildingChange(): when the building actually changed it rebuilds the home page via WebsitePage::getDefaultHomeContentForBuilding($newBuilding) (same generator as the create flow) and reports it in the success flash. (3) DEVELOPERS API (DEVELOPERS-API-DOCS.md): new DevelopersApiService (X-API-Key client, 10-min cache, graceful when unconfigured) with search / getDeveloper / matchDeveloperByBuildingName (exact-normalized building-name match, else single-hit; resolves the developer slug via the search's developers half) / importDeveloper (existing local developer matched by slug then case-insensitive name is REUSED and only its empty fields are filled — local data always wins; logo downloaded into storage/developers so it renders like uploads; soft-deleted slug-holders are restored to avoid the unique-slug collision). Three admin proxy endpoints (key stays server-side): GET api/developers-api/search, POST .../match-building (match + import + return local {id,name,type,slug}), POST .../import. Frontend: reusable DeveloperApiSearch (350ms debounce, stale-response guard, logo thumbs + building counts, "not configured" notice) mounted under the Developer dropdown on Buildings Create/Edit; building-name onBlur auto-matches and auto-selects the developer (never overrides an existing pick); DeveloperModal ("+ New" from building forms) and the Developers tab Add-modal both got a directory-first search — picking a result imports in one click, manual entry stays as fallback. Env: DEVELOPERS_API_URL (defaulted) + DEVELOPERS_API_KEY in .env/.env.example + config/services.php. Verified: php -l clean on all, route:list shows the 3 routes, npm run build green, tinker smoke tests (unconfigured API degrades to empty results; save hooks fire; purge returns "not configured" cleanly). PENDING FOR LIVE: paste the real DEVELOPERS_API_KEY into .env + `php artisan config:clear`; the Cloudflare purge uses the existing CLOUDFLARE_API_TOKEN — it needs the Cache Purge permission on the zone (Zone.Cache Purge: Purge) or purges will log a failure.

- **[FEATURE]** One-click website creation with background AI auto-provisioning
  - Files changed: NEW `app/Jobs/GenerateWebsiteAiContentJob.php`, `database/migrations/2026_07_10_100000_add_ai_content_status_to_websites_table.php`; REWRITTEN `resources/js/Pages/Admin/Websites/Create.jsx` (~1200 → ~400 lines, one screen); UPDATED `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Services/GeminiAIService.php`, `app/Models/Website.php`, `resources/js/Pages/Admin/Websites/Created.jsx`, `routes/web.php`
  - Details: Creation flow is now pick building → (optional domain) → Create. The old 2-step wizard's advanced form (colors, logo/favicon, contact, agent, social, SEO, timezone) is gone from Create — all of it inherits server-side in store() from the default website (brand_colors/fonts inheritance extended to contact_info, social_media, logo, logo_url, favicon_url, timezone) and the building's agent (AgentInfo fallback: building agent → default site agentInfo), everything editable later on Edit/EditHomePage. Website name defaults to the building name; instant template SEO meta is written synchronously so sites are usable even without a queue worker. NEW GenerateWebsiteAiContentJob (queued, tries=1, timeout=120, dispatched from store() when a building is linked; websites.ai_content_status pending/completed/failed + ai_content_error + ai_content_generated_at): (a) generates + saves the building description when missing, (b) Gemini SEO meta → meta_* columns, (c) NEW GeminiAIService::generateWebsiteHomeContent() one JSON call (hero welcome_text/main_heading/subheading, About overview, 6 building-specific FAQs, 3000 max tokens — generateContent() gained a maxOutputTokens param + 60s timeout for big generations) patched into the home page via data_set on ONLY those keys (fresh re-read + updateOrCreate on the (website_id,page_type) unique index, so carousel/footer/key-facts/admin edits survive; failed() handler prevents stuck "pending"). Status verdict = homepage copy success ONLY (SEO/description degrade to fallbacks internally and must not mask API failure). Created.jsx: new "AI content" row, auto-refresh extended to AI pending (even without a domain), "queue worker running?" hint after 4 polls, Regenerate button on failure → new POST {website}/retry-ai-content (force bypasses the completed-guard). create() props trimmed (defaults now server-side) + buildingIdsWithWebsite for a "Has a website" badge (duplicates allowed, slug auto-suffixes); removed aiGenerateSeo()/ai-generate-seo route (superseded by the job). Also fixed nearby_transit/amenities array-of-objects flattening in the new prompt builder, and ported buildAiDescriptionPrompt to a model-based public variant buildBuildingDescriptionPromptFromModel(). Verified: migration ran, php -l clean, npm run build green, route:list correct; live job smoke test on a throwaway site confirmed status flow, JSON patch preserving carousel settings, and the failed-path (current GEMINI_API_KEY is dead — Google 403 "reported as leaked" — so AI content will fall back to templates until a new key is set in .env). NOTE: production needs a queue worker (or the existing queue:work --stop-when-empty scheduler fallback) for AI content + Cloudflare polling.

- **[FEATURE]** Registration confirmation email via Resend — new WelcomeNewUser notification wired into email-form and Google OAuth signups
  - Files changed: NEW `app/Notifications/WelcomeNewUser.php`; UPDATED `app/Http/Controllers/Auth/RegisteredUserController.php`, `app/Http/Controllers/Auth/GoogleAuthController.php` (both new-user branches), `.env`, `.env.example`, `composer.json`, `composer.lock`
  - Details: Every new registrant now gets a branded "registration confirmed" welcome email, sent to the registrant themselves (counterpart to the admin-facing NewUserRegistered). Same established pattern: synchronous notification (no ShouldQueue — production runs no queue worker), guarded static `WelcomeNewUser::send()` with try/catch + Log::warning so mail failure can never break signup, site name from default Website falling back to app.name. Wired next to the existing NewUserRegistered::notifyAdmins() calls in all three create-points (email form + both Google OAuth callbacks); tenant name resolved once and shared. Existing-user Google logins do NOT trigger it. Infrastructure: installed resend/resend-php v1.5.1 (required by Laravel 12's built-in `resend` transport already present in config/mail.php), switched .env MAIL_MAILER=log → resend and added empty RESEND_API_KEY line (key to be pasted by user; config/services.php already reads RESEND_API_KEY). Email signups also still get the stock verification email — which now actually delivers instead of going to the log. Verified: php -l clean, config resolves mailer=resend, Resend SDK classes autoload. PENDING: paste real RESEND_API_KEY into .env and set MAIL_FROM_ADDRESS to an address on the Resend-verified domain (currently hello@example.com — Resend will reject sends until changed), then `php artisan config:clear` and live-test.

- **[FIX]** building.wpbun.xyz still 404 after env advice: admin-host resolution no longer depends on a correct server .env — building.wpbun.xyz baked into the default, APP_URL host always admin
  - Files changed: `app/Services/Tenancy/TenantResolver.php`, `config/tenancy.php`, `.env.example`
  - Details: Follow-up to the earlier ADMIN_HOST fix — the site still 404'd because the server's .env was never updated (and nobu.wpbun.xyz now 301s to an unrelated site, confirming the project only lives on building.wpbun.xyz). The 404 originates from TenantResolver (used by WebsiteController::getCurrentWebsite and the HandleInertiaRequests middleware), not from nginx. Made the resolution deploy-proof: (1) `config/tenancy.php` default is now `building.wpbun.xyz,nobu.wpbun.xyz` — a plain git pull fixes the site even when ADMIN_HOST is absent from the server .env; (2) `TenantResolver::adminHosts()` additionally always includes APP_URL's host (the app's canonical URL can never be a tenant), so even a stale explicit ADMIN_HOST can't 404 the main site if APP_URL is right; (3) `.env.example` updated to the comma list. Verified via tinker: fresh-default case and stale-ADMIN_HOST + correct-APP_URL case both resolve building.wpbun.xyz as admin, stranger hosts still 404. DEPLOY: git pull on the server, then `php artisan config:cache && php artisan cache:clear` (resolver caches host misses 60s). Emergency switch if ever needed: TENANCY_UNKNOWN_HOST=default serves the main site on any host.

- **[FEATURE]** Ploi integration removed entirely — replaced with Cloudflare for SaaS Custom Hostnames
  - Files changed: NEW `app/Services/CloudflareService.php`, `app/Services/HealthCheckService.php`, `app/Jobs/SyncCustomHostnameStatusJob.php`, `app/Console/Commands/{CloudflareSyncHostnames,CloudflareHealth}.php`, `database/migrations/2026_07_10_150000_replace_ploi_columns_with_cloudflare.php`, `docs/CLOUDFLARE-DOMAINS.md`; REWRITTEN `resources/js/Pages/Admin/Websites/Created.jsx`; UPDATED `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Models/Website.php`, `config/services.php`, `config/tenancy.php`, `routes/{web,console}.php`, `resources/js/Pages/Admin/Websites/{Create,Edit,Index}.jsx`, `resources/js/Pages/Admin/Settings/Index.jsx`, `app/Http/Controllers/Admin/SettingsController.php`, `.env`, `.env.example`; DELETED `app/Services/PloiService.php`, `app/Jobs/RequestPloiSslJob.php`, `app/Console/Commands/{PloiAuditAliases,PloiWatchDns,PloiReissueSsl,PloiCleanup,PloiDiagnose}.php`
  - Details: Architecture swap: customer domains no longer become nginx aliases with certbot SANs — they are Cloudflare Custom Hostnames on the SaaS zone. New flow: save domain -> CloudflareService::createCustomHostname (POST /zones/{zone}/custom_hostnames, ssl http/dv, idempotent, admin host refused) -> cloudflare_hostname_id stored -> status page shows the ONE required record (CNAME domain -> CLOUDFLARE_CNAME_TARGET, apex via provider flattening) -> SyncCustomHostnameStatusJob polls with backoff and cloudflare:sync-hostnames (every 5 min) covers the long tail -> hostname+SSL active => website live. Structural safety win: every hostname has its OWN edge certificate, so add/retry/delete of one domain can never affect the admin domain or another tenant (the entire class of shared-cert-file outages from the Ploi/certbot era is impossible). TenantResolver untouched (requirement), unknown hosts still 404, domain uniqueness kept. websites table: ploi_* columns dropped, cloudflare_{hostname_id(indexed),status,ssl_status,last_error,active_at} added; model fillable/casts swapped. Controller: provisionCustomHostname replaces runProvisioning; created() rebuilt on HealthCheckService (hostname exists / ssl active / verification / ownership) with live-state sync; three retry routes collapsed into POST {website}/retry-hostname; update() deletes the old hostname + registers the new on domain change; destroy() deletes the hostname. Frontend: Create wizard shows single-CNAME instructions (A-record table + orange-cloud warning gone), Edit's Domain & Hosting explains the CNAME + links to status page (connect dialog removed), Created.jsx fully rewritten (CNAME card with copy button, provisioning report, live Cloudflare state, retry + refresh, 15s auto-poll until live), Index delete message updated. Schedules: ploi:watch-dns/ploi:audit -> cloudflare:sync-hostnames (5 min) + cloudflare:health (weekly). Env: all PLOI_* removed from .env/.env.example; CLOUDFLARE_API_TOKEN/ZONE_ID/CNAME_TARGET added. Old 2026_05_17 ploi migration file kept for history (its columns are dropped by the new migration). Verified: migration ran, php -l clean on all, route:list shows only retry-hostname, artisan list shows only cloudflare:* (no ploi:*), schedule:list correct, both commands smoke-run, zero ploi references left outside migration history + Ploi-panel cron comments. DEPLOY: git pull; set CLOUDFLARE_API_TOKEN/ZONE_ID/CNAME_TARGET and remove PLOI_* in live .env; php artisan migrate && config:cache; enable Cloudflare for SaaS on the zone + set fallback origin; re-register existing domains via cloudflare:sync-hostnames; customers switch A records to the CNAME; old Ploi aliases/certs can be cleaned manually in the Ploi panel.

- **[FIX]** 404 on new domain building.wpbun.xyz: tenancy admin host is env-driven and was still nobu.wpbun.xyz — ADMIN_HOST now accepts a comma-separated list for domain moves
  - Files changed: `app/Services/Tenancy/TenantResolver.php`, `app/Services/CloudflareService.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Console/Commands/CloudflareHealth.php`, `config/tenancy.php`
  - Details: User moved the project live to building.wpbun.xyz and got Laravel's 404 (confirmed via curl: the 404 page is Laravel's, so DNS/nginx/SSL reach the app fine). Root cause: TenantResolver treats any host that is not `config('tenancy.admin_host')` (env ADMIN_HOST, default nobu.wpbun.xyz) and matches no active tenant website domain as unknown → `tenancy.unknown_host = '404'` aborts. The server's .env still had the old/unset ADMIN_HOST. Code update: new `TenantResolver::adminHosts()` parses ADMIN_HOST as a comma-separated, normalized list; `isAdminHost()`, `CloudflareService::isAdminHost()`, the domain-reserved validation rule in WebsiteManagementController and the cloudflare:health header all use the list — so `ADMIN_HOST=building.wpbun.xyz,nobu.wpbun.xyz` keeps the panel live on both domains during a move, and no tenant can ever claim either. Server fix required (can't be done from here): set ADMIN_HOST + APP_URL in the site's .env, then `php artisan config:cache && php artisan cache:clear` (resolver caches unknown-host misses for 60s). Verified via tinker: list parsing, www./case normalization, and stranger-host rejection all correct.

- **[FEATURE]** Admin feedback batch: hero description summary, Places autofill, SEO AI prompt + description below details, developer modal, MLS maintenance fees, sticky action bar fix, queued CSV import, scheduler queue fallback
  - Files changed: `resources/css/app.css`, `resources/js/Website/Sections/Home/HeroSection.jsx`, `app/Models/WebsitePage.php`, `resources/js/hooks/useGooglePlacesAutocomplete.js` (new), `resources/js/Pages/Admin/Buildings/Create.jsx`, `resources/js/Pages/Admin/Buildings/Edit.jsx`, `app/Http/Controllers/Api/BuildingController.php`, `resources/js/Components/Admin/DeveloperModal.jsx` (new), `resources/js/Components/Admin/QuickCreateSelect.jsx`, `app/Http/Controllers/Admin/DeveloperController.php`, `resources/js/Pages/Admin/Developers/Index.jsx`, `app/Models/Building.php`, `app/Services/BuildingCsvImportService.php`, `app/Jobs/ProcessBuildingImportChunkJob.php` (new), `app/Http/Controllers/Admin/BuildingImportController.php`, `resources/js/Pages/Admin/Buildings/Import.jsx`, `routes/web.php`, `routes/console.php`
  - Details: (1) Hero subheading: HeroSection now prefers a ~180-char summary of the building's own description over the auto-seeded "Whether buying or renting..." tagline (a custom admin subheading still wins); getDefaultHomeContentForBuilding seeds new sites from the description too. (2) Google Places autofill: new useGooglePlacesAutocomplete hook (Maps JS + places lib already loaded globally) attached to the Address input on Building Create/Edit — picking a suggestion fills postal code, city, province (short_name matches the select's ON/BC codes), country, lat/lng. (3) AI description: prompt rewritten for SEO (building name + address as primary keyword, "{name} condos" / "condos for sale in {area}" phrases, no invented facts, 150-220 words, anti-cliche list); payload enriched with neighbourhood/sub-neighbourhood/development status/parking/lockers, and fixed Create.jsx sending undefined developer_name (now resolved from developerOptions). Description field moved out of Basic Information into its own card AFTER Building Details so generation uses everything typed above. (4) Developer quick-add on building forms replaced with the full Add Developer modal (name/type/email/phone/website/year/description/LOGO upload) — new DeveloperModal posts FormData to the extended quickStore endpoint (now validates + stores all profile fields and the logo); QuickCreateSelect gained onRequestCreate to delegate "+ New". (5) Developers tab modal cleanup: removed SEO Meta Title/Description (auto-generated at render time in WebsiteController via existing fallbacks), Projects Completed/Under Construction/Upcoming, Expertise Highlights, Awards; logo kept; controller rules trimmed (existing DB values untouched since fields are never submitted). (6) Maintenance Fee Range now auto-fills from live MLS: refreshPriceRangeFromMls derives fee-per-sqft from Repliers details.maintenanceFee / sqft midpoint and writes "$X.XX - $Y.YY per sq ft" ONLY when the field is blank (manual entry always wins); form hint says leave blank to auto-fill. Parking/locker fees are not in MLS data — stay manual. (7) Sticky action bar root cause: `body { overflow-x: hidden }` in app.css made body a scroll container which silently disables position:sticky for ALL descendants (the bars already had sticky bottom-0); body switched to overflow-x: clip (html keeps hidden). (8) CSV import is now queued: run() validates mapping, parses totals, seeds cache progress and dispatches ProcessBuildingImportChunkJob (25 rows/chunk, 10s delay between chunks, self-chaining, cache-tracked, file deleted at end, failed() handler); new GET import/progress/{token} endpoint; Import wizard gained an "Importing" step with progress bar + live counts polling every 2.5s (page can be closed, import continues). (9) Scheduler: queue:work --stop-when-empty every minute as a fallback worker (safe next to a daemon; makes the database queue drain with only the schedule:run cron), plus buildings:refresh-mls-stats daily at 05:30. Ploi API is IP-allowlisted so server state couldn't be verified from here — the one required server cron is `* * * * * php artisan schedule:run` (Ploi panel → server → Scheduler). Verified: php -l clean on all changed PHP, route:list shows the new progress route, schedule:list shows both new entries, npm run build green.

- **[FIX]** THE actual SSL-down mechanism: cert batches without the primary domain overwrite the shared cert file — admin host now in EVERY issuance; new ploi:cleanup one-shot heal command
  - Files changed: `app/Services/PloiService.php`, `app/Console/Commands/PloiCleanup.php` (new)
  - Details: Researched why connect/update/delete kept killing the MAIN site's SSL even after cert deletion was removed. Mechanism: Ploi runs certbot with --cert-name = the site's PRIMARY domain, so the newest issuance REPLACES the single live cert file nginx serves for every domain on the site (the nginx include /etc/nginx/ssl/nobu.wpbun.xyz points at it). requestSsl's SAN-union pulled in "already covered" domains from the existing cert list — but NEVER guaranteed the primary (nobu.wpbun.xyz) itself was in the batch. Any issuance for a tenant domain where the current cert list didn't include the admin host produced a cert covering ONLY the tenant -> shared file overwritten -> main site instantly serves a cert without its own name -> ERR_CERT. FIX: requestSsl now unconditionally puts the admin host FIRST in every issuance batch (+ www.admin when it resolves) — every cert is a superset of the primary; if a batch fails, certbot leaves the existing file untouched (fails safe). This covers all paths: Connect button, retry, watch-dns, reissue. NEW `php artisan ploi:cleanup [--keep-orphans]` one-shot heal for the current mess: (1) dedupe duplicate alias rows (the triple zz-test-tower in server_name), (2) remove ORPHAN aliases belonging to no active website (admin host always excluded/protected), (3) force-reissue the superset certificate restoring main-site SSL, (4) re-sync every active website's persisted ssl_status against actual cert coverage (covered -> issued, uncovered -> waiting_dns for the 5-min watcher). Reminder for live: the recurring duplicates in the screenshot also indicate the server hasn't pulled the dedupe/idempotency commits — cleanup + git pull together fix it permanently.

- **[FIX]** PRODUCTION INCIDENT: cert deletion in removeDomainFromPloi took the main site's SSL down — never delete certificates; add ploi:reissue recovery command
  - Files changed: `app/Services/PloiService.php`, `app/Console/Commands/PloiReissueSsl.php` (new)
  - Details: Deleting a test website (or re-connecting a domain) killed SSL on the MAIN site (nobu.wpbun.xyz). Root cause: the domain-lifecycle change earlier today deleted certificates "dedicated" to the removed domain — but Ploi issues EVERY cert under the site primary's cert-name, so all domains on the site are served from ONE live cert file; deleting any certificate entry removes that file and takes the primary and every alias offline (exactly the hazard the old destroy() comment warned about). FIX: removeDomainFromPloi no longer touches certificates EVER (alias rows only; removed domains drop out of the next issuance via the known-domains union filter); deleteCertificate() removed entirely with a NOTE comment explaining why it must not exist; requestSsl() gained a $force flag that bypasses the already-covered idempotency check. New `php artisan ploi:reissue` recovery command: force-issues a fresh certificate for the admin host (union pulls in every still-covered, resolving tenant domain so the new cert is a superset and restores the shared file nginx serves), then flips any active tenant domain NOT covered by the fresh cert to waiting_dns so ploi:watch-dns re-provisions it within 5 minutes. RECOVERY ON LIVE: git pull, php artisan ploi:reissue (restores main-site SSL), then watch ploi:watch-dns pick up any tenants. If nginx itself is down (config pointing at missing cert files), restart nginx from the Ploi panel after reissue.

- **[FEATURE]** SSL waits for DNS automatically: waiting_dns status + ploi:watch-dns scheduler (no more manual Retry after DNS propagates)
  - Files changed: `app/Console/Commands/PloiWatchDns.php` (new), `app/Jobs/RequestPloiSslJob.php`, `app/Services/PloiService.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `routes/console.php`, `resources/js/Pages/Admin/Websites/Created.jsx`
  - Details: Reported flaw: "SSL kabhi kabhi kaam nahi karta" — root cause: when a domain's DNS wasn't pointing yet, RequestPloiSslJob detected the mismatch and marked ploi_ssl_status=FAILED permanently; nothing ever retried after DNS propagated, so the admin had to notice and press Retry SSL manually. New design: (1) PloiService::domainPointsToServer() (public tri-state DNS check) + shared PloiService::isDnsMismatchMessage() (covers our pre-flight wording and Ploi/LE messages; replaces the job's private copy); (2) the SSL job now checks DNS BEFORE attempting issuance — if not pointing, it parks the website in the new `waiting_dns` status (no LE rate-limit burn) and exits; DNS-mismatch failures from Ploi also park as waiting_dns instead of failed; (3) retrySsl controller path maps DNS-mismatch to waiting_dns too, with a "will retry automatically" note; (4) new `ploi:watch-dns` command scheduled everyFiveMinutes (withoutOverlapping, logs to storage/logs/ploi-watch-dns.log): scans active websites with ssl_status in [waiting_dns, pending], checks DNS, and the moment the A record points at the server runs the FULL provisioning automatically — addAlias (idempotent+dedupe) then requestSsl (idempotent already-covered check), updating statuses; DNS flap between our check and LE's re-parks as waiting_dns; (5) ploi:audit scheduled weekly (report-only); (6) Created/status page: "Waiting for DNS" amber badge with explainer ("nothing to do besides updating DNS"), and waiting_dns keeps the 15s auto-poll alive so the page flips to Issued on its own. Flow now: save domain -> point DNS whenever -> within 5 minutes alias+SSL provision themselves. schedule:list verified (watcher every 5 min); watcher smoke-run OK. Live needs the cron scheduler running (php artisan schedule:run every minute — already required by saved-search alerts).

- **[FEATURE]** Domain lifecycle: removing/changing a website's domain now detaches it from Ploi (nginx + dedicated SSL), admin host protected
  - Files changed: `app/Services/PloiService.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`
  - Details: Previously only website DELETE removed the Ploi alias; editing a website's domain (change or clear) left the old domain in nginx's server_name and its certs on Ploi forever. New `PloiService::removeDomainFromPloi($domain)`: refuses the reserved admin host outright (verified: apex/www/uppercase all refused — nobu.wpbun.xyz can never be detached), deletes EVERY alias row for apex+www (handles duplicate rows via new deleteAliasById), and deletes certificates DEDICATED to that domain (SAN set subset of {apex, www}) via new deleteCertificate() — shared SAN certs are left alone since deleting one would break other domains. Wired into: (a) update() — when domain changes/clears, old domain is detached, ploi_alias/ssl_status reset to pending (new domain) or not_required (cleared), flash message reports cleanup + reminds to press Connect for the new domain; (b) destroy() — now uses the same helper (previously alias-only, now dedicated certs too); replaced the aliasVariants helper. Also requestSsl's SAN-union now filters to KNOWN domains only (admin host + current website domains, apex/www) so a removed/replaced domain can never ride along into future certificate issuances (previously it stayed in every renewal batch as long as its DNS pointed at the server).

- **[FIX]** Ploi duplicate alias root cause: cert issuance auto-adds SAN aliases — made requestSsl idempotent + auto-dedupe
  - Files changed: `app/Services/PloiService.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Console/Commands/PloiAuditAliases.php`
  - Details: User confirmed re-pressing "Connect domain to Ploi" duplicates the domain in nginx server_name AND adds another certificate file. Root cause is NOT our addAlias (its duplicate pre-check works): Ploi itself auto-creates an alias row per SAN subject after every successful certificate issuance without checking for an existing row, and every re-run also issued a brand-new certificate. Fixes: (1) requestSsl() is now idempotent — before requesting, it checks the site's certificates and returns early ("SSL already covered") when an ACTIVE cert already includes every requested domain, so re-pressing Connect issues nothing and triggers no auto-alias; (2) new PloiService::deleteAliasById() + dedupeAliases() (keeps the oldest row per canonical domain, logs removals); (3) dedupe runs self-healingly at three points: start of addAlias (each Connect run cleans past mess), the already-covered early-return, and the Created/status page's live query — which polls every 15s while SSL is pending, so cleanup lands right after Ploi's async issuance creates the duplicate rows; (4) ploi:audit --fix now deletes precise extra row IDs via deleteAliasById instead of first-match domain deletes. Existing live duplicates: run `php artisan ploi:audit --fix` once, or simply open the website's status page (auto-heals now).

- **[FEATURE]** `php artisan ploi:audit [--fix]` — alias/domain health check command
  - Files changed: `app/Console/Commands/PloiAuditAliases.php` (new)
  - Details: Live nginx showed `server_name ... zz-test-tower.wpbun.xyz zz-test-tower.wpbun.xyz` — a duplicate alias row on the Ploi site (created before addAlias() gained its duplicate pre-check; new duplicates can no longer be created but existing ones need cleanup). New read-only audit command compares Ploi's live alias list (with row IDs) against websites.domain and reports: DUPLICATE alias rows, ADMIN HOST registered as an alias (must only ever be the primary domain), ORPHAN aliases (on Ploi, no website row -> visitors 404), INACTIVE-website aliases, and MISSING aliases (website has domain but Ploi doesn't). `--fix` removes duplicate rows (deleteAlias once per extra copy — it deletes the first matching row ID) and admin-host aliases; orphans/missing are report-only since they may be intentional. Run on live: `php artisan ploi:audit` then `--fix`. Architecture context: current TenantResolver design is transport-agnostic — it works identically whether domains reach nginx via aliases on one Ploi site (current) or one-Ploi-site-per-customer (advisor's recommendation); a future site-per-customer migration only needs a websites.ploi_site_id column + PloiService::createSite() + symlinked webroot deploy script, no resolver changes.

- **[FEATURE]** Multi-tenancy hardening: canonical TenantResolver, reserved admin host, domain normalization + unique index, unknown-host 404
  - Files changed: `app/Services/Tenancy/TenantResolver.php` (new), `config/tenancy.php` (new), `database/migrations/2026_07_10_120000_normalize_and_index_website_domains.php` (new), `app/Providers/AppServiceProvider.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `app/Http/Controllers/WebsiteController.php`, `app/Http/Controllers/Api/HomepagePropertiesController.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Http/Controllers/Auth/{GoogleAuthController,RegisteredUserController}.php`, `app/Models/Website.php`, `.env.example`
  - Details: Architecture audit for "admin domain (nobu.wpbun.xyz) sometimes serves a customer website". ROOT CAUSES FOUND: (a) no admin-host concept existed — the admin domain went through the same Website::where('domain', host) lookup as tenants, so ANY tenant row containing nobu.wpbun.xyz (nothing prevented it: domain validated as nullable|string only, no uniqueness, no normalization, no reserved list) captured the admin domain; (b) host->website resolution was DUPLICATED in 4+ places (HandleInertiaRequests, WebsiteController, HomepagePropertiesController, inline auth-controller queries) with drifting fallbacks — WebsiteController's last resort was `Website::first()` (any row, even inactive tenants) so a deactivated/missing default made the admin domain serve the lowest-id site; (c) ?website= preview param was honored on EVERY host incl. tenants; (d) duplicate domains allowed -> `first()` = insertion-order lottery ("sometimes" symptom); (e) websites.domain had no index (full scan xN resolvers per request — blocks scaling to thousands of domains). FIX: new App\Services\Tenancy\TenantResolver (container-scoped, per-request memo + 60s cross-request domain cache with miss sentinel): admin host (config tenancy.admin_host / ADMIN_HOST env, default nobu.wpbun.xyz; www/case-insensitive) and local dev hosts ALWAYS resolve to the default website; preview param honored only on admin/local hosts; other hosts resolve by exact normalized domain; unknown hosts return null -> public pages abort 404 (config tenancy.unknown_host, 'default' as rollback switch). All four resolution sites now delegate to it; admin pages pin shared branding to the default website. WebsiteManagementController store/update now normalize the domain (lowercase, scheme/port/path/www stripped) and validate unique + reserved-admin-host. Migration canonicalizes existing domain values, nulls exact duplicates (keeps oldest, logs), and adds a unique index (plain-index fallback if blocked). Website model saved/deleted hooks flush the resolver's domain cache. NO cross-domain redirects existed anywhere (no redirect()->away, no forceRootUrl/forceScheme; all redirects are relative/route-based = same-host) — the "redirect" symptom was Host-based RENDERING capture, now impossible. Verified via tinker: admin host (any casing/www) -> default site, unknown host -> 404, local -> default, normalization correct; migration ran clean. Deploy: live .env should set ADMIN_HOST explicitly + php artisan migrate + config:cache.

- **[FIX]** Proactive flaw sweep across Websites + Real Estate admin modules: dead buttons, debug console noise, native alerts/confirms, generic Admin titles
  - Files changed: `resources/js/Pages/Admin/Websites/{Pages,Edit,EditHomePage}.jsx`, `resources/js/Pages/Admin/Buildings/{Create,Edit}.jsx`, `resources/js/Pages/Admin/RealEstate/Schools.jsx`, `resources/js/Pages/Admin/SavedSearches/Index.jsx`
  - Details: User asked for a proactive audit of every Websites-module page plus the Buildings/Real Estate modules. Found and fixed: (1) Websites Pages.jsx — "Add Page" button did NOTHING (no onClick/href), the empty-state "Create your first page" button was equally dead, and handleDelete only console.logged after confirming (no backend page-delete exists) — removed all three dead controls, added a note that pages are auto-created, and fixed the kebab's hardcoded aria-expanded="true" to dynamic + aria-label. (2) Buildings Edit.jsx — 30+ debug console.log lines fired on EVERY page load/submit/toggle (component-load dumps, amenity-check loops, form-data dumps) — all stripped; image upload/delete used 5 native alert()s and a native confirm() — replaced with an inline red error banner in Media & Links and the shared ConfirmDialog for image deletion. (3) Buildings Create.jsx — same upload alert()s and console.errors replaced with the inline banner. (4) Websites Edit.jsx — leftover logo-file console.logs removed. (5) Websites EditHomePage.jsx — icon delete used confirm()+alert()+window.location.reload(): now ConfirmDialog + dismissible error toast + router.reload(); icon edit's window.location.reload also switched to router.reload; success/error console noise removed. (6) RealEstate Schools.jsx — used the unsupported `header` prop so the top bar said generic "Admin" (same flaw class as old Maintenance Fees): now title="Schools"; delete + batch-geocode native confirms → ConfirmDialogs (danger/neutral); geocode alert() results → inline dismissible success/error banner. (7) SavedSearches Index.jsx — same header-prop flaw → title="Saved Searches". Remaining native confirms in Contacts/BlogCategories/Icons pages left for a future sweep (outside the requested modules).

- **[FIX]** Websites Edit: show the actual server IP in Domain & Hosting and the connect-domain dialog
  - Files changed: `app/Http/Controllers/Admin/WebsiteManagementController.php`, `resources/js/Pages/Admin/Websites/Edit.jsx`
  - Details: The connect-domain dialog said "make sure the DNS A record points to the server" without saying WHICH IP — users had no way to know. edit() now passes `serverIp` (PloiService::getServerIp(): PLOI_SERVER_IP env, else Ploi API, same source the Create wizard uses; frontend falls back to 157.180.26.95). The Domain & Hosting section description and the ConfirmDialog message both show the IP, and the dialog clarifies apex "@" and "www" records are both needed or SSL issuance fails.

- **[STYLE]** Websites Edit: "Connect domain to Ploi" native confirm() replaced with styled ConfirmDialog
  - Files changed: `resources/js/Pages/Admin/Websites/Edit.jsx`
  - Details: User screenshot showed the native browser confirm popup when clicking "Connect domain to Ploi" on the Edit Website page. Replaced with the shared ConfirmDialog (neutral variant, "Connect" label) showing the domain being connected and a reminder that the DNS A record must already point to the server; confirm fires the existing retry-ploi POST.

- **[STYLE]** Button color captions: role-based instead of color-named
  - Files changed: `resources/js/Pages/Admin/Websites/{Create,Edit}.jsx`, `resources/js/utils/brandColors.js`
  - Details: Edit Website review passed everything functionally but flagged misleading Button Colors captions — "Primary BG" said "Blue buttons" while the site's actual primary was red (#912018), and "Secondary BG" said "Red/Brown buttons" while blue. The captions were hardcoded to the DEFAULT palette's colors, so any site with customized colors makes them wrong. Replaced color-named captions with role descriptions that stay true regardless of chosen color: Primary = "Available Building, Sign Up buttons", Secondary = "Contact Agent, Show All Listings", Tertiary = "Request Tour buttons", Quaternary = "Outline buttons (View Details)". Applied in the Create wizard, Edit page, and the shared brandColors label map (detail page).

- **[FIX]** Deep-test round: styled delete-confirmation dialogs replace native confirm() across admin lists; debounced taxonomy search; contextual search empty states
  - Files changed: `resources/js/Components/Admin/ConfirmDialog.jsx` (new), `resources/js/Pages/Admin/{Neighbourhoods,SubNeighbourhoods,Amenities,MaintenanceFeeAmenities,Developers}/Index.jsx`, `resources/js/Pages/Admin/Buildings/Index.jsx`, `resources/js/Pages/Admin/Websites/Index.jsx`
  - Details: Reviewer deep-tested Neighbourhoods/Sub-Neighbourhoods CRUD (create/edit/parent-linkage all pass) and flagged three things. (1) Native confirm() on Delete freezes the renderer and clashes with the polished custom modals — new reusable ConfirmDialog component (danger/neutral variants, warning icon, backdrop + Escape cancel, autofocused confirm) wired into 7 list pages: Neighbourhoods, Sub-Neighbourhoods, Amenities, Maintenance Fees, Developers, Buildings, and Websites (both Delete with the Ploi-alias note and Duplicate as a neutral variant). Remaining confirm() call sites (Contacts, BlogCategories, Icons, Websites Pages/EditHomePage/Edit retry-alias, Schools) left for a later sweep. (2) Search lag on Neighbourhoods/Sub-Neighbourhoods: router.get fired on EVERY keystroke with no debounce — now 300ms debounced (same pattern as Maintenance Fees), still server-side so all pages are searched. (3) Search empty state said "No neighbourhoods found. Create your first one!" even when the search simply had no matches — now shows `No results for "query"` when a search filter is active (both taxonomy pages). Reviewer's ZZ Deep Test records live on the live site and must be deleted there (their tooling refuses permanent deletes by design).

- **[STYLE]** Taxonomy modules review: Maintenance Fees module modernized to match the rest of the admin; Amenities filtered count
  - Files changed: `resources/js/Pages/Admin/MaintenanceFeeAmenities/Index.jsx` (rewritten), `app/Http/Controllers/Admin/MaintenanceFeeAmenityController.php`, `resources/js/Pages/Admin/Amenities/Index.jsx`
  - Details: Reviewer audit of the 4 taxonomy modules found Maintenance Fees the odd-one-out (old Breeze design language) and one minor Amenities UX nit. Maintenance Fees fixes: (1) header showed generic "Admin" because the page passed an unsupported `header` prop — now `title="Maintenance Fees"`; (2) page heading "Manage Amenities" and button "+ Create New Amenity" renamed to "Maintenance Fees" / "Add Maintenance Fee" (it is a separate concept from building Amenities); (3) old submit-button search replaced with debounced (300ms) instant server-side search via router.get preserveState — searches ALL pages, not just the current one; (4) full visual rebuild in the modern design language: header + subtitle, Total/Active stat cards, slate-palette table (Category as badge, responsive column hiding below md), modern pagination with results summary, modern modals with shared form fields; (5) icon upload now accepts SVG/PNG/JPG/WebP like the Amenities module — frontend accept attr + controller store/update validation switched from `mimes:svg` to the shared `SvgOrImage` rule; (6) latent silent-validation bug fixed: edit submitted via router.post with _method PUT so useForm `errors` never populated — now useForm post() with transform() spoofing (create also resets the transformer). Amenities: "Total Amenities" stat now shows the filtered match count while searching ("N Matching Amenities (of 14 total)"). Neighbourhoods/Sub-Neighbourhoods: no changes needed per review. Reviewer's "ZZ Test Neighbourhood (delete me)" lives on the live site DB — delete there.

- **[FIX]** Review follow-up 2: created-page "cleanly ran" copy, wizard name tooltips, combobox arrow-key navigation; mobile drawer verified working in headless Chromium
  - Files changed: `resources/js/Pages/Admin/Websites/Created.jsx`, `resources/js/Pages/Admin/Websites/Create.jsx`, `resources/js/Components/Admin/QuickCreateSelect.jsx`
  - Details: Reviewer retest (against a stale live bundle) re-reported the mobile drawer as missing. Verified the current build end-to-end in headless Chromium (playwright-core + cached ms-playwright Chromium, 390x844 viewport, temp admin user, php artisan serve): drawer present in DOM before click, hamburger visible, click flips aria-expanded to true, panel slides to translate-x-0 with 10 nav links visible, Escape closes — all 10 assertions passed, screenshot confirmed. Conclusion: the fix shipped in the previous commit works; the LIVE server is serving an old bundle (reviewer also still sees "Form errors: Object" and duplicate "Status" labels, both long gone from source) — live needs git pull + hard refresh. Test artifacts (script, screenshot, temp user) removed. New fixes this round: (1) Websites Created page claimed "Everything ran cleanly: database, Ploi alias, and SSL." even when Ploi/SSL were Skipped (report ok=null counts as not-failed) — now shows "Website created — domain steps skipped (no custom domain)." when neither step actually ran; (2) building name/address in the website wizard cards (step-1 grid + locked card) truncate with title tooltips now; (3) QuickCreateSelect combobox (already search-as-you-type) gained ArrowDown/ArrowUp highlight + Enter-to-select keyboard navigation. Duplicate "Status" labels and "Form errors" console noise: already fixed in prior commits (Construction Stage / Visibility Status). Reviewer's ZZ Test records do not exist in the local DB — they were created on the live site and must be deleted there.

- **[FIX]** Review follow-up: always-mounted sliding mobile drawer, dashboard layout rebalance, stat-card focus/aria polish, status label rename, developer logo fallback, CSV template download
  - Files changed: `resources/js/Layouts/AdminLayout.jsx`, `resources/js/Pages/Admin/Dashboard.jsx`, `resources/js/Pages/Admin/Websites/Index.jsx`, `resources/js/Pages/Admin/Buildings/{Index,Create,Edit,Import}.jsx`, `resources/js/Pages/Admin/Developers/Index.jsx`
  - Details: Reviewer retested and reported the mobile drawer still not opening (button state flips but "nothing renders") despite the drawer existing in source as a conditionally-rendered block. Rebuilt it as an ALWAYS-MOUNTED drawer: panel stays in the DOM and slides via `transform transition-transform` (`-translate-x-full` ↔ `translate-x-0`), backdrop fades via opacity with `pointer-events-none` when closed, wrapper `aria-hidden` toggles, button gains `aria-controls="mobile-navigation-drawer"` and toggles state, Escape closes (new useEffect). This is more robust against the reviewer's DOM-snapshot testing (drawer element is always findable) and adds the requested slide transition; verified the id/markup lands in the built AdminLayout chunk. Dashboard empty-gap: Quick Actions moved to a horizontal 3-up strip under the stats; Websites and Recent Activity now sit side-by-side in a lg:grid-cols-2 with items-start — no more short right column. Stat cards on Dashboard got aria-labels ("8 Total Properties" reads correctly) and focus-visible rings. The "black outline" on Total Websites/Total Buildings cards was the always-on selected ring for the default 'all' filter state — removed the default-state highlight (only Active/Featured show a colored border when engaged) and swapped default focus outlines for focus-visible rings. Buildings Create/Edit: labels renamed per feedback to "Construction Stage" (development_status) and "Visibility Status" (status) with clarified helper text. Developers list: broken logo thumbnails now fall back to the initial-letter avatar via new DeveloperLogo onError component. Buildings CSV import: "Download sample CSV template" button (client-side Blob, canonical auto-mapped headings + example row) plus a recognized-columns list on the upload step. "Form errors: Object" on list/view pages: string no longer exists anywhere in source or the built bundle after the previous round's fixes — cannot recur once deployed. Full npm run build (client+SSR) passes.

- **[FIX]** Buildings module review round: status field split (data-integrity), title framework leak, table overflow, filters, clickable rows, thumbnail fallback, console cleanup
  - Files changed: `database/migrations/2026_07_10_100000_add_development_status_to_buildings_table.php` (new), `app/Models/Building.php`, `app/Http/Controllers/Admin/BuildingController.php`, `resources/js/Pages/Admin/Buildings/{Create,Edit,Show,Index}.jsx`, `resources/js/Pages/Admin/Websites/Edit.jsx`, `resources/js/{app,ssr}.jsx`, `.env`, `.env.example`
  - Details: Client review of /admin/buildings reported 10 issues. (1) DUPLICATE STATUS (root cause): Create and Edit forms rendered two selects both bound to the same `data.status` — Basic Information offered construction phases (pre_construction/under_construction/completed/sold_out) while Status & Settings offered publish states (active/inactive/pending); saving a construction phase into `status` breaks every public `status='active'` query and the admin list badge (falls back to "pending"). Split into two real fields: new nullable `buildings.development_status` column (migration also rescues any rows where a construction phase was stored in `status` — moves it to development_status and resets status to active), model fillable, store/update validation now `status in:active,inactive,pending` + `development_status in:pre_construction,...`, Basic Information select renamed "Development Status" (optional, "Not specified" default, helper text pointing to the publish Status), edit() passes development_status, admin Show page displays a Development Status badge. (2) "Form errors: Object" console errors on list/view pages: not reproducible in current source — reviewer's bundle (app-BtOBZj3D.js) predates round-1 fixes and exists in no committed manifest; the only remaining source of that string was Websites/Edit.jsx's onError console.error, which also masked the SAME silent-validation bug fixed on Websites/Create (useForm errors + router.post): switched to useForm post() with transform() for the _method=PUT spoof, removed the console.error. Also removed emoji console.log/error noise from Buildings Create/Edit AI-description handlers (errors already surface via setAiDescriptionError). (3) "- Laravel" title: .env had APP_NAME=Laravel which VITE_APP_NAME bakes into the bundle title template; set APP_NAME="Nobu Residences" in .env and .env.example and changed the app.jsx/ssr.jsx fallback from 'Laravel' to 'Nobu Residences' (also fixes MAIL_FROM_NAME). Deploy note: live server .env needs the same APP_NAME change + config:cache. (4) Table overflow at 1150px: building name cell now truncates (max-w-[220px] + title tooltip), Location hidden below md, Type and Featured hidden below xl (th+td pairs), overflow-x-auto retained as fallback. (5) Broken thumbnail: new BuildingRowThumb component with onError fallback to the placeholder icon (also treats /images/no-image-placeholder.jpg as missing). (6) Mobile nav: fixed in round 1 (stale bundle); rebuild keeps it current. (7) Header logout "inconsistency": AdminLayout is shared — pure stale-cache artifact, no code change. (8) Filters added: status/type/featured selects beside search (client-side within the current page, consistent with existing search; server-side pagination already existed at >10 rows). (9) Stat cards now filter on click (Total→clear, Active→status=active, Featured→featured only) with selected ring; building name links to the View page. (10) Emoji star on Show replaced with SVG (repo no-emoji rule). Migration run locally; php -l passes; full npm run build (client+SSR) passes.

- **[FIX]** Websites module review round: silent create-form validation, thumbnail fallback, logo/swatch visibility, list search+filter, clickable rows, pluralization, a11y
  - Files changed: `resources/js/Pages/Admin/Websites/Create.jsx`, `resources/js/Pages/Admin/Websites/Show.jsx`, `resources/js/Pages/Admin/Websites/Index.jsx`, `resources/js/utils/brandColors.js` (new)
  - Details: Client review of /admin/websites (list, detail, create wizard) reported 12 issues. (1) SILENT VALIDATION (root cause): Create.jsx destructured `errors` from `useForm()` but submitted via plain `router.post()` — router-post validation errors go to page props, never into useForm's `errors`, so every `<InputError>` stayed permanently empty and an empty Website Name looked like a dead button. Fixed by submitting through useForm's own `post()` (errors now populate), adding a client-side guard (`setError('name', 'Website name is required.')` + focus), `required` + `aria-invalid` + red ring on the name input, label now "Website Name *", and removed the leftover `console.error` onError handler. Server-side `slug` required is satisfied because store() auto-generates slug from name. (2) Broken "Lakeshore Vista" thumbnail: new `BuildingThumb` component with `onError` state falls back to the same building-icon placeholder used for missing images; used in both the step-1 grid and the step-2 locked card. (3) Invisible white logo on detail page: logo preview now sits on a checkered backing (`CHECKERBOARD_STYLE` in new `resources/js/utils/brandColors.js`). (4) Mobile nav: already fixed in the previous round (stale build assets) — this round's rebuild keeps it current. (5) List search + status filter added (client-side by name/slug/domain + all/active/inactive select; server pagination deferred until counts warrant it), with a "no matches / clear filters" empty state distinct from "no websites". (6) Stat cards now filter the table on click (Total→all, Active→active) with a selected ring; the website name cell is a Link to the detail page. (7) Delete/Duplicate already existed with confirm() dialogs — reviewer saw a stale bundle and a default-only dataset (Delete is intentionally hidden for the default website); added an explanatory "The default website cannot be deleted." note in the kebab menu for the default site. (8) "1 pages" → proper singular/plural. (9) Detail page color names: `name.replace('_',' ')` only replaces the FIRST underscore ("Button Primary_bg") — replaced with shared `brandColorLabel()` friendly-label map (same wording as the create wizard) with a title-case fallback. (10) Color swatches on detail page now render on a checkered backing with `role="img"` + aria-label + title hex so white chips are perceivable. (11) Duplicate confirm existed already (see 7). (12) Kebab button got `aria-label`/`aria-haspopup`/`aria-expanded`; decorative SVGs `aria-hidden`. Full `npm run build` (client+SSR) passes.

- **[FIX]** Admin Dashboard review round: real stats, activity feed, logout, clickable cards, a11y, mobile-nav rebuild
  - Files changed: `app/Http/Controllers/Admin/AdminController.php`, `resources/js/Pages/Admin/Dashboard.jsx`, `resources/js/Layouts/AdminLayout.jsx`
  - Details: Client review of /admin/dashboard reported 10 issues. (1) "Mobile nav broken / hamburger does nothing": AdminLayout source already contains a working drawer (fixed overlay + 85vw panel wired to sidebarOpen state) — root cause was stale committed build assets that predate the drawer; `npm run build` (client+SSR) regenerated them. Also added `aria-label`/`aria-expanded` to the hamburger and `aria-label` to the drawer close button. (2) Fake stats: dashboard() previously hardcoded total_properties/active_listings/pending_listings to 0 while Dashboard.jsx hardcoded +12%/+8%/-3% trend badges and two fake "Recent Activity" entries. Controller now returns real counts (`Building::count()`, status=active, status=pending, `User::count()`); the impossible trend badges were removed entirely (no historical snapshots exist to compute real deltas — replaced with a drill-in chevron) and Recent Activity is a real feed (`recentActivity()`: latest 5 buildings + latest 5 contact inquiries merged, sorted, top 6, diffForHumans timestamps) with an empty state. (3) No logout: added a POST `route('logout')` icon button (Inertia `as="button"`) in the admin header next to the profile link. (4) Stat cards are now `<Link>`s (buildings index for the three property cards, users index for Total Users) with hover affordance. (5) Empty state: when total_properties is 0 a "Get started — add your first building" banner with CTA renders above the stats. (6) Recent Activity rows now use `divide-y` + per-row padding instead of cramped stacking. (7) Layout rebalanced: Recent Activity moved from the right rail into the wide left column under Websites, killing the lower-left void. (8) Websites card rows are now `<Link>`s to `admin.websites.show`; the card also stopped being hardcoded — `websitesSummary()` returns real `Website::withCount('pages')` data (name, domain, active/inactive from is_active, pages count replaces the always-0 "properties" count) plus an empty state. (9) Avatar link got an aria-label with the user's name (initial marked aria-hidden); decorative SVGs marked `aria-hidden="true"`. (10) View Site changed to a plain `<a target="_blank" rel="noopener noreferrer">` (Inertia Link ignores target for internal visits). PHP lint + full `npm run build` (client & SSR) pass.

## 2026-07-09

- **[FEATURE]** Building domains: one-click launch flow (client item #7)
  - Files changed: `app/Http/Controllers/WebsiteController.php`, `app/Models/WebsitePage.php`, `app/Http/Controllers/Admin/WebsiteManagementController.php`, `app/Http/Controllers/Admin/BuildingController.php`, `resources/js/Pages/Admin/Websites/Create.jsx`, `resources/js/Pages/Admin/Buildings/Edit.jsx`, `docs/INFRA-PLAN.md`
  - Details: Exploration showed the multi-domain system was ~90% built (website-create wizard already starts with a building picker; Ploi alias+SSL auto-provisioning complete; Host-header routing works) — only 3 gaps closed, no new tables/routes/migrations. (1) Restored the "Use Building Page as Homepage" behavior: extracted the tail of `buildingDetail()` into `renderBuildingDetailPage(Building)` and `home()` now serves the full BuildingDetail page at "/" for NON-default sites with the flag on (default site guarded so the main Nobu Welcome design is immune to stale flags; deleted building → `find()` null → Welcome fallback; FK is ON DELETE SET NULL anyway). (2) `WebsitePage::getDefaultHomeContentForBuilding()` personalizes new building-site home content — hero welcome/subheading/image, about title/image/overview (building description), key facts rebuilt from floors/units/year_built/sqft_range/avg_price_per_sqft, FAQ brand-name swap, footer description/copyright — wired into `store()` via `$website->homepageBuilding`. (3) "Launch Website" header button on Buildings Edit → `/admin/websites/create?building_id=X`; Create.jsx mount-effect preselects that building via existing `chooseBuilding()` and jumps to step 2; buildings that already have a site show a green "Website: {domain}" badge (new `linkedWebsite` prop from `edit()`). Verified end-to-end locally with a test website (`?website=` preview since Host matching skips .test): checkbox ON → "/" renders BuildingDetail component; OFF → Welcome with "WELCOME TO THE SKYLINE TOWER" personalized hero; default site "/" unchanged (Welcome); direct /building/{slug} route 200 after extraction. Full npm run build (client+SSR) passes. INFRA-PLAN.md Part 1 rewritten as the implemented one-click launch guide (Ploi env vars needed on live: PLOI_API_TOKEN/SERVER_ID/SITE_ID).

- **[FEATURE]** Client feedback round: 9 workstreams (fees, AI description, map fix, email verification, alerts CTA, FUB pixel, developer SEO, FAQ system, infra plan)
  - Files changed: `app/Http/Controllers/PropertySearchController.php`, `app/Http/Controllers/WebsiteController.php`, `app/Http/Controllers/ProfileController.php`, `app/Http/Controllers/Admin/{BuildingController,DeveloperController,WebsiteManagementController,FaqController,SavedSearchAdminController}.php`, `app/Http/Middleware/HandleInertiaRequests.php`, `app/Models/{User,Building,Developer,Website,Faq}.php`, `app/Services/SavedSearchAlertService.php`, `app/Jobs/GeneratePropertyAiContentJob.php`, `app/Notifications/Auth/VerifyEmailNotification.php`, `app/Support/Schema.php`, `resources/views/app.blade.php`, `routes/web.php`, `resources/js/Website/Components/{ClusteredPropertyMap,PropertyDetail/PropertyDetailsSections,PropertySearch/SaveSearchModal}.jsx`, `resources/js/Website/Pages/{Search,BuildingDetail,SchoolDetail,Blog}.jsx`, `resources/js/Website/Sections/{PropertyDetail/PropertySections,BuildingDetail/BuildingSections}.jsx`, `resources/js/Website/Global/Components/FAQ.jsx`, `resources/js/Pages/{DeveloperDetail,Blog,BlogDetail,Contact}.jsx`, `resources/js/Pages/Admin/{Developers/Index,Faqs/Index,SavedSearches/Index,Buildings/Edit,Buildings/Create,Websites/Edit}.jsx`, `resources/js/Layouts/AdminLayout.jsx`, 6 new migrations `2026_07_09_1000xx_*`, `docs/INFRA-PLAN.md`
  - Details: (1) FEES: 'Parking Maintenance'/'Locker Maintenance' amenity rows (updateOrInsert migration) + `parking_maintenance_fee`/`locker_maintenance_fee` decimal columns on buildings with admin inputs and listing-page "Fees & assessments" rows (Repliers `condo.fees.parking/locker` first, building values as fallback, empty auto-hidden). (2) AI DESCRIPTION (was never rendering): `aiDescription` prop now threaded into PropertyDetailsSections — AI `overview` is the primary Overview text, expanded state shows `detailed` + "Original listing remarks" (MLS compliance); the two blocking first-view Gemini calls in `propertyDetail()` replaced with `GeneratePropertyAiContentJob::dispatchAfterResponse()` (Cache::add dedupe, 300s). (3) MAP PINS on city pages (root cause: Toronto-centered lat/long/radius ANDed with city=Aurora → empty): `getMapCoordinates()` retries without spatial params on empty when a query is scoped (skipped for user-drawn polygons), returns `fit_bounds` so the frontend pans to results (single-point guard, loop-safe via lastBoundsRef reset), GTA polygon post-filter skipped for matched `matchGtaCity()` queries, and Repliers exceptions/empty results fall back to the local-DB coordinates path (which now supports city filter + fit_bounds + uniform `clusters:[]`). Verified: 401 now serves local pins instead of a 500; full verification needs a valid live REPLIERS_API_KEY. (4) EMAIL VERIFICATION: `MustVerifyEmail` enabled, queued branded `VerifyEmailNotification` (Website::default name), `email_verified_at` backfill migration grandfathers existing users (verified 0 unverified after), ProfileController re-sends on email change; MAIL_MAILER stays `log` — SMTP later is .env-only. Verified end-to-end via Registered event → queue:work → branded mail in log. (5) ALERTS: new SaveSearchModal (email_alerts default ON, Daily/Weekly/Monthly frequency, criteria summary, guest intent preserved in sessionStorage and modal reopens after login), "Get alerts" bell CTA in Search results header, "Notify me when units become available" card+mobile CTA on BuildingDetail (building-scoped saved search); `findNewListings()` gained min/max_sqft and building_id (address-prefix) filters; new /admin/saved-searches page with stats (getAlertStats) + table + sidebar link. Scheduler already ran alerts daily 08:00/18:00 — needs cron+queue worker on live. (6) FUB PIXEL: `websites.tracking_scripts` TEXT column, admin Websites Edit "Tracking & Integrations" textarea, raw `{!! !!}` injection in public <head> only (verified present on / and absent on /admin) + guarded `widgetTracker('set',{email})` identify for logged-in users — client pastes the FUB snippet when they get it. (7) DEVELOPER SEO: persisted unique `slug` column (backfilled), UUID/name-variant URLs 301 to canonical slug (verified), fillable bug fixed (website/description/established_year were silently dropped), new meta_title/meta_description/projects_completed/under_construction/upcoming/highlights/awards columns, per-page `seo` prop + JSON-LD mechanism in app.blade.php (server-rendered; verified in view-source), DeveloperDetail.jsx fake content (154 Ratings, "90 Years", 90000/10/12 stats, fabricated awards) replaced with data-driven sections that hide when empty, admin modal expanded with all content/SEO fields incl. repeatable highlights/awards rows (JSON via FormData). (8) FAQ SYSTEM: `faqs` table (page_type: global/home/search/buildings/building_detail/developers/developer_detail/blog/contact/compare/schools), Faq::forPage merges page + global, /admin/faqs CRUD (modal, filter, sidebar), FAQ.jsx lorem defaults DELETED (renders null when empty — kills placeholder text on Blog/BuildingDetail/DeveloperDetail/SchoolDetail), wired with FAQPage JSON-LD into developers, developer detail, blog, blog detail, building detail, school detail, contact, search (verified schema on /developers). (9) `docs/INFRA-PLAN.md`: building-domains playbook (existing websites.domain routing, launch steps, gaps incl. website_id FK + one-click launch button, subdomain-first recommendation) + AWS/Cloudflare migration runbook (Cloudflare can't run PHP — DNS/CDN only; Forge/Ploi-managed VPS + RDS + S3 + SES, dump-import self-repair note, staging smoke tests, DNS cutover + rollback, ~$40-70/mo). All migrations run locally; full `npm run build` (client+SSR) passes; key public pages smoke-tested 200.
  - Files changed: `resources/js/Website/Components/ClusteredPropertyMap.jsx`, `database/migrations/2026_07_09_210000_add_mls_sqft_stats_to_buildings_table.php`, `app/Models/Building.php`, `app/Jobs/RefreshBuildingPriceRangeJob.php`, `app/Console/Commands/RefreshBuildingMlsStats.php`, `resources/js/Website/Sections/BuildingDetail/BuildingGallery.jsx`
  - Details: (1) Search-map cluster circles shrunk from 40/50/60px to 28/34/42px (font 12/12/11, pad 3) per client feedback that they were too big. (2) Added `sqft_range` and `avg_price_per_sqft` columns to buildings (idempotent migration). `Building::refreshPriceRangeFromMls()` now also collects Repliers `details.sqft` (handles both numeric and "500-599" range strings via new `parseSqftBounds()`), saving a formatted sqft range ("510 sqft - 5,001 sqft") and average price per square foot ("$948", price / sqft midpoint averaged across matched listings) in the same API pass as price_range — no extra Repliers calls. Runs via the existing RefreshBuildingPriceRangeJob on every building save; new `php artisan buildings:refresh-mls-stats [--id=]` command bulk-refreshes existing buildings (run once on live after deploy). (3) Root cause found on display side: `Building::getDisplayData()` whitelists fields and never included price_range/sqft_range — so BuildingGallery's existing `buildingData?.sqft_range` read could never fire; added building_type, price_range, sqft_range, avg_price_per_sqft to the whitelist and a new "Avg Price / Sqft" sidebar row. Verified: `parseSqftBounds` unit-tested via tinker (ranges, numerics, garbage, reversed ranges), refresh pipeline exercised up to the Repliers call (local API key is invalid, 401 logged — works on live), full npm run build passes. Confirmed already-correct per client: year_built/floors/total_units/building_type are manual admin inputs, management_name is a plain text input, developer is a taxonomy.

- **[FIX]** Property detail mobile feedback round: building card position, one-line title/breadcrumb, Comparable Sales card width, search-on-suggestion-click
  - Files changed: `resources/js/Website/Sections/PropertyDetail/PropertySections.jsx`, `resources/js/Website/Global/Components/PropertyHeader.jsx`, `resources/css/idx-ampre-styles.css`, `resources/css/enhanced-property-listings.css`, `resources/js/Website/Components/PropertyDetail/ComparableSales.jsx`, `resources/js/Website/Components/PropertyDetail/MoreBuildings.jsx`, `resources/js/Website/Components/PropertyDetail/DeveloperBuildings.jsx`, `resources/js/Website/Components/PropertySearch/IDXAmpreSearchBar.jsx`
  - Details: (1) Moved the MerchandiseLofts building-info card ("N Condos for Rent/Sale") from below MarketData to directly after PropertyDetailsSections, i.e. right under the Listing details / MLS number card. (2) PropertyHeader title now 26px + truncate on mobile (40px desktop unchanged) and the building/neighbourhood breadcrumb is text-base + truncate on mobile, so both render on one line; added min-w-0 to the flex parent so truncation works. (3) Root cause of full-width Comparable Sales cards on mobile: enhanced-property-listings.css (which sized .carousel-item) is not imported anywhere; the globally-imported idx-ampre-styles.css only sized .idx-ampre-property-card, so the .carousel-item wrapper had no width and PropertyCardV5's mobile w-full container class made cards full-width. Added a 240px .mobile-listings-scroll .carousel-item rule to idx-ampre-styles.css, switched the mobile carousel branches of ComparableSales/MoreBuildings/DeveloperBuildings from className="w-[300px]" to "w-full", and removed DeveloperBuildings' leaking inline .carousel-item{min-width:300px} rule — now ~1.6 cards visible with the next card peeking. (4) IDXAmpreSearchBar: clicking an autocomplete suggestion now applies the search immediately — handleSearch accepts a dataOverride param (state not yet committed at click time), handleSuggestionSelect calls it with the fresh location, and the search button was changed to onClick={() => handleSearch()} so the click event isn't passed as override. Verified with full npm run build (client + SSR pass).

- **[FIX]** Make 8 migrations idempotent for dump-imported DBs (blogs "table already exists" error)
  - Files changed: `database/migrations/2025_12_03_202114_create_blogs_table.php`, `database/migrations/2025_12_08_211942_add_neighbourhood_fields_to_buildings_table.php`, `database/migrations/2025_12_09_214331_create_neighbourhoods_table.php`, `database/migrations/2025_12_09_214331_create_sub_neighbourhoods_table.php`, `database/migrations/2025_12_09_214412_add_neighbourhood_ids_to_buildings_table.php`, `database/migrations/2026_04_06_205513_add_repliers_client_id_to_users_table.php`, `database/migrations/2026_05_17_034951_add_additional_addresses_to_buildings_table.php`, `database/migrations/2026_06_10_130000_create_newsletter_subscribers_table.php`
  - Details: `php artisan migrate` failed with SQLSTATE 42S01 "Table 'blogs' already exists" on a DB imported from a SQL dump where tables exist but the `migrations` records for them are missing. Added `Schema::hasTable()` early-return guards to the create-table migrations and `Schema::hasColumn()` guards to the add-column migrations (blogs, neighbourhoods, sub_neighbourhoods, newsletter_subscribers, buildings neighbourhood/ID/additional_addresses columns, users repliers_client_id). Verified by deleting those 8 migration records locally and re-running `migrate` — all skip cleanly, get recorded, and existing data is untouched.

- **[STYLE]** Mobile slidable Nearby/Similar Listings (like Comparable Sales)
  - Files changed: `resources/js/Website/Components/PropertyDetail/MoreBuildings.jsx`
  - Details: On mobile, the carousel-mode sections rendered by MoreBuildings (Nearby Listings, Similar Listings, More Buildings By Agent on the Property Detail page) used a vertical `grid grid-cols-1` capped at 6 cards. Replaced with the same horizontal swipe row ComparableSales uses (`.mobile-listings-scroll` + `.carousel-item`, 300px cards, hidden scrollbar, all items reachable). Desktop slider unchanged; building-page grid mode (Properties For Sale/Rent) unaffected.

- **[FIX]** `npm run build` hang on Windows — vite build never exits, SSR build never runs
  - Files changed: `vite.config.js`
  - Details: `vite build` completed (`✓ built`) but the node process never exited, so `&& vite build --ssr` never started and the SSR bundle went stale. Diagnosed via `process.getActiveResourcesInfo()`: after the bundle closes, the esbuild child process (ProcessWrap) leaves a pipe (PipeWrap) open, keeping the event loop alive (vite 7.0.6 / esbuild 0.25.8 / Node 22.22 on Windows). Not caused by laravel-vite-plugin (react-only build hung too); no SCSS involved. Fix: `forceExitAfterBuild` plugin in vite.config.js — `closeBundle` (sequential, order post) calls `process.exit(0)` after a 500ms grace. Full `npm run build` now runs both passes and exits 0.

## 2026-07-09 — Admin building/website flow overhaul (client feedback)

**Feedback (paraphrased):** Building creation is too slow because taxonomies (neighbourhoods, sub-neighbourhoods) and developers must be created beforehand; the manual Price Range input should come from MLS instead; a CSV import with field mapping is needed; developer add/edit should be possible inline; the building landing-page (website) flow should be 2 steps and domain connect wasn't working.

**Root causes:**
- Building Create/Edit forms only offered static dropdowns for neighbourhood/sub-neighbourhood/developer — no inline creation, forcing a round-trip to 3 other admin pages first.
- `price_range` was a free-text input even though live MLS (Repliers) data is available per building address.
- No bulk import existed at all.
- Sub-neighbourhood filter on the Create form compared string select values to numeric IDs with `===`, so filtering by neighbourhood always returned an empty list (latent bug).
- Website creation was already 2 steps; domain connect fails because PLOI_API_TOKEN / PLOI_SERVER_ID / PLOI_SITE_ID are not set in the environment — the UI just didn't say so up front.

**Fix:**
- New JSON quick-create endpoints: `POST admin/api/neighbourhoods`, `POST admin/api/sub-neighbourhoods`, `POST admin/api/developers` (firstOrCreate by name).
- New reusable `QuickCreateSelect` component (select + inline "+ New" form) used for Developer, Neighbourhood, and Sub-Neighbourhood on both Building Create and Edit pages.
- Removed the manual Price Range input from both forms; `RefreshBuildingPriceRangeJob` (dispatchAfterResponse) recomputes `price_range` from live Repliers sale listings after every building save via `Building::refreshPriceRangeFromMls()`.
- CSV import wizard at `admin/buildings/import` (upload → column-to-field mapping with auto-guess → results) backed by `BuildingImportController` + `BuildingCsvImportService`; auto-creates developers/neighbourhoods/sub-neighbourhoods by name, skip-or-update duplicate handling, per-row error reporting. "Import CSV" button added to Buildings index.
- Fixed the string/number `===` sub-neighbourhood filter bug.
- Websites Create: amber warning under the Custom Domain field when Ploi auto-provision is not configured, explaining exactly why the domain won't connect and what to set.

**Files changed:**
- app/Http/Controllers/Admin/NeighbourhoodController.php (quickStore)
- app/Http/Controllers/Admin/SubNeighbourhoodController.php (quickStore)
- app/Http/Controllers/Admin/DeveloperController.php (quickStore)
- app/Http/Controllers/Admin/BuildingController.php (price-range job dispatch)
- app/Http/Controllers/Admin/BuildingImportController.php (new)
- app/Services/BuildingCsvImportService.php (new)
- app/Jobs/RefreshBuildingPriceRangeJob.php (new)
- app/Models/Building.php (refreshPriceRangeFromMls)
- routes/web.php (quick-create + import routes)
- resources/js/Components/Admin/QuickCreateSelect.jsx (new)
- resources/js/Pages/Admin/Buildings/Create.jsx
- resources/js/Pages/Admin/Buildings/Edit.jsx
- resources/js/Pages/Admin/Buildings/Index.jsx (Import CSV button)
- resources/js/Pages/Admin/Buildings/Import.jsx (new)
- resources/js/Pages/Admin/Websites/Create.jsx (domain-connect warning)
